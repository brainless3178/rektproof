//! Auto-generated Trident fuzz test for ''
//! Generated by Proktor Security Swarm — Trident Integration
//!
//! Run with: trident fuzz run fuzz_test

use trident_fuzz::prelude::*;
use anchor_lang::prelude::*;

/// Fuzz account storage for the fuzzing campaign.
#[derive(Default)]
pub struct FuzzAccounts {
    pub merkle_tree: AccountId,
    pub authority: AccountId,
    pub noop: AccountId,
    pub recipient: AccountId,
    pub payer: AccountId,
    pub creator: AccountId,
    pub amm_config: AccountId,
    pub pool_state: AccountId,
    pub token_0_mint: AccountId,
    pub token_1_mint: AccountId,
    pub lp_mint: AccountId,
    pub payer_token_0: AccountId,
    pub payer_token_1: AccountId,
    pub payer_lp_token: AccountId,
    pub token_0_vault: AccountId,
    pub token_1_vault: AccountId,
    pub create_pool_fee: AccountId,
    pub observation_state: AccountId,
    pub permission: AccountId,
    pub token_program: AccountId,
    pub token_0_program: AccountId,
    pub token_1_program: AccountId,
    pub associated_token_program: AccountId,
    pub system_program: AccountId,
    pub vault_0_mint: AccountId,
    pub vault_1_mint: AccountId,
    pub creator_token_0: AccountId,
    pub creator_token_1: AccountId,
    pub owner: AccountId,
    pub owner_lp_token: AccountId,
    pub token_0_account: AccountId,
    pub token_1_account: AccountId,
    pub token_program_2022: AccountId,
    pub creator_lp_token: AccountId,
    pub rent: AccountId,
    pub memo_program: AccountId,
    pub permission_authority: AccountId,
    pub recipient_token_0_account: AccountId,
    pub recipient_token_1_account: AccountId,
    pub input_token_account: AccountId,
    pub output_token_account: AccountId,
    pub input_vault: AccountId,
    pub output_vault: AccountId,
    pub input_token_program: AccountId,
    pub output_token_program: AccountId,
    pub input_token_mint: AccountId,
    pub output_token_mint: AccountId,
    pub whirlpools_config: AccountId,
    pub whirlpools_config_extension: AccountId,
    pub config_extension_authority: AccountId,
    pub new_config_extension_authority: AccountId,
    pub whirlpool_one: AccountId,
    pub whirlpool_two: AccountId,
    pub token_mint_input: AccountId,
    pub token_mint_intermediate: AccountId,
    pub token_mint_output: AccountId,
    pub token_program_input: AccountId,
    pub token_program_intermediate: AccountId,
    pub token_program_output: AccountId,
    pub token_owner_account_input: AccountId,
    pub token_vault_one_input: AccountId,
    pub token_vault_one_intermediate: AccountId,
    pub token_vault_two_intermediate: AccountId,
    pub token_vault_two_output: AccountId,
    pub token_owner_account_output: AccountId,
    pub token_authority: AccountId,
    pub tick_array_one_0: AccountId,
    pub tick_array_one_1: AccountId,
    pub tick_array_one_2: AccountId,
    pub tick_array_two_0: AccountId,
    pub tick_array_two_1: AccountId,
    pub tick_array_two_2: AccountId,
    pub oracle_one: AccountId,
    pub oracle_two: AccountId,
    pub config: AccountId,
    pub config_extension: AccountId,
    pub funder: AccountId,
    pub fee_authority: AccountId,
    pub whirlpool: AccountId,
    pub token_program_a: AccountId,
    pub token_program_b: AccountId,
    pub position_authority: AccountId,
    pub position: AccountId,
    pub position_token_account: AccountId,
    pub token_mint_a: AccountId,
    pub token_mint_b: AccountId,
    pub token_owner_account_a: AccountId,
    pub token_owner_account_b: AccountId,
    pub token_vault_a: AccountId,
    pub token_vault_b: AccountId,
    pub existing_tick_array_lower: AccountId,
    pub existing_tick_array_upper: AccountId,
    pub new_tick_array_lower: AccountId,
    pub new_tick_array_upper: AccountId,
    pub token_badge_a: AccountId,
    pub token_badge_b: AccountId,
    pub fee_tier: AccountId,
    pub token_badge_authority: AccountId,
    pub token_mint: AccountId,
    pub token_badge: AccountId,
    pub receiver: AccountId,
    pub reward_authority: AccountId,
    pub reward_vault: AccountId,
    pub collect_protocol_fees_authority: AccountId,
    pub token_destination_a: AccountId,
    pub token_destination_b: AccountId,
    pub reward_owner_account: AccountId,
    pub reward_mint: AccountId,
    pub reward_token_program: AccountId,
    pub new_token_badge_authority: AccountId,
    pub tick_array_0: AccountId,
    pub tick_array_1: AccountId,
    pub tick_array_2: AccountId,
    pub oracle: AccountId,
    pub tick_array_lower: AccountId,
    pub tick_array_upper: AccountId,
    pub reward_token_badge: AccountId,
    pub position_bundle: AccountId,
    pub position_bundle_mint: AccountId,
    pub position_bundle_metadata: AccountId,
    pub position_bundle_token_account: AccountId,
    pub position_bundle_owner: AccountId,
    pub metadata_update_auth: AccountId,
    pub metadata_program: AccountId,
    pub new_fee_authority: AccountId,
    pub token_owner_account_one_a: AccountId,
    pub token_vault_one_a: AccountId,
    pub token_owner_account_one_b: AccountId,
    pub token_vault_one_b: AccountId,
    pub token_owner_account_two_a: AccountId,
    pub token_vault_two_a: AccountId,
    pub token_owner_account_two_b: AccountId,
    pub token_vault_two_b: AccountId,
    pub reward_emissions_super_authority: AccountId,
    pub new_reward_emissions_super_authority: AccountId,
    pub new_collect_protocol_fees_authority: AccountId,
    pub tick_array: AccountId,
    pub position_mint: AccountId,
    pub position_metadata_account: AccountId,
    pub adaptive_fee_tier: AccountId,
    pub new_delegated_fee_authority: AccountId,
    pub initialize_pool_authority: AccountId,
    pub new_initialize_pool_authority: AccountId,
    pub delegated_fee_authority: AccountId,
    pub token_2022_program: AccountId,
    pub destination_token_account: AccountId,
    pub lock_config: AccountId,
    pub bundled_position: AccountId,
    pub position_bundle_authority: AccountId,
    pub new_reward_authority: AccountId,
    pub multisig: AccountId,
    pub proposal: AccountId,
    pub batch: AccountId,
    pub transaction: AccountId,
    pub member: AccountId,
    pub rent_payer: AccountId,
    pub spending_limit: AccountId,
    pub vault: AccountId,
    pub destination: AccountId,
    pub mint: AccountId,
    pub vault_token_account: AccountId,
    pub config_authority: AccountId,
    pub rent_collector: AccountId,
    pub proposal_vote: AccountId,
    pub null: AccountId,
    pub program_config: AccountId,
    pub treasury: AccountId,
    pub create_key: AccountId,
    pub transaction_buffer: AccountId,
    pub initializer: AccountId,
    pub vault_transaction_create: AccountId,
    pub price: AccountId,
    pub faucet_config: AccountId,
    pub admin: AccountId,
    pub mint_account: AccountId,
    pub user_token_account: AccountId,
    pub mint_authority: AccountId,
    pub keeper: AccountId,
    pub pyth_solana_receiver: AccountId,
    pub encoded_vaa: AccountId,
    pub price_feed: AccountId,
    pub guardian_set: AccountId,
    pub state: AccountId,
    pub quote_asset_mint: AccountId,
    pub drift_signer: AccountId,
    pub spot_market: AccountId,
    pub spot_market_mint: AccountId,
    pub spot_market_vault: AccountId,
    pub insurance_fund_vault: AccountId,
    pub base_spot_market: AccountId,
    pub quote_spot_market: AccountId,
    pub serum_program: AccountId,
    pub serum_market: AccountId,
    pub serum_open_orders: AccountId,
    pub serum_fulfillment_config: AccountId,
    pub phoenix_program: AccountId,
    pub phoenix_market: AccountId,
    pub phoenix_fulfillment_config: AccountId,
    pub srm_vault: AccountId,
    pub perp_market: AccountId,
    pub amm_cache: AccountId,
    pub source_vault: AccountId,
    pub old_oracle: AccountId,
    pub user_stats: AccountId,
    pub user: AccountId,
    pub protocol_if_shares_transfer_config: AccountId,
    pub prelaunch_oracle: AccountId,
    pub openbook_v2_program: AccountId,
    pub openbook_v2_market: AccountId,
    pub openbook_v2_fulfillment_config: AccountId,
    pub lazer_oracle: AccountId,
    pub high_leverage_mode_config: AccountId,
    pub protected_maker_mode_config: AccountId,
    pub admin_token_account: AccountId,
    pub if_rebalance_config: AccountId,
    pub insurance_fund_stake: AccountId,
    pub pyth_lazer_storage: AccountId,
    pub ix_sysvar: AccountId,
    pub constituent: AccountId,
    pub constituent_token_account: AccountId,
    pub amm_constituent_mapping: AccountId,
    pub constituent_target_base: AccountId,
    pub lp_pool: AccountId,
    pub constituent_correlations: AccountId,
    pub constituent_in_token_account: AccountId,
    pub constituent_out_token_account: AccountId,
    pub user_in_token_account: AccountId,
    pub user_out_token_account: AccountId,
    pub in_constituent: AccountId,
    pub out_constituent: AccountId,
    pub in_market_mint: AccountId,
    pub out_market_mint: AccountId,
    pub user_lp_token_account: AccountId,
    pub lp_pool_token_vault: AccountId,
    pub spot_market_token_account: AccountId,
    pub signer: AccountId,
    pub transfer_config: AccountId,
    pub out_insurance_fund_vault: AccountId,
    pub in_insurance_fund_vault: AccountId,
    pub out_token_account: AccountId,
    pub in_token_account: AccountId,
    pub instructions: AccountId,
    pub signed_msg_user_orders: AccountId,
    pub signed_msg_ws_delegates: AccountId,
    pub fuel_overflow: AccountId,
    pub referrer_name: AccountId,
    pub from_user: AccountId,
    pub to_user: AccountId,
    pub deposit_from_spot_market_vault: AccountId,
    pub deposit_to_spot_market_vault: AccountId,
    pub borrow_from_spot_market_vault: AccountId,
    pub borrow_to_spot_market_vault: AccountId,
    pub taker: AccountId,
    pub taker_stats: AccountId,
    pub taker_signed_msg_user_orders: AccountId,
    pub out_spot_market_vault: AccountId,
    pub in_spot_market_vault: AccountId,
    pub revenue_share: AccountId,
    pub escrow: AccountId,
    pub constituent_vault: AccountId,
    pub signer_out_token_account: AccountId,
    pub signer_in_token_account: AccountId,
    pub quote_market: AccountId,
    pub constituent_quote_token_account: AccountId,
    pub quote_token_vault: AccountId,
    pub filler: AccountId,
    pub filler_stats: AccountId,
    pub liquidator: AccountId,
    pub liquidator_stats: AccountId,
    pub liability_spot_market_vault: AccountId,
    pub asset_spot_market_vault: AccountId,
    pub liability_token_account: AccountId,
    pub asset_token_account: AccountId,
    pub keeper_stats: AccountId,
    pub dummy_authority: AccountId,
    pub validator_list: AccountId,
    pub stake_list: AccountId,
    pub stake_account: AccountId,
    pub stake_deposit_authority: AccountId,
    pub reserve_pda: AccountId,
    pub split_stake_account: AccountId,
    pub split_stake_rent_payer: AccountId,
    pub dest_validator_account: AccountId,
    pub redelegate_stake_account: AccountId,
    pub clock: AccountId,
    pub stake_history: AccountId,
    pub stake_config: AccountId,
    pub stake_program: AccountId,
    pub destination_stake: AccountId,
    pub source_stake: AccountId,
    pub stake_withdraw_authority: AccountId,
    pub operational_sol_account: AccountId,
    pub msol_mint: AccountId,
    pub msol_mint_authority: AccountId,
    pub treasury_msol_account: AccountId,
    pub common: AccountId,
    pub epoch_schedule: AccountId,
    pub validator_vote: AccountId,
    pub burn_msol_from: AccountId,
    pub burn_msol_authority: AccountId,
    pub new_ticket_account: AccountId,
    pub ticket_account: AccountId,
    pub transfer_sol_to: AccountId,
    pub burn_from: AccountId,
    pub burn_from_authority: AccountId,
    pub transfer_msol_to: AccountId,
    pub liq_pool_sol_leg_pda: AccountId,
    pub liq_pool_msol_leg: AccountId,
    pub liq_pool_msol_leg_authority: AccountId,
    pub lp_mint_authority: AccountId,
    pub transfer_from: AccountId,
    pub mint_to: AccountId,
    pub get_msol_from: AccountId,
    pub get_msol_from_authority: AccountId,
    pub validator_manager_authority: AccountId,
    pub manager_authority: AccountId,
    pub duplication_flag: AccountId,
    pub pause_authority: AccountId,
    pub admin_authority: AccountId,
    pub rent_funds: AccountId,
    pub liq_pool: AccountId,
    pub sol_leg_pda: AccountId,
    pub msol_leg: AccountId,
    pub stake_authority: AccountId,
    pub arrow: AccountId,
    pub saber_swap: AccountId,
    pub pool_mint: AccountId,
    pub reserve_a: AccountId,
    pub reserve_b: AccountId,
    pub depositor: AccountId,
    pub depositor_source: AccountId,
    pub mint_destination: AccountId,
    pub issue_authority: AccountId,
    pub bank: AccountId,
    pub collateral: AccountId,
    pub crate_token: AccountId,
    pub crate_mint: AccountId,
    pub crate_collateral_tokens: AccountId,
    pub crate_token_program: AccountId,
    pub burner: AccountId,
    pub burned_cash_source: AccountId,
    pub withdraw_destination: AccountId,
    pub author_fee_destination: AccountId,
    pub protocol_fee_destination: AccountId,
    pub withdraw_authority: AccountId,
    pub brrr_issue_authority: AccountId,
    pub burn_withdraw_authority: AccountId,
    pub curator: AccountId,
    pub bankman: AccountId,
    pub next_curator: AccountId,
    pub next_bankman: AccountId,
    pub author_fees: AccountId,
    pub guardian_signatures: AccountId,
    pub refund_recipient: AccountId,
    pub bridge: AccountId,
    pub message: AccountId,
    pub emitter: AccountId,
    pub sequence: AccountId,
    pub fee_collector: AccountId,
    pub wormhole_program: AccountId,
    pub wormhole_verify_vaa_shim: AccountId,
    pub deployer: AccountId,
    pub program_data: AccountId,
    pub wormhole_post_message_shim: AccountId,
    pub wormhole_post_message_shim_ea: AccountId,
    pub posted: AccountId,
    pub signature_set: AccountId,
    pub instruction_acc: AccountId,
    pub vaa: AccountId,
    pub claim: AccountId,
    pub upgrade_authority: AccountId,
    pub spill: AccountId,
    pub buffer: AccountId,
    pub own_address: AccountId,
    pub bpf_loader: AccountId,
    pub system: AccountId,
    pub guardian_set_old: AccountId,
    pub guardian_set_new: AccountId,
    pub bridge_info: AccountId,
    pub bridge_program: AccountId,
    pub wrapped_meta: AccountId,
    pub spl_metadata: AccountId,
    pub chain_registration: AccountId,
    pub to: AccountId,
    pub redeemer: AccountId,
    pub to_fees: AccountId,
    pub custody: AccountId,
    pub custody_signer: AccountId,
    pub meta: AccountId,
    pub from: AccountId,
    pub authority_signer: AccountId,
    pub from_owner: AccountId,
    pub endpoint: AccountId,
    pub sender: AccountId,
    pub to_authority: AccountId,
    pub pool: AccountId,
    pub from_mint: AccountId,
    pub to_mint: AccountId,
    pub to_token_custody: AccountId,
    pub from_token_custody: AccountId,
    pub user_from_acc: AccountId,
    pub user_to_acc: AccountId,
    pub share_mint: AccountId,
    pub to_lp_acc: AccountId,
    pub lp_share_acc: AccountId,
    pub from_lp_acc: AccountId,
    pub swap: AccountId,
    pub swap_authority: AccountId,
    pub token_a: AccountId,
    pub token_b: AccountId,
    pub output_lp: AccountId,
    pub input_a: AccountId,
    pub input_b: AccountId,
    pub input: AccountId,
    pub output: AccountId,
    pub input_lp: AccountId,
    pub quote_reserves: AccountId,
    pub output_a: AccountId,
    pub output_b: AccountId,
    pub admin_ctx: AccountId,
    pub fee_account: AccountId,
    pub new_admin: AccountId,
    pub reserve: AccountId,
    pub fees: AccountId,
    pub user_token: AccountId,
    pub user_authority: AccountId,
    pub token_state: AccountId,
    pub minter: AccountId,
    pub caller: AccountId,
    pub from_account: AccountId,
    pub to_account: AccountId,
    pub target_program: AccountId,
    pub analyst_wallet: AccountId,
    pub analyst_account: AccountId,
    pub new_authority: AccountId,
    pub confirming_analyst_signer: AccountId,
    pub confirming_analyst_account: AccountId,
    pub original_analyst_account: AccountId,
    pub risk_score: AccountId,
    pub confirmation_receipt: AccountId,
    pub analyst_signer: AccountId,
    pub user_stake: AccountId,
    pub staker: AccountId,
    pub voter: AccountId,
    pub user_account: AccountId,
    pub dest_vault: AccountId,
}

#[derive(Default)]
pub struct FuzzTest {
    pub trident: TridentSVM,
    pub fuzz_accounts: FuzzAccounts,
}

impl FuzzTest {
    /// Initialize program state before fuzzing begins.
    #[init]
    fn start(&mut self) {
        // Initialize via 'init_empty_merkle_tree'
        let mut tx = InitEmptyMerkleTreeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("init_empty_merkle_tree"));

        // Initialize via 'init_prepared_tree_with_root'
        let mut tx = InitPreparedTreeWithRootTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("init_prepared_tree_with_root"));

        // Initialize via 'create_amm_config'
        let mut tx = CreateAmmConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_amm_config"));

        // Initialize via 'create_permission_pda'
        let mut tx = CreatePermissionPdaTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_permission_pda"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize_with_permission'
        let mut tx = InitializeWithPermissionTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_with_permission"));

        // Initialize via 'initialize_with_permission'
        let mut tx = InitializeWithPermissionTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_with_permission"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'create_permission_pda'
        let mut tx = CreatePermissionPdaTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_permission_pda"));

        // Initialize via 'create_amm_config'
        let mut tx = CreateAmmConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_amm_config"));

        // Initialize via 'initialize_config'
        let mut tx = InitializeConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_config"));

        // Initialize via 'initialize_pool'
        let mut tx = InitializePoolTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_pool"));

        // Initialize via 'initialize_tick_array'
        let mut tx = InitializeTickArrayTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_tick_array"));

        // Initialize via 'initialize_dynamic_tick_array'
        let mut tx = InitializeDynamicTickArrayTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_dynamic_tick_array"));

        // Initialize via 'initialize_fee_tier'
        let mut tx = InitializeFeeTierTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_fee_tier"));

        // Initialize via 'initialize_reward'
        let mut tx = InitializeRewardTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_reward"));

        // Initialize via 'initialize_position_bundle'
        let mut tx = InitializePositionBundleTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_position_bundle"));

        // Initialize via 'initialize_position_bundle_with_metadata'
        let mut tx = InitializePositionBundleWithMetadataTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_position_bundle_with_metadata"));

        // Initialize via 'initialize_adaptive_fee_tier'
        let mut tx = InitializeAdaptiveFeeTierTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_adaptive_fee_tier"));

        // Initialize via 'set_initialize_pool_authority'
        let mut tx = SetInitializePoolAuthorityTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("set_initialize_pool_authority"));

        // Initialize via 'initialize_pool_with_adaptive_fee'
        let mut tx = InitializePoolWithAdaptiveFeeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_pool_with_adaptive_fee"));

        // Initialize via 'initialize_pool_v2'
        let mut tx = InitializePoolV2Transaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_pool_v2"));

        // Initialize via 'initialize_reward_v2'
        let mut tx = InitializeRewardV2Transaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_reward_v2"));

        // Initialize via 'initialize_config_extension'
        let mut tx = InitializeConfigExtensionTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_config_extension"));

        // Initialize via 'initialize_token_badge'
        let mut tx = InitializeTokenBadgeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_token_badge"));

        // Initialize via 'init_tick_arrays_for_range'
        let mut tx = InitTickArraysForRangeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("init_tick_arrays_for_range"));

        // Initialize via 'program_config_init'
        let mut tx = ProgramConfigInitTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("program_config_init"));

        // Initialize via 'multisig_create'
        let mut tx = MultisigCreateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("multisig_create"));

        // Initialize via 'multisig_create_v2'
        let mut tx = MultisigCreateV2Transaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("multisig_create_v2"));

        // Initialize via 'config_transaction_create'
        let mut tx = ConfigTransactionCreateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("config_transaction_create"));

        // Initialize via 'vault_transaction_create'
        let mut tx = VaultTransactionCreateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("vault_transaction_create"));

        // Initialize via 'transaction_buffer_create'
        let mut tx = TransactionBufferCreateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("transaction_buffer_create"));

        // Initialize via 'vault_transaction_create_from_buffer'
        let mut tx = VaultTransactionCreateFromBufferTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("vault_transaction_create_from_buffer"));

        // Initialize via 'batch_create'
        let mut tx = BatchCreateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("batch_create"));

        // Initialize via 'proposal_create'
        let mut tx = ProposalCreateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("proposal_create"));

        // Initialize via 'create_multisig'
        let mut tx = CreateMultisigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_multisig"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize_user'
        let mut tx = InitializeUserTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_user"));

        // Initialize via 'initialize_user_stats'
        let mut tx = InitializeUserStatsTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_user_stats"));

        // Initialize via 'initialize_signed_msg_user_orders'
        let mut tx = InitializeSignedMsgUserOrdersTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_signed_msg_user_orders"));

        // Initialize via 'initialize_signed_msg_ws_delegates'
        let mut tx = InitializeSignedMsgWsDelegatesTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_signed_msg_ws_delegates"));

        // Initialize via 'initialize_fuel_overflow'
        let mut tx = InitializeFuelOverflowTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_fuel_overflow"));

        // Initialize via 'initialize_referrer_name'
        let mut tx = InitializeReferrerNameTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_referrer_name"));

        // Initialize via 'initialize_insurance_fund_stake'
        let mut tx = InitializeInsuranceFundStakeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_insurance_fund_stake"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize_spot_market'
        let mut tx = InitializeSpotMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_spot_market"));

        // Initialize via 'delete_initialized_spot_market'
        let mut tx = DeleteInitializedSpotMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("delete_initialized_spot_market"));

        // Initialize via 'initialize_serum_fulfillment_config'
        let mut tx = InitializeSerumFulfillmentConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_serum_fulfillment_config"));

        // Initialize via 'initialize_openbook_v2_fulfillment_config'
        let mut tx = InitializeOpenbookV2FulfillmentConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_openbook_v2_fulfillment_config"));

        // Initialize via 'initialize_phoenix_fulfillment_config'
        let mut tx = InitializePhoenixFulfillmentConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_phoenix_fulfillment_config"));

        // Initialize via 'initialize_perp_market'
        let mut tx = InitializePerpMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_perp_market"));

        // Initialize via 'initialize_amm_cache'
        let mut tx = InitializeAmmCacheTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_amm_cache"));

        // Initialize via 'update_initial_amm_cache_info'
        let mut tx = UpdateInitialAmmCacheInfoTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("update_initial_amm_cache_info"));

        // Initialize via 'initialize_prediction_market'
        let mut tx = InitializePredictionMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_prediction_market"));

        // Initialize via 'delete_initialized_perp_market'
        let mut tx = DeleteInitializedPerpMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("delete_initialized_perp_market"));

        // Initialize via 'update_spot_market_scale_initial_asset_weight_start'
        let mut tx = UpdateSpotMarketScaleInitialAssetWeightStartTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_scale_initial_asset_weight_start"));

        // Initialize via 'update_initial_pct_to_liquidate'
        let mut tx = UpdateInitialPctToLiquidateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("update_initial_pct_to_liquidate"));

        // Initialize via 'update_state_max_initialize_user_fee'
        let mut tx = UpdateStateMaxInitializeUserFeeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("update_state_max_initialize_user_fee"));

        // Initialize via 'initialize_prelaunch_oracle'
        let mut tx = InitializePrelaunchOracleTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_prelaunch_oracle"));

        // Initialize via 'initialize_pyth_pull_oracle'
        let mut tx = InitializePythPullOracleTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_pyth_pull_oracle"));

        // Initialize via 'initialize_pyth_lazer_oracle'
        let mut tx = InitializePythLazerOracleTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_pyth_lazer_oracle"));

        // Initialize via 'initialize_high_leverage_mode_config'
        let mut tx = InitializeHighLeverageModeConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_high_leverage_mode_config"));

        // Initialize via 'initialize_protected_maker_mode_config'
        let mut tx = InitializeProtectedMakerModeConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_protected_maker_mode_config"));

        // Initialize via 'initialize_if_rebalance_config'
        let mut tx = InitializeIfRebalanceConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_if_rebalance_config"));

        // Initialize via 'initialize_revenue_share'
        let mut tx = InitializeRevenueShareTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_revenue_share"));

        // Initialize via 'initialize_revenue_share_escrow'
        let mut tx = InitializeRevenueShareEscrowTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_revenue_share_escrow"));

        // Initialize via 'initialize_lp_pool'
        let mut tx = InitializeLpPoolTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_lp_pool"));

        // Initialize via 'initialize_constituent'
        let mut tx = InitializeConstituentTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize_constituent"));

        // Initialize via 'handle_initialize'
        let mut tx = HandleInitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize"));

        // Initialize via 'handle_initialize_spot_market'
        let mut tx = HandleInitializeSpotMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_spot_market"));

        // Initialize via 'handle_initialize_serum_fulfillment_config'
        let mut tx = HandleInitializeSerumFulfillmentConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_serum_fulfillment_config"));

        // Initialize via 'handle_initialize_openbook_v2_fulfillment_config'
        let mut tx = HandleInitializeOpenbookV2FulfillmentConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_openbook_v2_fulfillment_config"));

        // Initialize via 'handle_initialize_phoenix_fulfillment_config'
        let mut tx = HandleInitializePhoenixFulfillmentConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_phoenix_fulfillment_config"));

        // Initialize via 'handle_initialize_perp_market'
        let mut tx = HandleInitializePerpMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_perp_market"));

        // Initialize via 'handle_initialize_amm_cache'
        let mut tx = HandleInitializeAmmCacheTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_amm_cache"));

        // Initialize via 'handle_initialize_prediction_market'
        let mut tx = HandleInitializePredictionMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_prediction_market"));

        // Initialize via 'handle_delete_initialized_perp_market'
        let mut tx = HandleDeleteInitializedPerpMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_delete_initialized_perp_market"));

        // Initialize via 'handle_delete_initialized_spot_market'
        let mut tx = HandleDeleteInitializedSpotMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_delete_initialized_spot_market"));

        // Initialize via 'handle_init_user_fuel'
        let mut tx = HandleInitUserFuelTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_init_user_fuel"));

        // Initialize via 'handle_update_spot_market_scale_initial_asset_weight_start'
        let mut tx = HandleUpdateSpotMarketScaleInitialAssetWeightStartTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_scale_initial_asset_weight_start"));

        // Initialize via 'handle_update_initial_pct_to_liquidate'
        let mut tx = HandleUpdateInitialPctToLiquidateTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_update_initial_pct_to_liquidate"));

        // Initialize via 'handle_update_state_max_initialize_user_fee'
        let mut tx = HandleUpdateStateMaxInitializeUserFeeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_update_state_max_initialize_user_fee"));

        // Initialize via 'handle_initialize_protocol_if_shares_transfer_config'
        let mut tx = HandleInitializeProtocolIfSharesTransferConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_protocol_if_shares_transfer_config"));

        // Initialize via 'handle_initialize_prelaunch_oracle'
        let mut tx = HandleInitializePrelaunchOracleTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_prelaunch_oracle"));

        // Initialize via 'handle_initialize_pyth_pull_oracle'
        let mut tx = HandleInitializePythPullOracleTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_pyth_pull_oracle"));

        // Initialize via 'handle_initialize_pyth_lazer_oracle'
        let mut tx = HandleInitializePythLazerOracleTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_pyth_lazer_oracle"));

        // Initialize via 'handle_initialize_high_leverage_mode_config'
        let mut tx = HandleInitializeHighLeverageModeConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_high_leverage_mode_config"));

        // Initialize via 'handle_initialize_protected_maker_mode_config'
        let mut tx = HandleInitializeProtectedMakerModeConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_protected_maker_mode_config"));

        // Initialize via 'handle_initialize_if_rebalance_config'
        let mut tx = HandleInitializeIfRebalanceConfigTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_if_rebalance_config"));

        // Initialize via 'handle_initialize_insurance_fund_stake'
        let mut tx = HandleInitializeInsuranceFundStakeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_insurance_fund_stake"));

        // Initialize via 'handle_initialize_user'
        let mut tx = HandleInitializeUserTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_user"));

        // Initialize via 'handle_initialize_user_stats'
        let mut tx = HandleInitializeUserStatsTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_user_stats"));

        // Initialize via 'handle_initialize_referrer_name'
        let mut tx = HandleInitializeReferrerNameTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_referrer_name"));

        // Initialize via 'handle_initialize_signed_msg_user_orders'
        let mut tx = HandleInitializeSignedMsgUserOrdersTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_signed_msg_user_orders"));

        // Initialize via 'handle_initialize_signed_msg_ws_delegates'
        let mut tx = HandleInitializeSignedMsgWsDelegatesTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_signed_msg_ws_delegates"));

        // Initialize via 'handle_initialize_fuel_overflow'
        let mut tx = HandleInitializeFuelOverflowTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_fuel_overflow"));

        // Initialize via 'handle_initialize_revenue_share'
        let mut tx = HandleInitializeRevenueShareTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_revenue_share"));

        // Initialize via 'handle_initialize_revenue_share_escrow'
        let mut tx = HandleInitializeRevenueShareEscrowTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_revenue_share_escrow"));

        // Initialize via 'handle_initialize_lp_pool'
        let mut tx = HandleInitializeLpPoolTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_lp_pool"));

        // Initialize via 'handle_initialize_constituent'
        let mut tx = HandleInitializeConstituentTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_initialize_constituent"));

        // Initialize via 'handle_update_initial_amm_cache_info'
        let mut tx = HandleUpdateInitialAmmCacheInfoTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("handle_update_initial_amm_cache_info"));

        // Initialize via 'create_market'
        let mut tx = CreateMarketTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_market"));

        // Initialize via 'create_open_orders_indexer'
        let mut tx = CreateOpenOrdersIndexerTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_open_orders_indexer"));

        // Initialize via 'create_open_orders_account'
        let mut tx = CreateOpenOrdersAccountTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_open_orders_account"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'create_account'
        let mut tx = CreateAccountTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_account"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'create_wrapped'
        let mut tx = CreateWrappedTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_wrapped"));

        // Initialize via 'create_wrapped_account'
        let mut tx = CreateWrappedAccountTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_wrapped_account"));

        // Initialize via 'register_chain'
        let mut tx = RegisterChainTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("register_chain"));

        // Initialize via 'create_wrapped'
        let mut tx = CreateWrappedTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_wrapped"));

        // Initialize via 'create_accounts'
        let mut tx = CreateAccountsTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_accounts"));

        // Initialize via 'register_chain'
        let mut tx = RegisterChainTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("register_chain"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'register_chain'
        let mut tx = RegisterChainTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("register_chain"));

        // Initialize via 'register_chain'
        let mut tx = RegisterChainTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("register_chain"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'create_pool'
        let mut tx = CreatePoolTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_pool"));

        // Initialize via 'init_empty_merkle_tree'
        let mut tx = InitEmptyMerkleTreeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("init_empty_merkle_tree"));

        // Initialize via 'init_prepared_tree_with_root'
        let mut tx = InitPreparedTreeWithRootTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("init_prepared_tree_with_root"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'register_analyst'
        let mut tx = RegisterAnalystTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("register_analyst"));

        // Initialize via 'register_analyst'
        let mut tx = RegisterAnalystTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("register_analyst"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'initialize'
        let mut tx = InitializeTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("initialize"));

        // Initialize via 'create_user_account'
        let mut tx = CreateUserAccountTransaction::build(&mut self.trident, &mut self.fuzz_accounts);
        self.trident.execute_transaction(&mut tx, Some("create_user_account"));

    }

    /// Fuzz flow for instruction 'prepare_batch_merkle_tree'
    #[flow]
    fn flow_prepare_batch_merkle_tree(&mut self) {
        let mut tx = PrepareBatchMerkleTreeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_depth(rand::random::<u32>());
        tx.set_max_buffer_size(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("prepare_batch_merkle_tree"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'append_canopy_nodes'
    #[flow]
    fn flow_append_canopy_nodes(&mut self) {
        let mut tx = AppendCanopyNodesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_start_index(rand::random::<u32>());
        tx.set_canopy_nodes(Default::default() /* Vec < [u8 ; 32] > */);
        self.trident.execute_transaction(&mut tx, Some("append_canopy_nodes"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'replace_leaf'
    #[flow]
    fn flow_replace_leaf(&mut self) {
        let mut tx = ReplaceLeafTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_root(Default::default() /* [u8 ; 32] */);
        tx.set_previous_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_new_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("replace_leaf"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_authority'
    #[flow]
    fn flow_transfer_authority(&mut self) {
        let mut tx = TransferAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_authority(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("transfer_authority"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_leaf'
    #[flow]
    fn flow_verify_leaf(&mut self) {
        let mut tx = VerifyLeafTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_root(Default::default() /* [u8 ; 32] */);
        tx.set_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("verify_leaf"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'append'
    #[flow]
    fn flow_append(&mut self) {
        let mut tx = AppendTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_leaf(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("append"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'insert_or_append'
    #[flow]
    fn flow_insert_or_append(&mut self) {
        let mut tx = InsertOrAppendTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_root(Default::default() /* [u8 ; 32] */);
        tx.set_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("insert_or_append"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_empty_tree'
    #[flow]
    fn flow_close_empty_tree(&mut self) {
        let mut tx = CloseEmptyTreeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("close_empty_tree"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'sign_send_instruction'
    #[flow]
    fn flow_sign_send_instruction(&mut self) {
        let mut tx = SignSendInstructionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_instruction(Default::default() /* Instruction */);
        tx.set_signers(Default::default() /* Vec < & Keypair > */);
        self.trident.execute_transaction(&mut tx, Some("sign_send_instruction"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_group'
    #[flow]
    fn flow_setup_group(&mut self) {
        let mut tx = SetupGroupTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_program_id(Pubkey::new_unique());
        tx.set_group(Default::default() /* & Keypair */);
        tx.set_mint(Default::default() /* & Keypair */);
        tx.set_mint_authority(Default::default() /* & Keypair */);
        tx.set_update_authority(Pubkey::new_unique());
        tx.set_max_size(rand::random::<u64>());
        tx.set_rent_lamports(rand::random::<u64>());
        tx.set_space(Default::default() /* usize */);
        self.trident.execute_transaction(&mut tx, Some("setup_group"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);
    }

    /// Fuzz flow for instruction 'make_decision'
    #[flow]
    fn flow_make_decision(&mut self) {
        let mut tx = MakeDecisionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_account(Pubkey::new_unique());
        tx.set_decider(Default::default() /* & Keypair */);
        tx.set_decision(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("make_decision"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'make_withdraw'
    #[flow]
    fn flow_make_withdraw(&mut self) {
        let mut tx = MakeWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_account(Pubkey::new_unique());
        tx.set_authority(Pubkey::new_unique());
        tx.set_user_transfer_authority(Pubkey::new_unique());
        tx.set_pool_deposit_account(Pubkey::new_unique());
        tx.set_user_pass_account(Pubkey::new_unique());
        tx.set_user_fail_account(Pubkey::new_unique());
        tx.set_token_pass_mint(Pubkey::new_unique());
        tx.set_token_fail_mint(Pubkey::new_unique());
        tx.set_user_account(Pubkey::new_unique());
        tx.set_withdraw_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("make_withdraw"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_amm_config'
    #[flow]
    fn flow_update_amm_config(&mut self) {
        let mut tx = UpdateAmmConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_param(rand::random::<u8>());
        tx.set_value(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_amm_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_pool_status'
    #[flow]
    fn flow_update_pool_status(&mut self) {
        let mut tx = UpdatePoolStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_pool_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_protocol_fee'
    #[flow]
    fn flow_collect_protocol_fee(&mut self) {
        let mut tx = CollectProtocolFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount_0_requested(rand::random::<u64>());
        tx.set_amount_1_requested(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("collect_protocol_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_fund_fee'
    #[flow]
    fn flow_collect_fund_fee(&mut self) {
        let mut tx = CollectFundFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount_0_requested(rand::random::<u64>());
        tx.set_amount_1_requested(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("collect_fund_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_creator_fee'
    #[flow]
    fn flow_collect_creator_fee(&mut self) {
        let mut tx = CollectCreatorFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("collect_creator_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_permission_pda'
    #[flow]
    fn flow_close_permission_pda(&mut self) {
        let mut tx = ClosePermissionPdaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("close_permission_pda"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit'
    #[flow]
    fn flow_deposit(&mut self) {
        let mut tx = DepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_token_amount(rand::random::<u64>());
        tx.set_maximum_token_0_amount(rand::random::<u64>());
        tx.set_maximum_token_1_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw'
    #[flow]
    fn flow_withdraw(&mut self) {
        let mut tx = WithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_token_amount(rand::random::<u64>());
        tx.set_minimum_token_0_amount(rand::random::<u64>());
        tx.set_minimum_token_1_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("withdraw"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'swap_base_input'
    #[flow]
    fn flow_swap_base_input(&mut self) {
        let mut tx = SwapBaseInputTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount_in(rand::random::<u64>());
        tx.set_minimum_amount_out(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("swap_base_input"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'swap_base_output'
    #[flow]
    fn flow_swap_base_output(&mut self) {
        let mut tx = SwapBaseOutputTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_amount_in(rand::random::<u64>());
        tx.set_amount_out(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("swap_base_output"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'swap_base_output'
    #[flow]
    fn flow_swap_base_output(&mut self) {
        let mut tx = SwapBaseOutputTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_amount_in(rand::random::<u64>());
        tx.set_amount_out_received(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("swap_base_output"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_creator_fee'
    #[flow]
    fn flow_collect_creator_fee(&mut self) {
        let mut tx = CollectCreatorFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("collect_creator_fee"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit'
    #[flow]
    fn flow_deposit(&mut self) {
        let mut tx = DepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_token_amount(rand::random::<u64>());
        tx.set_maximum_token_0_amount(rand::random::<u64>());
        tx.set_maximum_token_1_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw'
    #[flow]
    fn flow_withdraw(&mut self) {
        let mut tx = WithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_token_amount(rand::random::<u64>());
        tx.set_minimum_token_0_amount(rand::random::<u64>());
        tx.set_minimum_token_1_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("withdraw"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_pool_status'
    #[flow]
    fn flow_update_pool_status(&mut self) {
        let mut tx = UpdatePoolStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_pool_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_protocol_fee'
    #[flow]
    fn flow_collect_protocol_fee(&mut self) {
        let mut tx = CollectProtocolFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount_0_requested(rand::random::<u64>());
        tx.set_amount_1_requested(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("collect_protocol_fee"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_permission_pda'
    #[flow]
    fn flow_close_permission_pda(&mut self) {
        let mut tx = ClosePermissionPdaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("close_permission_pda"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_fund_fee'
    #[flow]
    fn flow_collect_fund_fee(&mut self) {
        let mut tx = CollectFundFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount_0_requested(rand::random::<u64>());
        tx.set_amount_1_requested(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("collect_fund_fee"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_amm_config'
    #[flow]
    fn flow_update_amm_config(&mut self) {
        let mut tx = UpdateAmmConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_param(rand::random::<u8>());
        tx.set_value(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_amm_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'swap_base_input'
    #[flow]
    fn flow_swap_base_input(&mut self) {
        let mut tx = SwapBaseInputTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount_in(rand::random::<u64>());
        tx.set_minimum_amount_out(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("swap_base_input"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_reward_emissions'
    #[flow]
    fn flow_set_reward_emissions(&mut self) {
        let mut tx = SetRewardEmissionsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        tx.set_emissions_per_second_x64(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("set_reward_emissions"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'open_position'
    #[flow]
    fn flow_open_position(&mut self) {
        let mut tx = OpenPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_bumps(Default::default() /* OpenPositionBumps */);
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("open_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'open_position_with_metadata'
    #[flow]
    fn flow_open_position_with_metadata(&mut self) {
        let mut tx = OpenPositionWithMetadataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_bumps(Default::default() /* OpenPositionWithMetadataBumps */);
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("open_position_with_metadata"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'increase_liquidity'
    #[flow]
    fn flow_increase_liquidity(&mut self) {
        let mut tx = IncreaseLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_max_a(rand::random::<u64>());
        tx.set_token_max_b(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("increase_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'decrease_liquidity'
    #[flow]
    fn flow_decrease_liquidity(&mut self) {
        let mut tx = DecreaseLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_min_a(rand::random::<u64>());
        tx.set_token_min_b(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("decrease_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_fees_and_rewards'
    #[flow]
    fn flow_update_fees_and_rewards(&mut self) {
        let mut tx = UpdateFeesAndRewardsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_fees_and_rewards"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_fees'
    #[flow]
    fn flow_collect_fees(&mut self) {
        let mut tx = CollectFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("collect_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_reward'
    #[flow]
    fn flow_collect_reward(&mut self) {
        let mut tx = CollectRewardTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("collect_reward"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_protocol_fees'
    #[flow]
    fn flow_collect_protocol_fees(&mut self) {
        let mut tx = CollectProtocolFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("collect_protocol_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'swap'
    #[flow]
    fn flow_swap(&mut self) {
        let mut tx = SwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_sqrt_price_limit(rand::random::<u128>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_position'
    #[flow]
    fn flow_close_position(&mut self) {
        let mut tx = ClosePositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("close_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_default_fee_rate'
    #[flow]
    fn flow_set_default_fee_rate(&mut self) {
        let mut tx = SetDefaultFeeRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("set_default_fee_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_default_protocol_fee_rate'
    #[flow]
    fn flow_set_default_protocol_fee_rate(&mut self) {
        let mut tx = SetDefaultProtocolFeeRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_protocol_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("set_default_protocol_fee_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_fee_rate'
    #[flow]
    fn flow_set_fee_rate(&mut self) {
        let mut tx = SetFeeRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("set_fee_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_protocol_fee_rate'
    #[flow]
    fn flow_set_protocol_fee_rate(&mut self) {
        let mut tx = SetProtocolFeeRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_protocol_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("set_protocol_fee_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_fee_authority'
    #[flow]
    fn flow_set_fee_authority(&mut self) {
        let mut tx = SetFeeAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_fee_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_collect_protocol_fees_authority'
    #[flow]
    fn flow_set_collect_protocol_fees_authority(&mut self) {
        let mut tx = SetCollectProtocolFeesAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_collect_protocol_fees_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_reward_authority'
    #[flow]
    fn flow_set_reward_authority(&mut self) {
        let mut tx = SetRewardAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("set_reward_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_reward_authority_by_super_authority'
    #[flow]
    fn flow_set_reward_authority_by_super_authority(&mut self) {
        let mut tx = SetRewardAuthorityBySuperAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("set_reward_authority_by_super_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_reward_emissions_super_authority'
    #[flow]
    fn flow_set_reward_emissions_super_authority(&mut self) {
        let mut tx = SetRewardEmissionsSuperAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_reward_emissions_super_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'two_hop_swap'
    #[flow]
    fn flow_two_hop_swap(&mut self) {
        let mut tx = TwoHopSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b_one(rand::random::<bool>());
        tx.set_a_to_b_two(rand::random::<bool>());
        tx.set_sqrt_price_limit_one(rand::random::<u128>());
        tx.set_sqrt_price_limit_two(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("two_hop_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_position_bundle'
    #[flow]
    fn flow_delete_position_bundle(&mut self) {
        let mut tx = DeletePositionBundleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("delete_position_bundle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'open_bundled_position'
    #[flow]
    fn flow_open_bundled_position(&mut self) {
        let mut tx = OpenBundledPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_bundle_index(rand::random::<u16>());
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("open_bundled_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_bundled_position'
    #[flow]
    fn flow_close_bundled_position(&mut self) {
        let mut tx = CloseBundledPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_bundle_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("close_bundled_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'open_position_with_token_extensions'
    #[flow]
    fn flow_open_position_with_token_extensions(&mut self) {
        let mut tx = OpenPositionWithTokenExtensionsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        tx.set_with_token_metadata_extension(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("open_position_with_token_extensions"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_position_with_token_extensions'
    #[flow]
    fn flow_close_position_with_token_extensions(&mut self) {
        let mut tx = ClosePositionWithTokenExtensionsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("close_position_with_token_extensions"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'lock_position'
    #[flow]
    fn flow_lock_position(&mut self) {
        let mut tx = LockPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lock_type(Default::default() /* LockType */);
        self.trident.execute_transaction(&mut tx, Some("lock_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'reset_position_range'
    #[flow]
    fn flow_reset_position_range(&mut self) {
        let mut tx = ResetPositionRangeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_tick_lower_index(rand::random::<i32>());
        tx.set_new_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("reset_position_range"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_locked_position'
    #[flow]
    fn flow_transfer_locked_position(&mut self) {
        let mut tx = TransferLockedPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("transfer_locked_position"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_default_base_fee_rate'
    #[flow]
    fn flow_set_default_base_fee_rate(&mut self) {
        let mut tx = SetDefaultBaseFeeRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_base_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("set_default_base_fee_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_delegated_fee_authority'
    #[flow]
    fn flow_set_delegated_fee_authority(&mut self) {
        let mut tx = SetDelegatedFeeAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_delegated_fee_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_preset_adaptive_fee_constants'
    #[flow]
    fn flow_set_preset_adaptive_fee_constants(&mut self) {
        let mut tx = SetPresetAdaptiveFeeConstantsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_filter_period(rand::random::<u16>());
        tx.set_decay_period(rand::random::<u16>());
        tx.set_reduction_factor(rand::random::<u16>());
        tx.set_adaptive_fee_control_factor(rand::random::<u32>());
        tx.set_max_volatility_accumulator(rand::random::<u32>());
        tx.set_tick_group_size(rand::random::<u16>());
        tx.set_major_swap_threshold_ticks(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("set_preset_adaptive_fee_constants"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_fee_rate_by_delegated_fee_authority'
    #[flow]
    fn flow_set_fee_rate_by_delegated_fee_authority(&mut self) {
        let mut tx = SetFeeRateByDelegatedFeeAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("set_fee_rate_by_delegated_fee_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_adaptive_fee_constants'
    #[flow]
    fn flow_set_adaptive_fee_constants(&mut self) {
        let mut tx = SetAdaptiveFeeConstantsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_filter_period(Default::default() /* Option < u16 > */);
        tx.set_decay_period(Default::default() /* Option < u16 > */);
        tx.set_reduction_factor(Default::default() /* Option < u16 > */);
        tx.set_adaptive_fee_control_factor(Default::default() /* Option < u32 > */);
        tx.set_max_volatility_accumulator(Default::default() /* Option < u32 > */);
        tx.set_tick_group_size(Default::default() /* Option < u16 > */);
        tx.set_major_swap_threshold_ticks(Default::default() /* Option < u16 > */);
        self.trident.execute_transaction(&mut tx, Some("set_adaptive_fee_constants"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_config_feature_flag'
    #[flow]
    fn flow_set_config_feature_flag(&mut self) {
        let mut tx = SetConfigFeatureFlagTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_feature_flag(Default::default() /* ConfigFeatureFlag */);
        self.trident.execute_transaction(&mut tx, Some("set_config_feature_flag"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'migrate_repurpose_reward_authority_space'
    #[flow]
    fn flow_migrate_repurpose_reward_authority_space(&mut self) {
        let mut tx = MigrateRepurposeRewardAuthoritySpaceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("migrate_repurpose_reward_authority_space"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_fees_v2'
    #[flow]
    fn flow_collect_fees_v2(&mut self) {
        let mut tx = CollectFeesV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("collect_fees_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_protocol_fees_v2'
    #[flow]
    fn flow_collect_protocol_fees_v2(&mut self) {
        let mut tx = CollectProtocolFeesV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("collect_protocol_fees_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_reward_v2'
    #[flow]
    fn flow_collect_reward_v2(&mut self) {
        let mut tx = CollectRewardV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("collect_reward_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'decrease_liquidity_v2'
    #[flow]
    fn flow_decrease_liquidity_v2(&mut self) {
        let mut tx = DecreaseLiquidityV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_min_a(rand::random::<u64>());
        tx.set_token_min_b(rand::random::<u64>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("decrease_liquidity_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'increase_liquidity_v2'
    #[flow]
    fn flow_increase_liquidity_v2(&mut self) {
        let mut tx = IncreaseLiquidityV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_max_a(rand::random::<u64>());
        tx.set_token_max_b(rand::random::<u64>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("increase_liquidity_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'increase_liquidity_by_token_amounts_v2'
    #[flow]
    fn flow_increase_liquidity_by_token_amounts_v2(&mut self) {
        let mut tx = IncreaseLiquidityByTokenAmountsV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_method(Default::default() /* IncreaseLiquidityMethod */);
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("increase_liquidity_by_token_amounts_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_reward_emissions_v2'
    #[flow]
    fn flow_set_reward_emissions_v2(&mut self) {
        let mut tx = SetRewardEmissionsV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        tx.set_emissions_per_second_x64(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("set_reward_emissions_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'swap_v2'
    #[flow]
    fn flow_swap_v2(&mut self) {
        let mut tx = SwapV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_sqrt_price_limit(rand::random::<u128>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b(rand::random::<bool>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("swap_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'two_hop_swap_v2'
    #[flow]
    fn flow_two_hop_swap_v2(&mut self) {
        let mut tx = TwoHopSwapV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b_one(rand::random::<bool>());
        tx.set_a_to_b_two(rand::random::<bool>());
        tx.set_sqrt_price_limit_one(rand::random::<u128>());
        tx.set_sqrt_price_limit_two(rand::random::<u128>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("two_hop_swap_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'reposition_liquidity_v2'
    #[flow]
    fn flow_reposition_liquidity_v2(&mut self) {
        let mut tx = RepositionLiquidityV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_tick_lower_index(rand::random::<i32>());
        tx.set_new_tick_upper_index(rand::random::<i32>());
        tx.set_method(Default::default() /* RepositionLiquidityMethod */);
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("reposition_liquidity_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_config_extension_authority'
    #[flow]
    fn flow_set_config_extension_authority(&mut self) {
        let mut tx = SetConfigExtensionAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_config_extension_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_token_badge_authority'
    #[flow]
    fn flow_set_token_badge_authority(&mut self) {
        let mut tx = SetTokenBadgeAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_token_badge_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_token_badge'
    #[flow]
    fn flow_delete_token_badge(&mut self) {
        let mut tx = DeleteTokenBadgeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("delete_token_badge"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_token_badge_attribute'
    #[flow]
    fn flow_set_token_badge_attribute(&mut self) {
        let mut tx = SetTokenBadgeAttributeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_attribute(Default::default() /* TokenBadgeAttribute */);
        self.trident.execute_transaction(&mut tx, Some("set_token_badge_attribute"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'idl_include'
    #[flow]
    fn flow_idl_include(&mut self) {
        let mut tx = IdlIncludeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("idl_include"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_feature_flag(Default::default() /* ConfigFeatureFlag */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_min_a(rand::random::<u64>());
        tx.set_token_min_b(rand::random::<u64>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b_one(rand::random::<bool>());
        tx.set_a_to_b_two(rand::random::<bool>());
        tx.set_sqrt_price_limit_one(rand::random::<u128>());
        tx.set_sqrt_price_limit_two(rand::random::<u128>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_tick_spacing(rand::random::<u16>());
        tx.set_initial_sqrt_price(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        tx.set_emissions_per_second_x64(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_sqrt_price_limit(rand::random::<u128>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b(rand::random::<bool>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_max_a(rand::random::<u64>());
        tx.set_token_max_b(rand::random::<u64>());
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_remaining_accounts_info(Default::default() /* Option < RemainingAccountsInfo > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_attribute(Default::default() /* TokenBadgeAttribute */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_min_a(rand::random::<u64>());
        tx.set_token_min_b(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b_one(rand::random::<bool>());
        tx.set_a_to_b_two(rand::random::<bool>());
        tx.set_sqrt_price_limit_one(rand::random::<u128>());
        tx.set_sqrt_price_limit_two(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_tick_lower_index(rand::random::<i32>());
        tx.set_new_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_protocol_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__bumps(Default::default() /* WhirlpoolBumps */);
        tx.set_tick_spacing(rand::random::<u16>());
        tx.set_initial_sqrt_price(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        tx.set_emissions_per_second_x64(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_start_tick_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_protocol_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_start_tick_index(rand::random::<i32>());
        tx.set_idempotent(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__bumps(Default::default() /* state :: OpenPositionWithMetadataBumps */);
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_tier_index(rand::random::<u16>());
        tx.set_tick_spacing(rand::random::<u16>());
        tx.set_initialize_pool_authority(Pubkey::new_unique());
        tx.set_delegated_fee_authority(Pubkey::new_unique());
        tx.set_default_base_fee_rate(rand::random::<u16>());
        tx.set_filter_period(rand::random::<u16>());
        tx.set_decay_period(rand::random::<u16>());
        tx.set_reduction_factor(rand::random::<u16>());
        tx.set_adaptive_fee_control_factor(rand::random::<u32>());
        tx.set_max_volatility_accumulator(rand::random::<u32>());
        tx.set_tick_group_size(rand::random::<u16>());
        tx.set_major_swap_threshold_ticks(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_filter_period(rand::random::<u16>());
        tx.set_decay_period(rand::random::<u16>());
        tx.set_reduction_factor(rand::random::<u16>());
        tx.set_adaptive_fee_control_factor(rand::random::<u32>());
        tx.set_max_volatility_accumulator(rand::random::<u32>());
        tx.set_tick_group_size(rand::random::<u16>());
        tx.set_major_swap_threshold_ticks(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_base_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_initial_sqrt_price(rand::random::<u128>());
        tx.set_trade_enable_timestamp(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_filter_period(Default::default() /* Option < u16 > */);
        tx.set_decay_period(Default::default() /* Option < u16 > */);
        tx.set_reduction_factor(Default::default() /* Option < u16 > */);
        tx.set_adaptive_fee_control_factor(Default::default() /* Option < u32 > */);
        tx.set_max_volatility_accumulator(Default::default() /* Option < u32 > */);
        tx.set_tick_group_size(Default::default() /* Option < u16 > */);
        tx.set_major_swap_threshold_ticks(Default::default() /* Option < u16 > */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_bundle_index(rand::random::<u16>());
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lock_type(Default::default() /* LockType */);
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_other_amount_threshold(rand::random::<u64>());
        tx.set_sqrt_price_limit(rand::random::<u128>());
        tx.set_amount_specified_is_input(rand::random::<bool>());
        tx.set_a_to_b(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__bumps(Default::default() /* state :: OpenPositionBumps */);
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_tick_lower_index(rand::random::<i32>());
        tx.set_tick_upper_index(rand::random::<i32>());
        tx.set_with_token_metadata(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_authority(Pubkey::new_unique());
        tx.set_collect_protocol_fees_authority(Pubkey::new_unique());
        tx.set_reward_emissions_super_authority(Pubkey::new_unique());
        tx.set_default_protocol_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidity_amount(rand::random::<u128>());
        tx.set_token_max_a(rand::random::<u64>());
        tx.set_token_max_b(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_tick_spacing(rand::random::<u16>());
        tx.set_default_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_fee_rate(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reward_index(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_bundle_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_decrease_liquidity'
    #[flow]
    fn flow_verify_decrease_liquidity(&mut self) {
        let mut tx = VerifyDecreaseLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_decrease_ix(Default::default() /* & crate :: DecreaseLiquidityInstruction */);
        tx.set_token_a_account(Pubkey::new_unique());
        tx.set_token_b_account(Pubkey::new_unique());
        tx.set_position_mint(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("verify_decrease_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_mints'
    #[flow]
    fn flow_setup_all_mints(&mut self) {
        let mut tx = SetupAllMintsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("setup_all_mints"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_atas'
    #[flow]
    fn flow_setup_all_atas(&mut self) {
        let mut tx = SetupAllAtasTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_minted(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("setup_all_atas"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_whirlpool'
    #[flow]
    fn flow_setup_whirlpool(&mut self) {
        let mut tx = SetupWhirlpoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_token_a(Pubkey::new_unique());
        tx.set_token_b(Pubkey::new_unique());
        tx.set_tick_spacing(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("setup_whirlpool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_position'
    #[flow]
    fn flow_setup_position(&mut self) {
        let mut tx = SetupPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_whirlpool(Pubkey::new_unique());
        tx.set_tick_range(Default::default() /* Option < (i32 , i32) > */);
        tx.set_owner(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("setup_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_te_position'
    #[flow]
    fn flow_setup_te_position(&mut self) {
        let mut tx = SetupTePositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_whirlpool(Pubkey::new_unique());
        tx.set_tick_range(Default::default() /* Option < (i32 , i32) > */);
        tx.set_owner(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("setup_te_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_position_bundle'
    #[flow]
    fn flow_setup_position_bundle(&mut self) {
        let mut tx = SetupPositionBundleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_whirlpool(Pubkey::new_unique());
        tx.set_bundle_positions(Default::default() /* Option < Vec < () > > */);
        self.trident.execute_transaction(&mut tx, Some("setup_position_bundle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_mint_te'
    #[flow]
    fn flow_setup_mint_te(&mut self) {
        let mut tx = SetupMintTeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_extensions(Default::default() /* & [ExtensionType] */);
        self.trident.execute_transaction(&mut tx, Some("setup_mint_te"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_mint_te_fee'
    #[flow]
    fn flow_setup_mint_te_fee(&mut self) {
        let mut tx = SetupMintTeFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("setup_mint_te_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_mint_te_sua'
    #[flow]
    fn flow_setup_mint_te_sua(&mut self) {
        let mut tx = SetupMintTeSuaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("setup_mint_te_sua"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_ata_te'
    #[flow]
    fn flow_setup_ata_te(&mut self) {
        let mut tx = SetupAtaTeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_mint(Pubkey::new_unique());
        tx.set_config(Default::default() /* Option < SetupAtaConfig > */);
        self.trident.execute_transaction(&mut tx, Some("setup_ata_te"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_ata'
    #[flow]
    fn flow_setup_ata(&mut self) {
        let mut tx = SetupAtaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_mint(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("setup_ata"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_ata_with_amount'
    #[flow]
    fn flow_setup_ata_with_amount(&mut self) {
        let mut tx = SetupAtaWithAmountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_mint(Pubkey::new_unique());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("setup_ata_with_amount"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_mint'
    #[flow]
    fn flow_setup_mint(&mut self) {
        let mut tx = SetupMintTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("setup_mint"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_mint_with_decimals'
    #[flow]
    fn flow_setup_mint_with_decimals(&mut self) {
        let mut tx = SetupMintWithDecimalsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_decimals(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("setup_mint_with_decimals"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_mints'
    #[flow]
    fn flow_setup_all_mints(&mut self) {
        let mut tx = SetupAllMintsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("setup_all_mints"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_atas'
    #[flow]
    fn flow_setup_all_atas(&mut self) {
        let mut tx = SetupAllAtasTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_minted(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("setup_all_atas"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_swap'
    #[flow]
    fn flow_verify_swap(&mut self) {
        let mut tx = VerifySwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_swap_ix(Default::default() /* & SwapInstructions */);
        tx.set_user_ata_for_final_a(Pubkey::new_unique());
        tx.set_user_ata_for_final_b(Pubkey::new_unique());
        tx.set_a_to_b(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("verify_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_increase_liquidity'
    #[flow]
    fn flow_verify_increase_liquidity(&mut self) {
        let mut tx = VerifyIncreaseLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_increase_ix(Default::default() /* & IncreaseLiquidityInstruction */);
        tx.set_token_a_account(Pubkey::new_unique());
        tx.set_token_b_account(Pubkey::new_unique());
        tx.set_position_mint(Pubkey::new_unique());
        tx.set_param(Default::default() /* IncreaseLiquidityParam */);
        tx.set_pool_name(Default::default() /* & str */);
        self.trident.execute_transaction(&mut tx, Some("verify_increase_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_mints'
    #[flow]
    fn flow_setup_all_mints(&mut self) {
        let mut tx = SetupAllMintsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("setup_all_mints"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_atas'
    #[flow]
    fn flow_setup_all_atas(&mut self) {
        let mut tx = SetupAllAtasTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_minted(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("setup_all_atas"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_harvest_position'
    #[flow]
    fn flow_verify_harvest_position(&mut self) {
        let mut tx = VerifyHarvestPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_harvest_ix(Default::default() /* & HarvestPositionInstruction */);
        tx.set_ata_a(Pubkey::new_unique());
        tx.set_ata_b(Pubkey::new_unique());
        tx.set_position_mint(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("verify_harvest_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_mints'
    #[flow]
    fn flow_setup_all_mints(&mut self) {
        let mut tx = SetupAllMintsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("setup_all_mints"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_all_atas'
    #[flow]
    fn flow_setup_all_atas(&mut self) {
        let mut tx = SetupAllAtasTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_minted(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("setup_all_atas"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'program_config_set_authority'
    #[flow]
    fn flow_program_config_set_authority(&mut self) {
        let mut tx = ProgramConfigSetAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* ProgramConfigSetAuthorityArgs */);
        self.trident.execute_transaction(&mut tx, Some("program_config_set_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'program_config_set_multisig_creation_fee'
    #[flow]
    fn flow_program_config_set_multisig_creation_fee(&mut self) {
        let mut tx = ProgramConfigSetMultisigCreationFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* ProgramConfigSetMultisigCreationFeeArgs */);
        self.trident.execute_transaction(&mut tx, Some("program_config_set_multisig_creation_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'program_config_set_treasury'
    #[flow]
    fn flow_program_config_set_treasury(&mut self) {
        let mut tx = ProgramConfigSetTreasuryTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* ProgramConfigSetTreasuryArgs */);
        self.trident.execute_transaction(&mut tx, Some("program_config_set_treasury"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_add_member'
    #[flow]
    fn flow_multisig_add_member(&mut self) {
        let mut tx = MultisigAddMemberTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigAddMemberArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_add_member"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_remove_member'
    #[flow]
    fn flow_multisig_remove_member(&mut self) {
        let mut tx = MultisigRemoveMemberTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigRemoveMemberArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_remove_member"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_set_time_lock'
    #[flow]
    fn flow_multisig_set_time_lock(&mut self) {
        let mut tx = MultisigSetTimeLockTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigSetTimeLockArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_set_time_lock"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_change_threshold'
    #[flow]
    fn flow_multisig_change_threshold(&mut self) {
        let mut tx = MultisigChangeThresholdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigChangeThresholdArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_change_threshold"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_set_config_authority'
    #[flow]
    fn flow_multisig_set_config_authority(&mut self) {
        let mut tx = MultisigSetConfigAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigSetConfigAuthorityArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_set_config_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_set_rent_collector'
    #[flow]
    fn flow_multisig_set_rent_collector(&mut self) {
        let mut tx = MultisigSetRentCollectorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigSetRentCollectorArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_set_rent_collector"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_add_spending_limit'
    #[flow]
    fn flow_multisig_add_spending_limit(&mut self) {
        let mut tx = MultisigAddSpendingLimitTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigAddSpendingLimitArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_add_spending_limit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'multisig_remove_spending_limit'
    #[flow]
    fn flow_multisig_remove_spending_limit(&mut self) {
        let mut tx = MultisigRemoveSpendingLimitTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* MultisigRemoveSpendingLimitArgs */);
        self.trident.execute_transaction(&mut tx, Some("multisig_remove_spending_limit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'config_transaction_execute'
    #[flow]
    fn flow_config_transaction_execute(&mut self) {
        let mut tx = ConfigTransactionExecuteTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("config_transaction_execute"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transaction_buffer_close'
    #[flow]
    fn flow_transaction_buffer_close(&mut self) {
        let mut tx = TransactionBufferCloseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("transaction_buffer_close"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transaction_buffer_extend'
    #[flow]
    fn flow_transaction_buffer_extend(&mut self) {
        let mut tx = TransactionBufferExtendTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* TransactionBufferExtendArgs */);
        self.trident.execute_transaction(&mut tx, Some("transaction_buffer_extend"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'vault_transaction_execute'
    #[flow]
    fn flow_vault_transaction_execute(&mut self) {
        let mut tx = VaultTransactionExecuteTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("vault_transaction_execute"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'batch_add_transaction'
    #[flow]
    fn flow_batch_add_transaction(&mut self) {
        let mut tx = BatchAddTransactionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* BatchAddTransactionArgs */);
        self.trident.execute_transaction(&mut tx, Some("batch_add_transaction"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'batch_execute_transaction'
    #[flow]
    fn flow_batch_execute_transaction(&mut self) {
        let mut tx = BatchExecuteTransactionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("batch_execute_transaction"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'proposal_activate'
    #[flow]
    fn flow_proposal_activate(&mut self) {
        let mut tx = ProposalActivateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("proposal_activate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'proposal_approve'
    #[flow]
    fn flow_proposal_approve(&mut self) {
        let mut tx = ProposalApproveTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* ProposalVoteArgs */);
        self.trident.execute_transaction(&mut tx, Some("proposal_approve"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'proposal_reject'
    #[flow]
    fn flow_proposal_reject(&mut self) {
        let mut tx = ProposalRejectTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* ProposalVoteArgs */);
        self.trident.execute_transaction(&mut tx, Some("proposal_reject"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'proposal_cancel'
    #[flow]
    fn flow_proposal_cancel(&mut self) {
        let mut tx = ProposalCancelTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* ProposalVoteArgs */);
        self.trident.execute_transaction(&mut tx, Some("proposal_cancel"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'proposal_cancel_v2'
    #[flow]
    fn flow_proposal_cancel_v2(&mut self) {
        let mut tx = ProposalCancelV2Transaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* ProposalVoteArgs */);
        self.trident.execute_transaction(&mut tx, Some("proposal_cancel_v2"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'spending_limit_use'
    #[flow]
    fn flow_spending_limit_use(&mut self) {
        let mut tx = SpendingLimitUseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_args(Default::default() /* SpendingLimitUseArgs */);
        self.trident.execute_transaction(&mut tx, Some("spending_limit_use"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'config_transaction_accounts_close'
    #[flow]
    fn flow_config_transaction_accounts_close(&mut self) {
        let mut tx = ConfigTransactionAccountsCloseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("config_transaction_accounts_close"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'vault_transaction_accounts_close'
    #[flow]
    fn flow_vault_transaction_accounts_close(&mut self) {
        let mut tx = VaultTransactionAccountsCloseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("vault_transaction_accounts_close"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'vault_batch_transaction_account_close'
    #[flow]
    fn flow_vault_batch_transaction_account_close(&mut self) {
        let mut tx = VaultBatchTransactionAccountCloseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("vault_batch_transaction_account_close"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'batch_accounts_close'
    #[flow]
    fn flow_batch_accounts_close(&mut self) {
        let mut tx = BatchAccountsCloseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("batch_accounts_close"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_price'
    #[flow]
    fn flow_set_price(&mut self) {
        let mut tx = SetPriceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_price(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("set_price"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_price_info'
    #[flow]
    fn flow_set_price_info(&mut self) {
        let mut tx = SetPriceInfoTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_price(rand::random::<i64>());
        tx.set_conf(rand::random::<u64>());
        tx.set_slot(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("set_price_info"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_twap'
    #[flow]
    fn flow_set_twap(&mut self) {
        let mut tx = SetTwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_twap(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("set_twap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'mint_to_user'
    #[flow]
    fn flow_mint_to_user(&mut self) {
        let mut tx = MintToUserTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("mint_to_user"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_mint_authority'
    #[flow]
    fn flow_transfer_mint_authority(&mut self) {
        let mut tx = TransferMintAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("transfer_mint_authority"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'resize_signed_msg_user_orders'
    #[flow]
    fn flow_resize_signed_msg_user_orders(&mut self) {
        let mut tx = ResizeSignedMsgUserOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_num_orders(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("resize_signed_msg_user_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'change_signed_msg_ws_delegate_status'
    #[flow]
    fn flow_change_signed_msg_ws_delegate_status(&mut self) {
        let mut tx = ChangeSignedMsgWsDelegateStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_delegate(Pubkey::new_unique());
        tx.set_add(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("change_signed_msg_ws_delegate_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'sweep_fuel'
    #[flow]
    fn flow_sweep_fuel(&mut self) {
        let mut tx = SweepFuelTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("sweep_fuel"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'reset_fuel_season'
    #[flow]
    fn flow_reset_fuel_season(&mut self) {
        let mut tx = ResetFuelSeasonTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("reset_fuel_season"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit'
    #[flow]
    fn flow_deposit(&mut self) {
        let mut tx = DepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        tx.set_reduce_only(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("deposit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw'
    #[flow]
    fn flow_withdraw(&mut self) {
        let mut tx = WithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        tx.set_reduce_only(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("withdraw"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_deposit'
    #[flow]
    fn flow_transfer_deposit(&mut self) {
        let mut tx = TransferDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("transfer_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_pools'
    #[flow]
    fn flow_transfer_pools(&mut self) {
        let mut tx = TransferPoolsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_deposit_from_market_index(rand::random::<u16>());
        tx.set_deposit_to_market_index(rand::random::<u16>());
        tx.set_borrow_from_market_index(rand::random::<u16>());
        tx.set_borrow_to_market_index(rand::random::<u16>());
        tx.set_deposit_amount(Default::default() /* Option < u64 > */);
        tx.set_borrow_amount(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("transfer_pools"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_perp_position'
    #[flow]
    fn flow_transfer_perp_position(&mut self) {
        let mut tx = TransferPerpPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(Default::default() /* Option < i64 > */);
        self.trident.execute_transaction(&mut tx, Some("transfer_perp_position"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit_into_isolated_perp_position'
    #[flow]
    fn flow_deposit_into_isolated_perp_position(&mut self) {
        let mut tx = DepositIntoIsolatedPerpPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit_into_isolated_perp_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_isolated_perp_position_deposit'
    #[flow]
    fn flow_transfer_isolated_perp_position_deposit(&mut self) {
        let mut tx = TransferIsolatedPerpPositionDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("transfer_isolated_perp_position_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw_from_isolated_perp_position'
    #[flow]
    fn flow_withdraw_from_isolated_perp_position(&mut self) {
        let mut tx = WithdrawFromIsolatedPerpPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("withdraw_from_isolated_perp_position"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_perp_order'
    #[flow]
    fn flow_place_perp_order(&mut self) {
        let mut tx = PlacePerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        self.trident.execute_transaction(&mut tx, Some("place_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'cancel_order'
    #[flow]
    fn flow_cancel_order(&mut self) {
        let mut tx = CancelOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("cancel_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'cancel_order_by_user_id'
    #[flow]
    fn flow_cancel_order_by_user_id(&mut self) {
        let mut tx = CancelOrderByUserIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_user_order_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("cancel_order_by_user_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'cancel_orders'
    #[flow]
    fn flow_cancel_orders(&mut self) {
        let mut tx = CancelOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_type(Default::default() /* Option < MarketType > */);
        tx.set_market_index(Default::default() /* Option < u16 > */);
        tx.set_direction(Default::default() /* Option < PositionDirection > */);
        self.trident.execute_transaction(&mut tx, Some("cancel_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'cancel_orders_by_ids'
    #[flow]
    fn flow_cancel_orders_by_ids(&mut self) {
        let mut tx = CancelOrdersByIdsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_ids(Default::default() /* Vec < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("cancel_orders_by_ids"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'modify_order'
    #[flow]
    fn flow_modify_order(&mut self) {
        let mut tx = ModifyOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        tx.set_modify_order_params(Default::default() /* ModifyOrderParams */);
        self.trident.execute_transaction(&mut tx, Some("modify_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'modify_order_by_user_id'
    #[flow]
    fn flow_modify_order_by_user_id(&mut self) {
        let mut tx = ModifyOrderByUserIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_user_order_id(rand::random::<u8>());
        tx.set_modify_order_params(Default::default() /* ModifyOrderParams */);
        self.trident.execute_transaction(&mut tx, Some("modify_order_by_user_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_and_take_perp_order'
    #[flow]
    fn flow_place_and_take_perp_order(&mut self) {
        let mut tx = PlaceAndTakePerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_success_condition(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("place_and_take_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_and_make_perp_order'
    #[flow]
    fn flow_place_and_make_perp_order(&mut self) {
        let mut tx = PlaceAndMakePerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_taker_order_id(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("place_and_make_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_and_make_signed_msg_perp_order'
    #[flow]
    fn flow_place_and_make_signed_msg_perp_order(&mut self) {
        let mut tx = PlaceAndMakeSignedMsgPerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_signed_msg_order_uuid(Default::default() /* [u8 ; 8] */);
        self.trident.execute_transaction(&mut tx, Some("place_and_make_signed_msg_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_signed_msg_taker_order'
    #[flow]
    fn flow_place_signed_msg_taker_order(&mut self) {
        let mut tx = PlaceSignedMsgTakerOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_signed_msg_order_params_message_bytes(Default::default() /* Vec < u8 > */);
        tx.set_is_delegate_signer(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("place_signed_msg_taker_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_spot_order'
    #[flow]
    fn flow_place_spot_order(&mut self) {
        let mut tx = PlaceSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        self.trident.execute_transaction(&mut tx, Some("place_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_and_take_spot_order'
    #[flow]
    fn flow_place_and_take_spot_order(&mut self) {
        let mut tx = PlaceAndTakeSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_fulfillment_type(Default::default() /* Option < SpotFulfillmentType > */);
        tx.set_maker_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("place_and_take_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_and_make_spot_order'
    #[flow]
    fn flow_place_and_make_spot_order(&mut self) {
        let mut tx = PlaceAndMakeSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_taker_order_id(rand::random::<u32>());
        tx.set_fulfillment_type(Default::default() /* Option < SpotFulfillmentType > */);
        self.trident.execute_transaction(&mut tx, Some("place_and_make_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_orders'
    #[flow]
    fn flow_place_orders(&mut self) {
        let mut tx = PlaceOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* Vec < OrderParams > */);
        self.trident.execute_transaction(&mut tx, Some("place_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_scale_orders'
    #[flow]
    fn flow_place_scale_orders(&mut self) {
        let mut tx = PlaceScaleOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* ScaleOrderParams */);
        self.trident.execute_transaction(&mut tx, Some("place_scale_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'begin_swap'
    #[flow]
    fn flow_begin_swap(&mut self) {
        let mut tx = BeginSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_amount_in(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("begin_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'end_swap'
    #[flow]
    fn flow_end_swap(&mut self) {
        let mut tx = EndSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        tx.set_reduce_only(Default::default() /* Option < SwapReduceOnly > */);
        self.trident.execute_transaction(&mut tx, Some("end_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_name'
    #[flow]
    fn flow_update_user_name(&mut self) {
        let mut tx = UpdateUserNameTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_name(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("update_user_name"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_custom_margin_ratio'
    #[flow]
    fn flow_update_user_custom_margin_ratio(&mut self) {
        let mut tx = UpdateUserCustomMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_margin_ratio(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_user_custom_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_perp_position_custom_margin_ratio'
    #[flow]
    fn flow_update_user_perp_position_custom_margin_ratio(&mut self) {
        let mut tx = UpdateUserPerpPositionCustomMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_margin_ratio(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_user_perp_position_custom_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_margin_trading_enabled'
    #[flow]
    fn flow_update_user_margin_trading_enabled(&mut self) {
        let mut tx = UpdateUserMarginTradingEnabledTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_margin_trading_enabled(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_user_margin_trading_enabled"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_pool_id'
    #[flow]
    fn flow_update_user_pool_id(&mut self) {
        let mut tx = UpdateUserPoolIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_pool_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_user_pool_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_delegate'
    #[flow]
    fn flow_update_user_delegate(&mut self) {
        let mut tx = UpdateUserDelegateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_delegate(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("update_user_delegate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_reduce_only'
    #[flow]
    fn flow_update_user_reduce_only(&mut self) {
        let mut tx = UpdateUserReduceOnlyTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_reduce_only(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_user_reduce_only"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_protected_maker_orders'
    #[flow]
    fn flow_update_user_protected_maker_orders(&mut self) {
        let mut tx = UpdateUserProtectedMakerOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_protected_maker_orders(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_user_protected_maker_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_user'
    #[flow]
    fn flow_delete_user(&mut self) {
        let mut tx = DeleteUserTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("delete_user"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'force_delete_user'
    #[flow]
    fn flow_force_delete_user(&mut self) {
        let mut tx = ForceDeleteUserTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("force_delete_user"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_signed_msg_user_orders'
    #[flow]
    fn flow_delete_signed_msg_user_orders(&mut self) {
        let mut tx = DeleteSignedMsgUserOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("delete_signed_msg_user_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'reclaim_rent'
    #[flow]
    fn flow_reclaim_rent(&mut self) {
        let mut tx = ReclaimRentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("reclaim_rent"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'enable_user_high_leverage_mode'
    #[flow]
    fn flow_enable_user_high_leverage_mode(&mut self) {
        let mut tx = EnableUserHighLeverageModeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_sub_account_id(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("enable_user_high_leverage_mode"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'fill_perp_order'
    #[flow]
    fn flow_fill_perp_order(&mut self) {
        let mut tx = FillPerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        tx.set__maker_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("fill_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'revert_fill'
    #[flow]
    fn flow_revert_fill(&mut self) {
        let mut tx = RevertFillTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("revert_fill"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'fill_spot_order'
    #[flow]
    fn flow_fill_spot_order(&mut self) {
        let mut tx = FillSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        tx.set_fulfillment_type(Default::default() /* Option < SpotFulfillmentType > */);
        tx.set_maker_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("fill_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'trigger_order'
    #[flow]
    fn flow_trigger_order(&mut self) {
        let mut tx = TriggerOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("trigger_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'force_cancel_orders'
    #[flow]
    fn flow_force_cancel_orders(&mut self) {
        let mut tx = ForceCancelOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("force_cancel_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_idle'
    #[flow]
    fn flow_update_user_idle(&mut self) {
        let mut tx = UpdateUserIdleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_user_idle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'log_user_balances'
    #[flow]
    fn flow_log_user_balances(&mut self) {
        let mut tx = LogUserBalancesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("log_user_balances"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'disable_user_high_leverage_mode'
    #[flow]
    fn flow_disable_user_high_leverage_mode(&mut self) {
        let mut tx = DisableUserHighLeverageModeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_disable_maintenance(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("disable_user_high_leverage_mode"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_stats_referrer_status'
    #[flow]
    fn flow_update_user_stats_referrer_status(&mut self) {
        let mut tx = UpdateUserStatsReferrerStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_user_stats_referrer_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'admin_update_user_stats_paused_operations'
    #[flow]
    fn flow_admin_update_user_stats_paused_operations(&mut self) {
        let mut tx = AdminUpdateUserStatsPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("admin_update_user_stats_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'settle_pnl'
    #[flow]
    fn flow_settle_pnl(&mut self) {
        let mut tx = SettlePnlTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("settle_pnl"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'settle_multiple_pnls'
    #[flow]
    fn flow_settle_multiple_pnls(&mut self) {
        let mut tx = SettleMultiplePnlsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_indexes(Default::default() /* Vec < u16 > */);
        tx.set_mode(Default::default() /* SettlePnlMode */);
        self.trident.execute_transaction(&mut tx, Some("settle_multiple_pnls"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'settle_funding_payment'
    #[flow]
    fn flow_settle_funding_payment(&mut self) {
        let mut tx = SettleFundingPaymentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("settle_funding_payment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'settle_expired_market'
    #[flow]
    fn flow_settle_expired_market(&mut self) {
        let mut tx = SettleExpiredMarketTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("settle_expired_market"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquidate_perp'
    #[flow]
    fn flow_liquidate_perp(&mut self) {
        let mut tx = LiquidatePerpTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_liquidator_max_base_asset_amount(rand::random::<u64>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("liquidate_perp"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquidate_perp_with_fill'
    #[flow]
    fn flow_liquidate_perp_with_fill(&mut self) {
        let mut tx = LiquidatePerpWithFillTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("liquidate_perp_with_fill"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquidate_spot'
    #[flow]
    fn flow_liquidate_spot(&mut self) {
        let mut tx = LiquidateSpotTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_market_index(rand::random::<u16>());
        tx.set_liability_market_index(rand::random::<u16>());
        tx.set_liquidator_max_liability_transfer(rand::random::<u128>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("liquidate_spot"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquidate_spot_with_swap_begin'
    #[flow]
    fn flow_liquidate_spot_with_swap_begin(&mut self) {
        let mut tx = LiquidateSpotWithSwapBeginTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_market_index(rand::random::<u16>());
        tx.set_liability_market_index(rand::random::<u16>());
        tx.set_swap_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("liquidate_spot_with_swap_begin"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquidate_spot_with_swap_end'
    #[flow]
    fn flow_liquidate_spot_with_swap_end(&mut self) {
        let mut tx = LiquidateSpotWithSwapEndTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_market_index(rand::random::<u16>());
        tx.set_liability_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("liquidate_spot_with_swap_end"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquidate_borrow_for_perp_pnl'
    #[flow]
    fn flow_liquidate_borrow_for_perp_pnl(&mut self) {
        let mut tx = LiquidateBorrowForPerpPnlTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_liquidator_max_liability_transfer(rand::random::<u128>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("liquidate_borrow_for_perp_pnl"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquidate_perp_pnl_for_deposit'
    #[flow]
    fn flow_liquidate_perp_pnl_for_deposit(&mut self) {
        let mut tx = LiquidatePerpPnlForDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_liquidator_max_pnl_transfer(rand::random::<u128>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("liquidate_perp_pnl_for_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_user_status_to_being_liquidated'
    #[flow]
    fn flow_set_user_status_to_being_liquidated(&mut self) {
        let mut tx = SetUserStatusToBeingLiquidatedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_user_status_to_being_liquidated"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'resolve_perp_pnl_deficit'
    #[flow]
    fn flow_resolve_perp_pnl_deficit(&mut self) {
        let mut tx = ResolvePerpPnlDeficitTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("resolve_perp_pnl_deficit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'resolve_perp_bankruptcy'
    #[flow]
    fn flow_resolve_perp_bankruptcy(&mut self) {
        let mut tx = ResolvePerpBankruptcyTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_quote_spot_market_index(rand::random::<u16>());
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("resolve_perp_bankruptcy"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'resolve_spot_bankruptcy'
    #[flow]
    fn flow_resolve_spot_bankruptcy(&mut self) {
        let mut tx = ResolveSpotBankruptcyTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("resolve_spot_bankruptcy"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'settle_revenue_to_insurance_fund'
    #[flow]
    fn flow_settle_revenue_to_insurance_fund(&mut self) {
        let mut tx = SettleRevenueToInsuranceFundTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("settle_revenue_to_insurance_fund"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_funding_rate'
    #[flow]
    fn flow_update_funding_rate(&mut self) {
        let mut tx = UpdateFundingRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_funding_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_prelaunch_oracle'
    #[flow]
    fn flow_update_prelaunch_oracle(&mut self) {
        let mut tx = UpdatePrelaunchOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_prelaunch_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_bid_ask_twap'
    #[flow]
    fn flow_update_perp_bid_ask_twap(&mut self) {
        let mut tx = UpdatePerpBidAskTwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_perp_bid_ask_twap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_cumulative_interest'
    #[flow]
    fn flow_update_spot_market_cumulative_interest(&mut self) {
        let mut tx = UpdateSpotMarketCumulativeInterestTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_cumulative_interest"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_amms'
    #[flow]
    fn flow_update_amms(&mut self) {
        let mut tx = UpdateAmmsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_indexes(Default::default() /* Vec < u16 > */);
        self.trident.execute_transaction(&mut tx, Some("update_amms"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_expiry'
    #[flow]
    fn flow_update_spot_market_expiry(&mut self) {
        let mut tx = UpdateSpotMarketExpiryTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_expiry_ts(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_expiry"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_quote_asset_insurance_stake'
    #[flow]
    fn flow_update_user_quote_asset_insurance_stake(&mut self) {
        let mut tx = UpdateUserQuoteAssetInsuranceStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_user_quote_asset_insurance_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_user_gov_token_insurance_stake'
    #[flow]
    fn flow_update_user_gov_token_insurance_stake(&mut self) {
        let mut tx = UpdateUserGovTokenInsuranceStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_user_gov_token_insurance_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_delegate_user_gov_token_insurance_stake'
    #[flow]
    fn flow_update_delegate_user_gov_token_insurance_stake(&mut self) {
        let mut tx = UpdateDelegateUserGovTokenInsuranceStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_delegate_user_gov_token_insurance_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_insurance_fund_stake'
    #[flow]
    fn flow_add_insurance_fund_stake(&mut self) {
        let mut tx = AddInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("add_insurance_fund_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'request_remove_insurance_fund_stake'
    #[flow]
    fn flow_request_remove_insurance_fund_stake(&mut self) {
        let mut tx = RequestRemoveInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("request_remove_insurance_fund_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'cancel_request_remove_insurance_fund_stake'
    #[flow]
    fn flow_cancel_request_remove_insurance_fund_stake(&mut self) {
        let mut tx = CancelRequestRemoveInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("cancel_request_remove_insurance_fund_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'remove_insurance_fund_stake'
    #[flow]
    fn flow_remove_insurance_fund_stake(&mut self) {
        let mut tx = RemoveInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("remove_insurance_fund_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'begin_insurance_fund_swap'
    #[flow]
    fn flow_begin_insurance_fund_swap(&mut self) {
        let mut tx = BeginInsuranceFundSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_amount_in(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("begin_insurance_fund_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'end_insurance_fund_swap'
    #[flow]
    fn flow_end_insurance_fund_swap(&mut self) {
        let mut tx = EndInsuranceFundSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("end_insurance_fund_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_protocol_if_shares_to_revenue_pool'
    #[flow]
    fn flow_transfer_protocol_if_shares_to_revenue_pool(&mut self) {
        let mut tx = TransferProtocolIfSharesToRevenuePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("transfer_protocol_if_shares_to_revenue_pool"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit_into_insurance_fund_stake'
    #[flow]
    fn flow_deposit_into_insurance_fund_stake(&mut self) {
        let mut tx = DepositIntoInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit_into_insurance_fund_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_pyth_pull_oracle'
    #[flow]
    fn flow_update_pyth_pull_oracle(&mut self) {
        let mut tx = UpdatePythPullOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_feed_id(Default::default() /* [u8 ; 32] */);
        tx.set_params(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("update_pyth_pull_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_pyth_pull_oracle_update_atomic'
    #[flow]
    fn flow_post_pyth_pull_oracle_update_atomic(&mut self) {
        let mut tx = PostPythPullOracleUpdateAtomicTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_feed_id(Default::default() /* [u8 ; 32] */);
        tx.set_params(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("post_pyth_pull_oracle_update_atomic"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_multi_pyth_pull_oracle_updates_atomic'
    #[flow]
    fn flow_post_multi_pyth_pull_oracle_updates_atomic(&mut self) {
        let mut tx = PostMultiPythPullOracleUpdatesAtomicTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("post_multi_pyth_pull_oracle_updates_atomic"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'pause_spot_market_deposit_withdraw'
    #[flow]
    fn flow_pause_spot_market_deposit_withdraw(&mut self) {
        let mut tx = PauseSpotMarketDepositWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("pause_spot_market_deposit_withdraw"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_serum_fulfillment_config_status'
    #[flow]
    fn flow_update_serum_fulfillment_config_status(&mut self) {
        let mut tx = UpdateSerumFulfillmentConfigStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* SpotFulfillmentConfigStatus */);
        self.trident.execute_transaction(&mut tx, Some("update_serum_fulfillment_config_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_serum_fulfillment_config'
    #[flow]
    fn flow_delete_serum_fulfillment_config(&mut self) {
        let mut tx = DeleteSerumFulfillmentConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("delete_serum_fulfillment_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'openbook_v2_fulfillment_config_status'
    #[flow]
    fn flow_openbook_v2_fulfillment_config_status(&mut self) {
        let mut tx = OpenbookV2FulfillmentConfigStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* SpotFulfillmentConfigStatus */);
        self.trident.execute_transaction(&mut tx, Some("openbook_v2_fulfillment_config_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_openbook_v2_fulfillment_config'
    #[flow]
    fn flow_delete_openbook_v2_fulfillment_config(&mut self) {
        let mut tx = DeleteOpenbookV2FulfillmentConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("delete_openbook_v2_fulfillment_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'phoenix_fulfillment_config_status'
    #[flow]
    fn flow_phoenix_fulfillment_config_status(&mut self) {
        let mut tx = PhoenixFulfillmentConfigStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* SpotFulfillmentConfigStatus */);
        self.trident.execute_transaction(&mut tx, Some("phoenix_fulfillment_config_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_market_to_amm_cache'
    #[flow]
    fn flow_add_market_to_amm_cache(&mut self) {
        let mut tx = AddMarketToAmmCacheTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("add_market_to_amm_cache"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_amm_cache'
    #[flow]
    fn flow_delete_amm_cache(&mut self) {
        let mut tx = DeleteAmmCacheTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("delete_amm_cache"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'move_amm_price'
    #[flow]
    fn flow_move_amm_price(&mut self) {
        let mut tx = MoveAmmPriceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_base_asset_reserve(rand::random::<u128>());
        tx.set_quote_asset_reserve(rand::random::<u128>());
        tx.set_sqrt_k(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("move_amm_price"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'recenter_perp_market_amm'
    #[flow]
    fn flow_recenter_perp_market_amm(&mut self) {
        let mut tx = RecenterPerpMarketAmmTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_peg_multiplier(rand::random::<u128>());
        tx.set_sqrt_k(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("recenter_perp_market_amm"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'recenter_perp_market_amm_crank'
    #[flow]
    fn flow_recenter_perp_market_amm_crank(&mut self) {
        let mut tx = RecenterPerpMarketAmmCrankTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_depth(Default::default() /* Option < u128 > */);
        self.trident.execute_transaction(&mut tx, Some("recenter_perp_market_amm_crank"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_amm_summary_stats'
    #[flow]
    fn flow_update_perp_market_amm_summary_stats(&mut self) {
        let mut tx = UpdatePerpMarketAmmSummaryStatsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* UpdatePerpMarketSummaryStatsParams */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_amm_summary_stats"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_expiry'
    #[flow]
    fn flow_update_perp_market_expiry(&mut self) {
        let mut tx = UpdatePerpMarketExpiryTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_expiry_ts(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_expiry"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_lp_pool_paused_operations'
    #[flow]
    fn flow_update_perp_market_lp_pool_paused_operations(&mut self) {
        let mut tx = UpdatePerpMarketLpPoolPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_lp_pool_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_lp_pool_status'
    #[flow]
    fn flow_update_perp_market_lp_pool_status(&mut self) {
        let mut tx = UpdatePerpMarketLpPoolStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_lp_pool_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_lp_pool_fee_transfer_scalar'
    #[flow]
    fn flow_update_perp_market_lp_pool_fee_transfer_scalar(&mut self) {
        let mut tx = UpdatePerpMarketLpPoolFeeTransferScalarTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_optional_lp_fee_transfer_scalar(Default::default() /* Option < u8 > */);
        tx.set_optional_lp_net_pnl_transfer_scalar(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_lp_pool_fee_transfer_scalar"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'settle_expired_market_pools_to_revenue_pool'
    #[flow]
    fn flow_settle_expired_market_pools_to_revenue_pool(&mut self) {
        let mut tx = SettleExpiredMarketPoolsToRevenuePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("settle_expired_market_pools_to_revenue_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit_into_perp_market_fee_pool'
    #[flow]
    fn flow_deposit_into_perp_market_fee_pool(&mut self) {
        let mut tx = DepositIntoPerpMarketFeePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit_into_perp_market_fee_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_pnl_pool'
    #[flow]
    fn flow_update_perp_market_pnl_pool(&mut self) {
        let mut tx = UpdatePerpMarketPnlPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_pnl_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit_into_spot_market_vault'
    #[flow]
    fn flow_deposit_into_spot_market_vault(&mut self) {
        let mut tx = DepositIntoSpotMarketVaultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit_into_spot_market_vault"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit_into_spot_market_revenue_pool'
    #[flow]
    fn flow_deposit_into_spot_market_revenue_pool(&mut self) {
        let mut tx = DepositIntoSpotMarketRevenuePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit_into_spot_market_revenue_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'repeg_amm_curve'
    #[flow]
    fn flow_repeg_amm_curve(&mut self) {
        let mut tx = RepegAmmCurveTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_peg_candidate(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("repeg_amm_curve"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_amm_oracle_twap'
    #[flow]
    fn flow_update_perp_market_amm_oracle_twap(&mut self) {
        let mut tx = UpdatePerpMarketAmmOracleTwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_amm_oracle_twap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'reset_perp_market_amm_oracle_twap'
    #[flow]
    fn flow_reset_perp_market_amm_oracle_twap(&mut self) {
        let mut tx = ResetPerpMarketAmmOracleTwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("reset_perp_market_amm_oracle_twap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_k'
    #[flow]
    fn flow_update_k(&mut self) {
        let mut tx = UpdateKTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_sqrt_k(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("update_k"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_margin_ratio'
    #[flow]
    fn flow_update_perp_market_margin_ratio(&mut self) {
        let mut tx = UpdatePerpMarketMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_margin_ratio_initial(rand::random::<u32>());
        tx.set_margin_ratio_maintenance(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_high_leverage_margin_ratio'
    #[flow]
    fn flow_update_perp_market_high_leverage_margin_ratio(&mut self) {
        let mut tx = UpdatePerpMarketHighLeverageMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_margin_ratio_initial(rand::random::<u16>());
        tx.set_margin_ratio_maintenance(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_high_leverage_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_funding_period'
    #[flow]
    fn flow_update_perp_market_funding_period(&mut self) {
        let mut tx = UpdatePerpMarketFundingPeriodTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_funding_period(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_funding_period"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_max_imbalances'
    #[flow]
    fn flow_update_perp_market_max_imbalances(&mut self) {
        let mut tx = UpdatePerpMarketMaxImbalancesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_unrealized_max_imbalance(rand::random::<u64>());
        tx.set_max_revenue_withdraw_per_period(rand::random::<u64>());
        tx.set_quote_max_insurance(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_max_imbalances"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_liquidation_fee'
    #[flow]
    fn flow_update_perp_market_liquidation_fee(&mut self) {
        let mut tx = UpdatePerpMarketLiquidationFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidator_fee(rand::random::<u32>());
        tx.set_if_liquidation_fee(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_liquidation_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_lp_pool_id'
    #[flow]
    fn flow_update_perp_market_lp_pool_id(&mut self) {
        let mut tx = UpdatePerpMarketLpPoolIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_pool_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_lp_pool_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_insurance_fund_unstaking_period'
    #[flow]
    fn flow_update_insurance_fund_unstaking_period(&mut self) {
        let mut tx = UpdateInsuranceFundUnstakingPeriodTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_insurance_fund_unstaking_period(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("update_insurance_fund_unstaking_period"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_pool_id'
    #[flow]
    fn flow_update_spot_market_pool_id(&mut self) {
        let mut tx = UpdateSpotMarketPoolIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_pool_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_liquidation_fee'
    #[flow]
    fn flow_update_spot_market_liquidation_fee(&mut self) {
        let mut tx = UpdateSpotMarketLiquidationFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidator_fee(rand::random::<u32>());
        tx.set_if_liquidation_fee(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_liquidation_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_withdraw_guard_threshold'
    #[flow]
    fn flow_update_withdraw_guard_threshold(&mut self) {
        let mut tx = UpdateWithdrawGuardThresholdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_withdraw_guard_threshold(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_withdraw_guard_threshold"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_if_factor'
    #[flow]
    fn flow_update_spot_market_if_factor(&mut self) {
        let mut tx = UpdateSpotMarketIfFactorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_user_if_factor(rand::random::<u32>());
        tx.set_total_if_factor(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_if_factor"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_revenue_settle_period'
    #[flow]
    fn flow_update_spot_market_revenue_settle_period(&mut self) {
        let mut tx = UpdateSpotMarketRevenueSettlePeriodTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_revenue_settle_period(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_revenue_settle_period"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_status'
    #[flow]
    fn flow_update_spot_market_status(&mut self) {
        let mut tx = UpdateSpotMarketStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* MarketStatus */);
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_paused_operations'
    #[flow]
    fn flow_update_spot_market_paused_operations(&mut self) {
        let mut tx = UpdateSpotMarketPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_asset_tier'
    #[flow]
    fn flow_update_spot_market_asset_tier(&mut self) {
        let mut tx = UpdateSpotMarketAssetTierTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_tier(Default::default() /* AssetTier */);
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_asset_tier"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_margin_weights'
    #[flow]
    fn flow_update_spot_market_margin_weights(&mut self) {
        let mut tx = UpdateSpotMarketMarginWeightsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_initial_asset_weight(rand::random::<u32>());
        tx.set_maintenance_asset_weight(rand::random::<u32>());
        tx.set_initial_liability_weight(rand::random::<u32>());
        tx.set_maintenance_liability_weight(rand::random::<u32>());
        tx.set_imf_factor(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_margin_weights"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_borrow_rate'
    #[flow]
    fn flow_update_spot_market_borrow_rate(&mut self) {
        let mut tx = UpdateSpotMarketBorrowRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_optimal_utilization(rand::random::<u32>());
        tx.set_optimal_borrow_rate(rand::random::<u32>());
        tx.set_max_borrow_rate(rand::random::<u32>());
        tx.set_min_borrow_rate(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_borrow_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_max_token_deposits'
    #[flow]
    fn flow_update_spot_market_max_token_deposits(&mut self) {
        let mut tx = UpdateSpotMarketMaxTokenDepositsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_token_deposits(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_max_token_deposits"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_max_token_borrows'
    #[flow]
    fn flow_update_spot_market_max_token_borrows(&mut self) {
        let mut tx = UpdateSpotMarketMaxTokenBorrowsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_token_borrows_fraction(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_max_token_borrows"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_oracle'
    #[flow]
    fn flow_update_spot_market_oracle(&mut self) {
        let mut tx = UpdateSpotMarketOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle(Pubkey::new_unique());
        tx.set_oracle_source(Default::default() /* OracleSource */);
        tx.set_skip_invariant_check(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_step_size_and_tick_size'
    #[flow]
    fn flow_update_spot_market_step_size_and_tick_size(&mut self) {
        let mut tx = UpdateSpotMarketStepSizeAndTickSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_step_size(rand::random::<u64>());
        tx.set_tick_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_step_size_and_tick_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_min_order_size'
    #[flow]
    fn flow_update_spot_market_min_order_size(&mut self) {
        let mut tx = UpdateSpotMarketMinOrderSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_min_order_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_orders_enabled'
    #[flow]
    fn flow_update_spot_market_orders_enabled(&mut self) {
        let mut tx = UpdateSpotMarketOrdersEnabledTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_orders_enabled(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_orders_enabled"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_if_paused_operations'
    #[flow]
    fn flow_update_spot_market_if_paused_operations(&mut self) {
        let mut tx = UpdateSpotMarketIfPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_if_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_name'
    #[flow]
    fn flow_update_spot_market_name(&mut self) {
        let mut tx = UpdateSpotMarketNameTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_name(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_name"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_status'
    #[flow]
    fn flow_update_perp_market_status(&mut self) {
        let mut tx = UpdatePerpMarketStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* MarketStatus */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_paused_operations'
    #[flow]
    fn flow_update_perp_market_paused_operations(&mut self) {
        let mut tx = UpdatePerpMarketPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_contract_tier'
    #[flow]
    fn flow_update_perp_market_contract_tier(&mut self) {
        let mut tx = UpdatePerpMarketContractTierTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_contract_tier(Default::default() /* ContractTier */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_contract_tier"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_imf_factor'
    #[flow]
    fn flow_update_perp_market_imf_factor(&mut self) {
        let mut tx = UpdatePerpMarketImfFactorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_imf_factor(rand::random::<u32>());
        tx.set_unrealized_pnl_imf_factor(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_imf_factor"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_unrealized_asset_weight'
    #[flow]
    fn flow_update_perp_market_unrealized_asset_weight(&mut self) {
        let mut tx = UpdatePerpMarketUnrealizedAssetWeightTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_unrealized_initial_asset_weight(rand::random::<u32>());
        tx.set_unrealized_maintenance_asset_weight(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_unrealized_asset_weight"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_concentration_coef'
    #[flow]
    fn flow_update_perp_market_concentration_coef(&mut self) {
        let mut tx = UpdatePerpMarketConcentrationCoefTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_concentration_scale(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_concentration_coef"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_curve_update_intensity'
    #[flow]
    fn flow_update_perp_market_curve_update_intensity(&mut self) {
        let mut tx = UpdatePerpMarketCurveUpdateIntensityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_curve_update_intensity(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_curve_update_intensity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_reference_price_offset_deadband_pct'
    #[flow]
    fn flow_update_perp_market_reference_price_offset_deadband_pct(&mut self) {
        let mut tx = UpdatePerpMarketReferencePriceOffsetDeadbandPctTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reference_price_offset_deadband_pct(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_reference_price_offset_deadband_pct"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_fee_structure'
    #[flow]
    fn flow_update_perp_fee_structure(&mut self) {
        let mut tx = UpdatePerpFeeStructureTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_structure(Default::default() /* FeeStructure */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_fee_structure"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_fee_structure'
    #[flow]
    fn flow_update_spot_fee_structure(&mut self) {
        let mut tx = UpdateSpotFeeStructureTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_structure(Default::default() /* FeeStructure */);
        self.trident.execute_transaction(&mut tx, Some("update_spot_fee_structure"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_liquidation_duration'
    #[flow]
    fn flow_update_liquidation_duration(&mut self) {
        let mut tx = UpdateLiquidationDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidation_duration(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_liquidation_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_liquidation_margin_buffer_ratio'
    #[flow]
    fn flow_update_liquidation_margin_buffer_ratio(&mut self) {
        let mut tx = UpdateLiquidationMarginBufferRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidation_margin_buffer_ratio(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_liquidation_margin_buffer_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_oracle_guard_rails'
    #[flow]
    fn flow_update_oracle_guard_rails(&mut self) {
        let mut tx = UpdateOracleGuardRailsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle_guard_rails(Default::default() /* OracleGuardRails */);
        self.trident.execute_transaction(&mut tx, Some("update_oracle_guard_rails"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_state_settlement_duration'
    #[flow]
    fn flow_update_state_settlement_duration(&mut self) {
        let mut tx = UpdateStateSettlementDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_settlement_duration(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_state_settlement_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_state_max_number_of_sub_accounts'
    #[flow]
    fn flow_update_state_max_number_of_sub_accounts(&mut self) {
        let mut tx = UpdateStateMaxNumberOfSubAccountsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_number_of_sub_accounts(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_state_max_number_of_sub_accounts"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_oracle'
    #[flow]
    fn flow_update_perp_market_oracle(&mut self) {
        let mut tx = UpdatePerpMarketOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle(Pubkey::new_unique());
        tx.set_oracle_source(Default::default() /* OracleSource */);
        tx.set_skip_invariant_check(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_base_spread'
    #[flow]
    fn flow_update_perp_market_base_spread(&mut self) {
        let mut tx = UpdatePerpMarketBaseSpreadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_base_spread(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_base_spread"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_amm_jit_intensity'
    #[flow]
    fn flow_update_amm_jit_intensity(&mut self) {
        let mut tx = UpdateAmmJitIntensityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amm_jit_intensity(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_amm_jit_intensity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_max_spread'
    #[flow]
    fn flow_update_perp_market_max_spread(&mut self) {
        let mut tx = UpdatePerpMarketMaxSpreadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_spread(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_max_spread"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_step_size_and_tick_size'
    #[flow]
    fn flow_update_perp_market_step_size_and_tick_size(&mut self) {
        let mut tx = UpdatePerpMarketStepSizeAndTickSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_step_size(rand::random::<u64>());
        tx.set_tick_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_step_size_and_tick_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_name'
    #[flow]
    fn flow_update_perp_market_name(&mut self) {
        let mut tx = UpdatePerpMarketNameTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_name(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_name"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_min_order_size'
    #[flow]
    fn flow_update_perp_market_min_order_size(&mut self) {
        let mut tx = UpdatePerpMarketMinOrderSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_min_order_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_max_slippage_ratio'
    #[flow]
    fn flow_update_perp_market_max_slippage_ratio(&mut self) {
        let mut tx = UpdatePerpMarketMaxSlippageRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_slippage_ratio(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_max_slippage_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_max_fill_reserve_fraction'
    #[flow]
    fn flow_update_perp_market_max_fill_reserve_fraction(&mut self) {
        let mut tx = UpdatePerpMarketMaxFillReserveFractionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_fill_reserve_fraction(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_max_fill_reserve_fraction"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_max_open_interest'
    #[flow]
    fn flow_update_perp_market_max_open_interest(&mut self) {
        let mut tx = UpdatePerpMarketMaxOpenInterestTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_open_interest(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_max_open_interest"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_number_of_users'
    #[flow]
    fn flow_update_perp_market_number_of_users(&mut self) {
        let mut tx = UpdatePerpMarketNumberOfUsersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_number_of_users(Default::default() /* Option < u32 > */);
        tx.set_number_of_users_with_base(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_number_of_users"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_fee_adjustment'
    #[flow]
    fn flow_update_perp_market_fee_adjustment(&mut self) {
        let mut tx = UpdatePerpMarketFeeAdjustmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_adjustment(rand::random::<i16>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_fee_adjustment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_market_fee_adjustment'
    #[flow]
    fn flow_update_spot_market_fee_adjustment(&mut self) {
        let mut tx = UpdateSpotMarketFeeAdjustmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_adjustment(rand::random::<i16>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_market_fee_adjustment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_protected_maker_params'
    #[flow]
    fn flow_update_perp_market_protected_maker_params(&mut self) {
        let mut tx = UpdatePerpMarketProtectedMakerParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_protected_maker_limit_price_divisor(Default::default() /* Option < u8 > */);
        tx.set_protected_maker_dynamic_divisor(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_protected_maker_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_oracle_low_risk_slot_delay_override'
    #[flow]
    fn flow_update_perp_market_oracle_low_risk_slot_delay_override(&mut self) {
        let mut tx = UpdatePerpMarketOracleLowRiskSlotDelayOverrideTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle_low_risk_slot_delay_override(rand::random::<i8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_oracle_low_risk_slot_delay_override"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_amm_spread_adjustment'
    #[flow]
    fn flow_update_perp_market_amm_spread_adjustment(&mut self) {
        let mut tx = UpdatePerpMarketAmmSpreadAdjustmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amm_spread_adjustment(rand::random::<i8>());
        tx.set_amm_inventory_spread_adjustment(rand::random::<i8>());
        tx.set_reference_price_offset(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_amm_spread_adjustment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_market_oracle_slot_delay_override'
    #[flow]
    fn flow_update_perp_market_oracle_slot_delay_override(&mut self) {
        let mut tx = UpdatePerpMarketOracleSlotDelayOverrideTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle_slot_delay_override(rand::random::<i8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_market_oracle_slot_delay_override"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_admin'
    #[flow]
    fn flow_update_admin(&mut self) {
        let mut tx = UpdateAdminTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_admin(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("update_admin"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_discount_mint'
    #[flow]
    fn flow_update_discount_mint(&mut self) {
        let mut tx = UpdateDiscountMintTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_discount_mint(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("update_discount_mint"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_exchange_status'
    #[flow]
    fn flow_update_exchange_status(&mut self) {
        let mut tx = UpdateExchangeStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_exchange_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_exchange_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_perp_auction_duration'
    #[flow]
    fn flow_update_perp_auction_duration(&mut self) {
        let mut tx = UpdatePerpAuctionDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_min_perp_auction_duration(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_perp_auction_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_spot_auction_duration'
    #[flow]
    fn flow_update_spot_auction_duration(&mut self) {
        let mut tx = UpdateSpotAuctionDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_spot_auction_duration(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_spot_auction_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_prelaunch_oracle_params'
    #[flow]
    fn flow_update_prelaunch_oracle_params(&mut self) {
        let mut tx = UpdatePrelaunchOracleParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* PrelaunchOracleParams */);
        self.trident.execute_transaction(&mut tx, Some("update_prelaunch_oracle_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delete_prelaunch_oracle'
    #[flow]
    fn flow_delete_prelaunch_oracle(&mut self) {
        let mut tx = DeletePrelaunchOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("delete_prelaunch_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_pyth_lazer_oracle_update'
    #[flow]
    fn flow_post_pyth_lazer_oracle_update(&mut self) {
        let mut tx = PostPythLazerOracleUpdateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pyth_message(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("post_pyth_lazer_oracle_update"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_high_leverage_mode_config'
    #[flow]
    fn flow_update_high_leverage_mode_config(&mut self) {
        let mut tx = UpdateHighLeverageModeConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_users(rand::random::<u32>());
        tx.set_reduce_only(rand::random::<bool>());
        tx.set_current_users(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("update_high_leverage_mode_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_protected_maker_mode_config'
    #[flow]
    fn flow_update_protected_maker_mode_config(&mut self) {
        let mut tx = UpdateProtectedMakerModeConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_users(rand::random::<u32>());
        tx.set_reduce_only(rand::random::<bool>());
        tx.set_current_users(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("update_protected_maker_mode_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'admin_deposit'
    #[flow]
    fn flow_admin_deposit(&mut self) {
        let mut tx = AdminDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("admin_deposit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_if_rebalance_config'
    #[flow]
    fn flow_update_if_rebalance_config(&mut self) {
        let mut tx = UpdateIfRebalanceConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* IfRebalanceConfigParams */);
        self.trident.execute_transaction(&mut tx, Some("update_if_rebalance_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_feature_bit_flags_mm_oracle'
    #[flow]
    fn flow_update_feature_bit_flags_mm_oracle(&mut self) {
        let mut tx = UpdateFeatureBitFlagsMmOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_feature_bit_flags_mm_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'zero_mm_oracle_fields'
    #[flow]
    fn flow_zero_mm_oracle_fields(&mut self) {
        let mut tx = ZeroMmOracleFieldsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("zero_mm_oracle_fields"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_feature_bit_flags_median_trigger_price'
    #[flow]
    fn flow_update_feature_bit_flags_median_trigger_price(&mut self) {
        let mut tx = UpdateFeatureBitFlagsMedianTriggerPriceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_feature_bit_flags_median_trigger_price"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_feature_bit_flags_builder_codes'
    #[flow]
    fn flow_update_feature_bit_flags_builder_codes(&mut self) {
        let mut tx = UpdateFeatureBitFlagsBuilderCodesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_feature_bit_flags_builder_codes"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'resize_revenue_share_escrow_orders'
    #[flow]
    fn flow_resize_revenue_share_escrow_orders(&mut self) {
        let mut tx = ResizeRevenueShareEscrowOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_num_orders(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("resize_revenue_share_escrow_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'change_approved_builder'
    #[flow]
    fn flow_change_approved_builder(&mut self) {
        let mut tx = ChangeApprovedBuilderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_builder(Pubkey::new_unique());
        tx.set_max_fee_bps(rand::random::<u16>());
        tx.set_add(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("change_approved_builder"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_feature_bit_flags_settle_lp_pool'
    #[flow]
    fn flow_update_feature_bit_flags_settle_lp_pool(&mut self) {
        let mut tx = UpdateFeatureBitFlagsSettleLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_feature_bit_flags_settle_lp_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_feature_bit_flags_swap_lp_pool'
    #[flow]
    fn flow_update_feature_bit_flags_swap_lp_pool(&mut self) {
        let mut tx = UpdateFeatureBitFlagsSwapLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_feature_bit_flags_swap_lp_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_feature_bit_flags_mint_redeem_lp_pool'
    #[flow]
    fn flow_update_feature_bit_flags_mint_redeem_lp_pool(&mut self) {
        let mut tx = UpdateFeatureBitFlagsMintRedeemLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("update_feature_bit_flags_mint_redeem_lp_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_constituent_status'
    #[flow]
    fn flow_update_constituent_status(&mut self) {
        let mut tx = UpdateConstituentStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_constituent_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_constituent_paused_operations'
    #[flow]
    fn flow_update_constituent_paused_operations(&mut self) {
        let mut tx = UpdateConstituentPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("update_constituent_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_constituent_params'
    #[flow]
    fn flow_update_constituent_params(&mut self) {
        let mut tx = UpdateConstituentParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_constituent_params(Default::default() /* ConstituentParams */);
        self.trident.execute_transaction(&mut tx, Some("update_constituent_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_lp_pool_params'
    #[flow]
    fn flow_update_lp_pool_params(&mut self) {
        let mut tx = UpdateLpPoolParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_pool_params(Default::default() /* LpPoolParams */);
        self.trident.execute_transaction(&mut tx, Some("update_lp_pool_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_amm_constituent_mapping_data'
    #[flow]
    fn flow_add_amm_constituent_mapping_data(&mut self) {
        let mut tx = AddAmmConstituentMappingDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amm_constituent_mapping_data(Default::default() /* Vec < AddAmmConstituentMappingDatum > */);
        self.trident.execute_transaction(&mut tx, Some("add_amm_constituent_mapping_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_amm_constituent_mapping_data'
    #[flow]
    fn flow_update_amm_constituent_mapping_data(&mut self) {
        let mut tx = UpdateAmmConstituentMappingDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amm_constituent_mapping_data(Default::default() /* Vec < AddAmmConstituentMappingDatum > */);
        self.trident.execute_transaction(&mut tx, Some("update_amm_constituent_mapping_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'remove_amm_constituent_mapping_data'
    #[flow]
    fn flow_remove_amm_constituent_mapping_data(&mut self) {
        let mut tx = RemoveAmmConstituentMappingDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_constituent_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("remove_amm_constituent_mapping_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_constituent_correlation_data'
    #[flow]
    fn flow_update_constituent_correlation_data(&mut self) {
        let mut tx = UpdateConstituentCorrelationDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_index1(rand::random::<u16>());
        tx.set_index2(rand::random::<u16>());
        tx.set_correlation(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("update_constituent_correlation_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_lp_constituent_target_base'
    #[flow]
    fn flow_update_lp_constituent_target_base(&mut self) {
        let mut tx = UpdateLpConstituentTargetBaseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_lp_constituent_target_base"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_lp_pool_aum'
    #[flow]
    fn flow_update_lp_pool_aum(&mut self) {
        let mut tx = UpdateLpPoolAumTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_lp_pool_aum"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_amm_cache'
    #[flow]
    fn flow_update_amm_cache(&mut self) {
        let mut tx = UpdateAmmCacheTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_amm_cache"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'override_amm_cache_info'
    #[flow]
    fn flow_override_amm_cache_info(&mut self) {
        let mut tx = OverrideAmmCacheInfoTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_override_params(Default::default() /* OverrideAmmCacheParams */);
        self.trident.execute_transaction(&mut tx, Some("override_amm_cache_info"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'lp_pool_swap'
    #[flow]
    fn flow_lp_pool_swap(&mut self) {
        let mut tx = LpPoolSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u64>());
        tx.set_min_out_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("lp_pool_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'view_lp_pool_swap_fees'
    #[flow]
    fn flow_view_lp_pool_swap_fees(&mut self) {
        let mut tx = ViewLpPoolSwapFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u64>());
        tx.set_in_target_weight(rand::random::<i64>());
        tx.set_out_target_weight(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("view_lp_pool_swap_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'lp_pool_add_liquidity'
    #[flow]
    fn flow_lp_pool_add_liquidity(&mut self) {
        let mut tx = LpPoolAddLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u128>());
        tx.set_min_mint_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("lp_pool_add_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'lp_pool_remove_liquidity'
    #[flow]
    fn flow_lp_pool_remove_liquidity(&mut self) {
        let mut tx = LpPoolRemoveLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u64>());
        tx.set_min_out_amount(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("lp_pool_remove_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'view_lp_pool_add_liquidity_fees'
    #[flow]
    fn flow_view_lp_pool_add_liquidity_fees(&mut self) {
        let mut tx = ViewLpPoolAddLiquidityFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("view_lp_pool_add_liquidity_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'view_lp_pool_remove_liquidity_fees'
    #[flow]
    fn flow_view_lp_pool_remove_liquidity_fees(&mut self) {
        let mut tx = ViewLpPoolRemoveLiquidityFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("view_lp_pool_remove_liquidity_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'begin_lp_swap'
    #[flow]
    fn flow_begin_lp_swap(&mut self) {
        let mut tx = BeginLpSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_amount_in(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("begin_lp_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'end_lp_swap'
    #[flow]
    fn flow_end_lp_swap(&mut self) {
        let mut tx = EndLpSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__in_market_index(rand::random::<u16>());
        tx.set__out_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("end_lp_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_constituent_oracle_info'
    #[flow]
    fn flow_update_constituent_oracle_info(&mut self) {
        let mut tx = UpdateConstituentOracleInfoTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("update_constituent_oracle_info"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit_to_program_vault'
    #[flow]
    fn flow_deposit_to_program_vault(&mut self) {
        let mut tx = DepositToProgramVaultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit_to_program_vault"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw_from_program_vault'
    #[flow]
    fn flow_withdraw_from_program_vault(&mut self) {
        let mut tx = WithdrawFromProgramVaultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("withdraw_from_program_vault"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'settle_perp_to_lp_pool'
    #[flow]
    fn flow_settle_perp_to_lp_pool(&mut self) {
        let mut tx = SettlePerpToLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("settle_perp_to_lp_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'calculate_settlement_amount'
    #[flow]
    fn flow_calculate_settlement_amount(&mut self) {
        let mut tx = CalculateSettlementAmountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("calculate_settlement_amount"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'validate_settlement_amount'
    #[flow]
    fn flow_validate_settlement_amount(&mut self) {
        let mut tx = ValidateSettlementAmountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_result(Default::default() /* & SettlementResult */);
        tx.set_perp_market(Default::default() /* & PerpMarket */);
        tx.set_quote_spot_market(Default::default() /* & SpotMarket */);
        self.trident.execute_transaction(&mut tx, Some("validate_settlement_amount"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'calculate_lp_to_perp_settlement'
    #[flow]
    fn flow_calculate_lp_to_perp_settlement(&mut self) {
        let mut tx = CalculateLpToPerpSettlementTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("calculate_lp_to_perp_settlement"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'calculate_perp_to_lp_settlement'
    #[flow]
    fn flow_calculate_perp_to_lp_settlement(&mut self) {
        let mut tx = CalculatePerpToLpSettlementTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("calculate_perp_to_lp_settlement"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'calculate_margin_requirement_and_total_collateral_and_liability_info'
    #[flow]
    fn flow_calculate_margin_requirement_and_total_collateral_and_liability_info(&mut self) {
        let mut tx = CalculateMarginRequirementAndTotalCollateralAndLiabilityInfoTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_user(Default::default() /* & User */);
        tx.set_perp_market_map(Default::default() /* & PerpMarketMap */);
        tx.set_spot_market_map(Default::default() /* & SpotMarketMap */);
        tx.set_oracle_map(Default::default() /* & mut OracleMap */);
        self.trident.execute_transaction(&mut tx, Some("calculate_margin_requirement_and_total_collateral_and_liability_info"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_pyth_pull_oracle'
    #[flow]
    fn flow_handle_update_pyth_pull_oracle(&mut self) {
        let mut tx = HandleUpdatePythPullOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_feed_id(Default::default() /* [u8 ; 32] */);
        tx.set_params(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_pyth_pull_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_post_pyth_pull_oracle_update_atomic'
    #[flow]
    fn flow_handle_post_pyth_pull_oracle_update_atomic(&mut self) {
        let mut tx = HandlePostPythPullOracleUpdateAtomicTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_feed_id(Default::default() /* [u8 ; 32] */);
        tx.set_params(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_post_pyth_pull_oracle_update_atomic"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_post_multi_pyth_pull_oracle_updates_atomic'
    #[flow]
    fn flow_handle_post_multi_pyth_pull_oracle_updates_atomic(&mut self) {
        let mut tx = HandlePostMultiPythPullOracleUpdatesAtomicTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_post_multi_pyth_pull_oracle_updates_atomic"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_pool_id'
    #[flow]
    fn flow_handle_update_spot_market_pool_id(&mut self) {
        let mut tx = HandleUpdateSpotMarketPoolIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_pool_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_serum_fulfillment_config_status'
    #[flow]
    fn flow_handle_update_serum_fulfillment_config_status(&mut self) {
        let mut tx = HandleUpdateSerumFulfillmentConfigStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* SpotFulfillmentConfigStatus */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_serum_fulfillment_config_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_delete_serum_fulfillment_config'
    #[flow]
    fn flow_handle_delete_serum_fulfillment_config(&mut self) {
        let mut tx = HandleDeleteSerumFulfillmentConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_delete_serum_fulfillment_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_serum_vault'
    #[flow]
    fn flow_handle_update_serum_vault(&mut self) {
        let mut tx = HandleUpdateSerumVaultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_serum_vault"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_openbook_v2_fulfillment_config_status'
    #[flow]
    fn flow_handle_update_openbook_v2_fulfillment_config_status(&mut self) {
        let mut tx = HandleUpdateOpenbookV2FulfillmentConfigStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* SpotFulfillmentConfigStatus */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_openbook_v2_fulfillment_config_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_delete_openbook_v2_fulfillment_config'
    #[flow]
    fn flow_handle_delete_openbook_v2_fulfillment_config(&mut self) {
        let mut tx = HandleDeleteOpenbookV2FulfillmentConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_delete_openbook_v2_fulfillment_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_phoenix_fulfillment_config_status'
    #[flow]
    fn flow_handle_update_phoenix_fulfillment_config_status(&mut self) {
        let mut tx = HandleUpdatePhoenixFulfillmentConfigStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* SpotFulfillmentConfigStatus */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_phoenix_fulfillment_config_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_add_market_to_amm_cache'
    #[flow]
    fn flow_handle_add_market_to_amm_cache(&mut self) {
        let mut tx = HandleAddMarketToAmmCacheTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_add_market_to_amm_cache"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_delete_amm_cache'
    #[flow]
    fn flow_handle_delete_amm_cache(&mut self) {
        let mut tx = HandleDeleteAmmCacheTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_delete_amm_cache"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_oracle'
    #[flow]
    fn flow_handle_update_spot_market_oracle(&mut self) {
        let mut tx = HandleUpdateSpotMarketOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle(Pubkey::new_unique());
        tx.set_oracle_source(Default::default() /* OracleSource */);
        tx.set_skip_invariant_check(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_expiry'
    #[flow]
    fn flow_handle_update_spot_market_expiry(&mut self) {
        let mut tx = HandleUpdateSpotMarketExpiryTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_expiry_ts(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_expiry"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_expiry'
    #[flow]
    fn flow_handle_update_perp_market_expiry(&mut self) {
        let mut tx = HandleUpdatePerpMarketExpiryTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_expiry_ts(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_expiry"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_move_amm_price'
    #[flow]
    fn flow_handle_move_amm_price(&mut self) {
        let mut tx = HandleMoveAmmPriceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_base_asset_reserve(rand::random::<u128>());
        tx.set_quote_asset_reserve(rand::random::<u128>());
        tx.set_sqrt_k(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_move_amm_price"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_recenter_perp_market_amm'
    #[flow]
    fn flow_handle_recenter_perp_market_amm(&mut self) {
        let mut tx = HandleRecenterPerpMarketAmmTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_peg_multiplier(rand::random::<u128>());
        tx.set_sqrt_k(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_recenter_perp_market_amm"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_recenter_perp_market_amm_crank'
    #[flow]
    fn flow_handle_recenter_perp_market_amm_crank(&mut self) {
        let mut tx = HandleRecenterPerpMarketAmmCrankTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_depth(Default::default() /* Option < u128 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_recenter_perp_market_amm_crank"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_amm_summary_stats'
    #[flow]
    fn flow_handle_update_perp_market_amm_summary_stats(&mut self) {
        let mut tx = HandleUpdatePerpMarketAmmSummaryStatsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* UpdatePerpMarketSummaryStatsParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_amm_summary_stats"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_settle_expired_market_pools_to_revenue_pool'
    #[flow]
    fn flow_handle_settle_expired_market_pools_to_revenue_pool(&mut self) {
        let mut tx = HandleSettleExpiredMarketPoolsToRevenuePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_settle_expired_market_pools_to_revenue_pool"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit_into_perp_market_fee_pool'
    #[flow]
    fn flow_handle_deposit_into_perp_market_fee_pool(&mut self) {
        let mut tx = HandleDepositIntoPerpMarketFeePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit_into_perp_market_fee_pool"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_pnl_pool'
    #[flow]
    fn flow_handle_update_perp_market_pnl_pool(&mut self) {
        let mut tx = HandleUpdatePerpMarketPnlPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_pnl_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit_into_spot_market_vault'
    #[flow]
    fn flow_handle_deposit_into_spot_market_vault(&mut self) {
        let mut tx = HandleDepositIntoSpotMarketVaultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit_into_spot_market_vault"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_repeg_amm_curve'
    #[flow]
    fn flow_handle_repeg_amm_curve(&mut self) {
        let mut tx = HandleRepegAmmCurveTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_peg_candidate(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_repeg_amm_curve"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_amm_oracle_twap'
    #[flow]
    fn flow_handle_update_amm_oracle_twap(&mut self) {
        let mut tx = HandleUpdateAmmOracleTwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_amm_oracle_twap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_k'
    #[flow]
    fn flow_handle_update_k(&mut self) {
        let mut tx = HandleUpdateKTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_sqrt_k(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_k"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_reset_amm_oracle_twap'
    #[flow]
    fn flow_handle_reset_amm_oracle_twap(&mut self) {
        let mut tx = HandleResetAmmOracleTwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_reset_amm_oracle_twap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_margin_ratio'
    #[flow]
    fn flow_handle_update_perp_market_margin_ratio(&mut self) {
        let mut tx = HandleUpdatePerpMarketMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_margin_ratio_initial(rand::random::<u32>());
        tx.set_margin_ratio_maintenance(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_high_leverage_margin_ratio'
    #[flow]
    fn flow_handle_update_perp_market_high_leverage_margin_ratio(&mut self) {
        let mut tx = HandleUpdatePerpMarketHighLeverageMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_margin_ratio_initial(rand::random::<u16>());
        tx.set_margin_ratio_maintenance(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_high_leverage_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_funding_period'
    #[flow]
    fn flow_handle_update_perp_market_funding_period(&mut self) {
        let mut tx = HandleUpdatePerpMarketFundingPeriodTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_funding_period(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_funding_period"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_max_imbalances'
    #[flow]
    fn flow_handle_update_perp_market_max_imbalances(&mut self) {
        let mut tx = HandleUpdatePerpMarketMaxImbalancesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_unrealized_max_imbalance(rand::random::<u64>());
        tx.set_max_revenue_withdraw_per_period(rand::random::<u64>());
        tx.set_quote_max_insurance(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_max_imbalances"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_name'
    #[flow]
    fn flow_handle_update_perp_market_name(&mut self) {
        let mut tx = HandleUpdatePerpMarketNameTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_name(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_name"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_name'
    #[flow]
    fn flow_handle_update_spot_market_name(&mut self) {
        let mut tx = HandleUpdateSpotMarketNameTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_name(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_name"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_liquidation_fee'
    #[flow]
    fn flow_handle_update_perp_liquidation_fee(&mut self) {
        let mut tx = HandleUpdatePerpLiquidationFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidator_fee(rand::random::<u32>());
        tx.set_if_liquidation_fee(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_liquidation_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_lp_pool_id'
    #[flow]
    fn flow_handle_update_perp_lp_pool_id(&mut self) {
        let mut tx = HandleUpdatePerpLpPoolIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_pool_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_lp_pool_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_insurance_fund_unstaking_period'
    #[flow]
    fn flow_handle_update_insurance_fund_unstaking_period(&mut self) {
        let mut tx = HandleUpdateInsuranceFundUnstakingPeriodTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_insurance_fund_unstaking_period(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_insurance_fund_unstaking_period"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_liquidation_fee'
    #[flow]
    fn flow_handle_update_spot_market_liquidation_fee(&mut self) {
        let mut tx = HandleUpdateSpotMarketLiquidationFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidator_fee(rand::random::<u32>());
        tx.set_if_liquidation_fee(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_liquidation_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_withdraw_guard_threshold'
    #[flow]
    fn flow_handle_update_withdraw_guard_threshold(&mut self) {
        let mut tx = HandleUpdateWithdrawGuardThresholdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_withdraw_guard_threshold(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_withdraw_guard_threshold"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_if_factor'
    #[flow]
    fn flow_handle_update_spot_market_if_factor(&mut self) {
        let mut tx = HandleUpdateSpotMarketIfFactorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_user_if_factor(rand::random::<u32>());
        tx.set_total_if_factor(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_if_factor"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_revenue_settle_period'
    #[flow]
    fn flow_handle_update_spot_market_revenue_settle_period(&mut self) {
        let mut tx = HandleUpdateSpotMarketRevenueSettlePeriodTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_revenue_settle_period(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_revenue_settle_period"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_status'
    #[flow]
    fn flow_handle_update_spot_market_status(&mut self) {
        let mut tx = HandleUpdateSpotMarketStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* MarketStatus */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_paused_operations'
    #[flow]
    fn flow_handle_update_spot_market_paused_operations(&mut self) {
        let mut tx = HandleUpdateSpotMarketPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_asset_tier'
    #[flow]
    fn flow_handle_update_spot_market_asset_tier(&mut self) {
        let mut tx = HandleUpdateSpotMarketAssetTierTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_tier(Default::default() /* AssetTier */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_asset_tier"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_margin_weights'
    #[flow]
    fn flow_handle_update_spot_market_margin_weights(&mut self) {
        let mut tx = HandleUpdateSpotMarketMarginWeightsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_initial_asset_weight(rand::random::<u32>());
        tx.set_maintenance_asset_weight(rand::random::<u32>());
        tx.set_initial_liability_weight(rand::random::<u32>());
        tx.set_maintenance_liability_weight(rand::random::<u32>());
        tx.set_imf_factor(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_margin_weights"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_borrow_rate'
    #[flow]
    fn flow_handle_update_spot_market_borrow_rate(&mut self) {
        let mut tx = HandleUpdateSpotMarketBorrowRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_optimal_utilization(rand::random::<u32>());
        tx.set_optimal_borrow_rate(rand::random::<u32>());
        tx.set_max_borrow_rate(rand::random::<u32>());
        tx.set_min_borrow_rate(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_borrow_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_max_token_deposits'
    #[flow]
    fn flow_handle_update_spot_market_max_token_deposits(&mut self) {
        let mut tx = HandleUpdateSpotMarketMaxTokenDepositsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_token_deposits(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_max_token_deposits"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_max_token_borrows'
    #[flow]
    fn flow_handle_update_spot_market_max_token_borrows(&mut self) {
        let mut tx = HandleUpdateSpotMarketMaxTokenBorrowsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_token_borrows_fraction(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_max_token_borrows"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_orders_enabled'
    #[flow]
    fn flow_handle_update_spot_market_orders_enabled(&mut self) {
        let mut tx = HandleUpdateSpotMarketOrdersEnabledTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_orders_enabled(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_orders_enabled"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_if_paused_operations'
    #[flow]
    fn flow_handle_update_spot_market_if_paused_operations(&mut self) {
        let mut tx = HandleUpdateSpotMarketIfPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_if_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_status'
    #[flow]
    fn flow_handle_update_perp_market_status(&mut self) {
        let mut tx = HandleUpdatePerpMarketStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_status(Default::default() /* MarketStatus */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_paused_operations'
    #[flow]
    fn flow_handle_update_perp_market_paused_operations(&mut self) {
        let mut tx = HandleUpdatePerpMarketPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_contract_tier'
    #[flow]
    fn flow_handle_update_perp_market_contract_tier(&mut self) {
        let mut tx = HandleUpdatePerpMarketContractTierTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_contract_tier(Default::default() /* ContractTier */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_contract_tier"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_imf_factor'
    #[flow]
    fn flow_handle_update_perp_market_imf_factor(&mut self) {
        let mut tx = HandleUpdatePerpMarketImfFactorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_imf_factor(rand::random::<u32>());
        tx.set_unrealized_pnl_imf_factor(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_imf_factor"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_unrealized_asset_weight'
    #[flow]
    fn flow_handle_update_perp_market_unrealized_asset_weight(&mut self) {
        let mut tx = HandleUpdatePerpMarketUnrealizedAssetWeightTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_unrealized_initial_asset_weight(rand::random::<u32>());
        tx.set_unrealized_maintenance_asset_weight(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_unrealized_asset_weight"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_concentration_coef'
    #[flow]
    fn flow_handle_update_perp_market_concentration_coef(&mut self) {
        let mut tx = HandleUpdatePerpMarketConcentrationCoefTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_concentration_scale(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_concentration_coef"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_curve_update_intensity'
    #[flow]
    fn flow_handle_update_perp_market_curve_update_intensity(&mut self) {
        let mut tx = HandleUpdatePerpMarketCurveUpdateIntensityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_curve_update_intensity(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_curve_update_intensity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_reference_price_offset_deadband_pct'
    #[flow]
    fn flow_handle_update_perp_market_reference_price_offset_deadband_pct(&mut self) {
        let mut tx = HandleUpdatePerpMarketReferencePriceOffsetDeadbandPctTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_reference_price_offset_deadband_pct(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_reference_price_offset_deadband_pct"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_fee_structure'
    #[flow]
    fn flow_handle_update_perp_fee_structure(&mut self) {
        let mut tx = HandleUpdatePerpFeeStructureTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_structure(Default::default() /* FeeStructure */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_fee_structure"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_fee_structure'
    #[flow]
    fn flow_handle_update_spot_fee_structure(&mut self) {
        let mut tx = HandleUpdateSpotFeeStructureTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_structure(Default::default() /* FeeStructure */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_fee_structure"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_liquidation_duration'
    #[flow]
    fn flow_handle_update_liquidation_duration(&mut self) {
        let mut tx = HandleUpdateLiquidationDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidation_duration(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_liquidation_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_liquidation_margin_buffer_ratio'
    #[flow]
    fn flow_handle_update_liquidation_margin_buffer_ratio(&mut self) {
        let mut tx = HandleUpdateLiquidationMarginBufferRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_liquidation_margin_buffer_ratio(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_liquidation_margin_buffer_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_oracle_guard_rails'
    #[flow]
    fn flow_handle_update_oracle_guard_rails(&mut self) {
        let mut tx = HandleUpdateOracleGuardRailsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle_guard_rails(Default::default() /* OracleGuardRails */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_oracle_guard_rails"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_state_settlement_duration'
    #[flow]
    fn flow_handle_update_state_settlement_duration(&mut self) {
        let mut tx = HandleUpdateStateSettlementDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_settlement_duration(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_state_settlement_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_state_max_number_of_sub_accounts'
    #[flow]
    fn flow_handle_update_state_max_number_of_sub_accounts(&mut self) {
        let mut tx = HandleUpdateStateMaxNumberOfSubAccountsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_number_of_sub_accounts(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_state_max_number_of_sub_accounts"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_oracle'
    #[flow]
    fn flow_handle_update_perp_market_oracle(&mut self) {
        let mut tx = HandleUpdatePerpMarketOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle(Pubkey::new_unique());
        tx.set_oracle_source(Default::default() /* OracleSource */);
        tx.set_skip_invariant_check(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_base_spread'
    #[flow]
    fn flow_handle_update_perp_market_base_spread(&mut self) {
        let mut tx = HandleUpdatePerpMarketBaseSpreadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_base_spread(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_base_spread"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_amm_jit_intensity'
    #[flow]
    fn flow_handle_update_amm_jit_intensity(&mut self) {
        let mut tx = HandleUpdateAmmJitIntensityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amm_jit_intensity(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_amm_jit_intensity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_max_spread'
    #[flow]
    fn flow_handle_update_perp_market_max_spread(&mut self) {
        let mut tx = HandleUpdatePerpMarketMaxSpreadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_spread(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_max_spread"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_step_size_and_tick_size'
    #[flow]
    fn flow_handle_update_perp_market_step_size_and_tick_size(&mut self) {
        let mut tx = HandleUpdatePerpMarketStepSizeAndTickSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_step_size(rand::random::<u64>());
        tx.set_tick_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_step_size_and_tick_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_min_order_size'
    #[flow]
    fn flow_handle_update_perp_market_min_order_size(&mut self) {
        let mut tx = HandleUpdatePerpMarketMinOrderSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_min_order_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_step_size_and_tick_size'
    #[flow]
    fn flow_handle_update_spot_market_step_size_and_tick_size(&mut self) {
        let mut tx = HandleUpdateSpotMarketStepSizeAndTickSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_step_size(rand::random::<u64>());
        tx.set_tick_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_step_size_and_tick_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_min_order_size'
    #[flow]
    fn flow_handle_update_spot_market_min_order_size(&mut self) {
        let mut tx = HandleUpdateSpotMarketMinOrderSizeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_size(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_min_order_size"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_max_slippage_ratio'
    #[flow]
    fn flow_handle_update_perp_market_max_slippage_ratio(&mut self) {
        let mut tx = HandleUpdatePerpMarketMaxSlippageRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_slippage_ratio(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_max_slippage_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_max_fill_reserve_fraction'
    #[flow]
    fn flow_handle_update_perp_market_max_fill_reserve_fraction(&mut self) {
        let mut tx = HandleUpdatePerpMarketMaxFillReserveFractionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_fill_reserve_fraction(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_max_fill_reserve_fraction"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_max_open_interest'
    #[flow]
    fn flow_handle_update_perp_market_max_open_interest(&mut self) {
        let mut tx = HandleUpdatePerpMarketMaxOpenInterestTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_open_interest(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_max_open_interest"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_fee_adjustment'
    #[flow]
    fn flow_handle_update_perp_market_fee_adjustment(&mut self) {
        let mut tx = HandleUpdatePerpMarketFeeAdjustmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_adjustment(rand::random::<i16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_fee_adjustment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_number_of_users'
    #[flow]
    fn flow_handle_update_perp_market_number_of_users(&mut self) {
        let mut tx = HandleUpdatePerpMarketNumberOfUsersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_number_of_users(Default::default() /* Option < u32 > */);
        tx.set_number_of_users_with_base(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_number_of_users"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_fuel'
    #[flow]
    fn flow_handle_update_perp_market_fuel(&mut self) {
        let mut tx = HandleUpdatePerpMarketFuelTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fuel_boost_taker(Default::default() /* Option < u8 > */);
        tx.set_fuel_boost_maker(Default::default() /* Option < u8 > */);
        tx.set_fuel_boost_position(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_fuel"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_protected_maker_params'
    #[flow]
    fn flow_handle_update_perp_market_protected_maker_params(&mut self) {
        let mut tx = HandleUpdatePerpMarketProtectedMakerParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_protected_maker_limit_price_divisor(Default::default() /* Option < u8 > */);
        tx.set_protected_maker_dynamic_divisor(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_protected_maker_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_lp_pool_paused_operations'
    #[flow]
    fn flow_handle_update_perp_market_lp_pool_paused_operations(&mut self) {
        let mut tx = HandleUpdatePerpMarketLpPoolPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_lp_pool_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_oracle_low_risk_slot_delay_override'
    #[flow]
    fn flow_handle_update_perp_market_oracle_low_risk_slot_delay_override(&mut self) {
        let mut tx = HandleUpdatePerpMarketOracleLowRiskSlotDelayOverrideTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle_low_risk_slot_delay_override(rand::random::<i8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_oracle_low_risk_slot_delay_override"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_amm_spread_adjustment'
    #[flow]
    fn flow_handle_update_perp_market_amm_spread_adjustment(&mut self) {
        let mut tx = HandleUpdatePerpMarketAmmSpreadAdjustmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amm_spread_adjustment(rand::random::<i8>());
        tx.set_amm_inventory_spread_adjustment(rand::random::<i8>());
        tx.set_reference_price_offset(rand::random::<i32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_amm_spread_adjustment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_oracle_slot_delay_override'
    #[flow]
    fn flow_handle_update_perp_market_oracle_slot_delay_override(&mut self) {
        let mut tx = HandleUpdatePerpMarketOracleSlotDelayOverrideTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_oracle_slot_delay_override(rand::random::<i8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_oracle_slot_delay_override"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_fee_adjustment'
    #[flow]
    fn flow_handle_update_spot_market_fee_adjustment(&mut self) {
        let mut tx = HandleUpdateSpotMarketFeeAdjustmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fee_adjustment(rand::random::<i16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_fee_adjustment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_fuel'
    #[flow]
    fn flow_handle_update_spot_market_fuel(&mut self) {
        let mut tx = HandleUpdateSpotMarketFuelTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fuel_boost_deposits(Default::default() /* Option < u8 > */);
        tx.set_fuel_boost_borrows(Default::default() /* Option < u8 > */);
        tx.set_fuel_boost_taker(Default::default() /* Option < u8 > */);
        tx.set_fuel_boost_maker(Default::default() /* Option < u8 > */);
        tx.set_fuel_boost_insurance(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_fuel"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_admin'
    #[flow]
    fn flow_handle_update_admin(&mut self) {
        let mut tx = HandleUpdateAdminTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_admin(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("handle_update_admin"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_whitelist_mint'
    #[flow]
    fn flow_handle_update_whitelist_mint(&mut self) {
        let mut tx = HandleUpdateWhitelistMintTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_whitelist_mint(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("handle_update_whitelist_mint"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_discount_mint'
    #[flow]
    fn flow_handle_update_discount_mint(&mut self) {
        let mut tx = HandleUpdateDiscountMintTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_discount_mint(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("handle_update_discount_mint"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_exchange_status'
    #[flow]
    fn flow_handle_update_exchange_status(&mut self) {
        let mut tx = HandleUpdateExchangeStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_exchange_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_exchange_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_auction_duration'
    #[flow]
    fn flow_handle_update_perp_auction_duration(&mut self) {
        let mut tx = HandleUpdatePerpAuctionDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_min_perp_auction_duration(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_auction_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_auction_duration'
    #[flow]
    fn flow_handle_update_spot_auction_duration(&mut self) {
        let mut tx = HandleUpdateSpotAuctionDurationTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_default_spot_auction_duration(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_auction_duration"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_admin_update_user_stats_paused_operations'
    #[flow]
    fn flow_handle_admin_update_user_stats_paused_operations(&mut self) {
        let mut tx = HandleAdminUpdateUserStatsPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_admin_update_user_stats_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_protocol_if_shares_transfer_config'
    #[flow]
    fn flow_handle_update_protocol_if_shares_transfer_config(&mut self) {
        let mut tx = HandleUpdateProtocolIfSharesTransferConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_whitelisted_signers(Pubkey::new_unique());
        tx.set_max_transfer_per_epoch(Default::default() /* Option < u128 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_protocol_if_shares_transfer_config"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_prelaunch_oracle_params'
    #[flow]
    fn flow_handle_update_prelaunch_oracle_params(&mut self) {
        let mut tx = HandleUpdatePrelaunchOracleParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* PrelaunchOracleParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_prelaunch_oracle_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_delete_prelaunch_oracle'
    #[flow]
    fn flow_handle_delete_prelaunch_oracle(&mut self) {
        let mut tx = HandleDeletePrelaunchOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__perp_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_delete_prelaunch_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_settle_expired_market'
    #[flow]
    fn flow_handle_settle_expired_market(&mut self) {
        let mut tx = HandleSettleExpiredMarketTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_settle_expired_market"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_high_leverage_mode_config'
    #[flow]
    fn flow_handle_update_high_leverage_mode_config(&mut self) {
        let mut tx = HandleUpdateHighLeverageModeConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_users(rand::random::<u32>());
        tx.set_reduce_only(rand::random::<bool>());
        tx.set_current_users(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_high_leverage_mode_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_protected_maker_mode_config'
    #[flow]
    fn flow_handle_update_protected_maker_mode_config(&mut self) {
        let mut tx = HandleUpdateProtectedMakerModeConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_users(rand::random::<u32>());
        tx.set_reduce_only(rand::random::<bool>());
        tx.set_current_users(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_protected_maker_mode_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_admin_deposit'
    #[flow]
    fn flow_handle_admin_deposit(&mut self) {
        let mut tx = HandleAdminDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_admin_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_if_rebalance_config'
    #[flow]
    fn flow_handle_update_if_rebalance_config(&mut self) {
        let mut tx = HandleUpdateIfRebalanceConfigTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* IfRebalanceConfigParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_if_rebalance_config"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_zero_mm_oracle_fields'
    #[flow]
    fn flow_handle_zero_mm_oracle_fields(&mut self) {
        let mut tx = HandleZeroMmOracleFieldsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_zero_mm_oracle_fields"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_feature_bit_flags_mm_oracle'
    #[flow]
    fn flow_handle_update_feature_bit_flags_mm_oracle(&mut self) {
        let mut tx = HandleUpdateFeatureBitFlagsMmOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_feature_bit_flags_mm_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_feature_bit_flags_median_trigger_price'
    #[flow]
    fn flow_handle_update_feature_bit_flags_median_trigger_price(&mut self) {
        let mut tx = HandleUpdateFeatureBitFlagsMedianTriggerPriceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_feature_bit_flags_median_trigger_price"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_delegate_user_gov_token_insurance_stake'
    #[flow]
    fn flow_handle_update_delegate_user_gov_token_insurance_stake(&mut self) {
        let mut tx = HandleUpdateDelegateUserGovTokenInsuranceStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_delegate_user_gov_token_insurance_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_feature_bit_flags_builder_codes'
    #[flow]
    fn flow_handle_update_feature_bit_flags_builder_codes(&mut self) {
        let mut tx = HandleUpdateFeatureBitFlagsBuilderCodesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_feature_bit_flags_builder_codes"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_feature_bit_flags_builder_referral'
    #[flow]
    fn flow_handle_update_feature_bit_flags_builder_referral(&mut self) {
        let mut tx = HandleUpdateFeatureBitFlagsBuilderReferralTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_feature_bit_flags_builder_referral"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_feature_bit_flags_settle_lp_pool'
    #[flow]
    fn flow_handle_update_feature_bit_flags_settle_lp_pool(&mut self) {
        let mut tx = HandleUpdateFeatureBitFlagsSettleLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_feature_bit_flags_settle_lp_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_feature_bit_flags_swap_lp_pool'
    #[flow]
    fn flow_handle_update_feature_bit_flags_swap_lp_pool(&mut self) {
        let mut tx = HandleUpdateFeatureBitFlagsSwapLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_feature_bit_flags_swap_lp_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_feature_bit_flags_mint_redeem_lp_pool'
    #[flow]
    fn flow_handle_update_feature_bit_flags_mint_redeem_lp_pool(&mut self) {
        let mut tx = HandleUpdateFeatureBitFlagsMintRedeemLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_enable(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_feature_bit_flags_mint_redeem_lp_pool"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_pyth_lazer_oracle'
    #[flow]
    fn flow_handle_update_pyth_lazer_oracle(&mut self) {
        let mut tx = HandleUpdatePythLazerOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pyth_message(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_pyth_lazer_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_constituent_target_base'
    #[flow]
    fn flow_handle_update_constituent_target_base(&mut self) {
        let mut tx = HandleUpdateConstituentTargetBaseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_constituent_target_base"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_lp_pool_aum'
    #[flow]
    fn flow_handle_update_lp_pool_aum(&mut self) {
        let mut tx = HandleUpdateLpPoolAumTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_lp_pool_aum"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_lp_pool_swap'
    #[flow]
    fn flow_handle_lp_pool_swap(&mut self) {
        let mut tx = HandleLpPoolSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u64>());
        tx.set_min_out_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_lp_pool_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_view_lp_pool_swap_fees'
    #[flow]
    fn flow_handle_view_lp_pool_swap_fees(&mut self) {
        let mut tx = HandleViewLpPoolSwapFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u64>());
        tx.set_in_target_weight(rand::random::<i64>());
        tx.set_out_target_weight(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_view_lp_pool_swap_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_lp_pool_add_liquidity'
    #[flow]
    fn flow_handle_lp_pool_add_liquidity(&mut self) {
        let mut tx = HandleLpPoolAddLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u128>());
        tx.set_min_mint_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_lp_pool_add_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_view_lp_pool_add_liquidity_fees'
    #[flow]
    fn flow_handle_view_lp_pool_add_liquidity_fees(&mut self) {
        let mut tx = HandleViewLpPoolAddLiquidityFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_in_amount(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_view_lp_pool_add_liquidity_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_lp_pool_remove_liquidity'
    #[flow]
    fn flow_handle_lp_pool_remove_liquidity(&mut self) {
        let mut tx = HandleLpPoolRemoveLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_lp_to_burn(rand::random::<u64>());
        tx.set_min_amount_out(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_lp_pool_remove_liquidity"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_view_lp_pool_remove_liquidity_fees'
    #[flow]
    fn flow_handle_view_lp_pool_remove_liquidity_fees(&mut self) {
        let mut tx = HandleViewLpPoolRemoveLiquidityFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_lp_to_burn(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_view_lp_pool_remove_liquidity_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_constituent_oracle_info'
    #[flow]
    fn flow_handle_update_constituent_oracle_info(&mut self) {
        let mut tx = HandleUpdateConstituentOracleInfoTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_constituent_oracle_info"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit_to_program_vault'
    #[flow]
    fn flow_handle_deposit_to_program_vault(&mut self) {
        let mut tx = HandleDepositToProgramVaultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit_to_program_vault"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_withdraw_from_program_vault'
    #[flow]
    fn flow_handle_withdraw_from_program_vault(&mut self) {
        let mut tx = HandleWithdrawFromProgramVaultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_withdraw_from_program_vault"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_add_insurance_fund_stake'
    #[flow]
    fn flow_handle_add_insurance_fund_stake(&mut self) {
        let mut tx = HandleAddInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_add_insurance_fund_stake"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_request_remove_insurance_fund_stake'
    #[flow]
    fn flow_handle_request_remove_insurance_fund_stake(&mut self) {
        let mut tx = HandleRequestRemoveInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_request_remove_insurance_fund_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_cancel_request_remove_insurance_fund_stake'
    #[flow]
    fn flow_handle_cancel_request_remove_insurance_fund_stake(&mut self) {
        let mut tx = HandleCancelRequestRemoveInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_cancel_request_remove_insurance_fund_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_remove_insurance_fund_stake'
    #[flow]
    fn flow_handle_remove_insurance_fund_stake(&mut self) {
        let mut tx = HandleRemoveInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_remove_insurance_fund_stake"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_transfer_protocol_if_shares'
    #[flow]
    fn flow_handle_transfer_protocol_if_shares(&mut self) {
        let mut tx = HandleTransferProtocolIfSharesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_shares(rand::random::<u128>());
        self.trident.execute_transaction(&mut tx, Some("handle_transfer_protocol_if_shares"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_begin_insurance_fund_swap'
    #[flow]
    fn flow_handle_begin_insurance_fund_swap(&mut self) {
        let mut tx = HandleBeginInsuranceFundSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_amount_in(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_begin_insurance_fund_swap"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_end_insurance_fund_swap'
    #[flow]
    fn flow_handle_end_insurance_fund_swap(&mut self) {
        let mut tx = HandleEndInsuranceFundSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_end_insurance_fund_swap"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_transfer_protocol_if_shares_to_revenue_pool'
    #[flow]
    fn flow_handle_transfer_protocol_if_shares_to_revenue_pool(&mut self) {
        let mut tx = HandleTransferProtocolIfSharesToRevenuePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_transfer_protocol_if_shares_to_revenue_pool"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit_into_insurance_fund_stake'
    #[flow]
    fn flow_handle_deposit_into_insurance_fund_stake(&mut self) {
        let mut tx = HandleDepositIntoInsuranceFundStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit_into_insurance_fund_stake"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_resize_signed_msg_user_orders'
    #[flow]
    fn flow_handle_resize_signed_msg_user_orders(&mut self) {
        let mut tx = HandleResizeSignedMsgUserOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_num_orders(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_resize_signed_msg_user_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_change_signed_msg_ws_delegate_status'
    #[flow]
    fn flow_handle_change_signed_msg_ws_delegate_status(&mut self) {
        let mut tx = HandleChangeSignedMsgWsDelegateStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_delegate(Pubkey::new_unique());
        tx.set_add(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_change_signed_msg_ws_delegate_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_sweep_fuel'
    #[flow]
    fn flow_handle_sweep_fuel(&mut self) {
        let mut tx = HandleSweepFuelTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_sweep_fuel"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_reset_fuel_season'
    #[flow]
    fn flow_handle_reset_fuel_season(&mut self) {
        let mut tx = HandleResetFuelSeasonTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_reset_fuel_season"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_migrate_referrer'
    #[flow]
    fn flow_handle_migrate_referrer(&mut self) {
        let mut tx = HandleMigrateReferrerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_migrate_referrer"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_resize_revenue_share_escrow_orders'
    #[flow]
    fn flow_handle_resize_revenue_share_escrow_orders(&mut self) {
        let mut tx = HandleResizeRevenueShareEscrowOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_num_orders(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_resize_revenue_share_escrow_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_change_approved_builder'
    #[flow]
    fn flow_handle_change_approved_builder(&mut self) {
        let mut tx = HandleChangeApprovedBuilderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_builder(Pubkey::new_unique());
        tx.set_max_fee_tenth_bps(rand::random::<u16>());
        tx.set_add(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_change_approved_builder"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit'
    #[flow]
    fn flow_handle_deposit(&mut self) {
        let mut tx = HandleDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        tx.set_reduce_only(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_withdraw'
    #[flow]
    fn flow_handle_withdraw(&mut self) {
        let mut tx = HandleWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        tx.set_reduce_only(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_withdraw"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_transfer_deposit'
    #[flow]
    fn flow_handle_transfer_deposit(&mut self) {
        let mut tx = HandleTransferDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_transfer_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_transfer_pools'
    #[flow]
    fn flow_handle_transfer_pools(&mut self) {
        let mut tx = HandleTransferPoolsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_deposit_from_market_index(rand::random::<u16>());
        tx.set_deposit_to_market_index(rand::random::<u16>());
        tx.set_borrow_from_market_index(rand::random::<u16>());
        tx.set_borrow_to_market_index(rand::random::<u16>());
        tx.set_deposit_amount(Default::default() /* Option < u64 > */);
        tx.set_borrow_amount(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_transfer_pools"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_transfer_perp_position'
    #[flow]
    fn flow_handle_transfer_perp_position(&mut self) {
        let mut tx = HandleTransferPerpPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_amount(Default::default() /* Option < i64 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_transfer_perp_position"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit_into_isolated_perp_position'
    #[flow]
    fn flow_handle_deposit_into_isolated_perp_position(&mut self) {
        let mut tx = HandleDepositIntoIsolatedPerpPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit_into_isolated_perp_position"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_transfer_isolated_perp_position_deposit'
    #[flow]
    fn flow_handle_transfer_isolated_perp_position_deposit(&mut self) {
        let mut tx = HandleTransferIsolatedPerpPositionDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_transfer_isolated_perp_position_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_withdraw_from_isolated_perp_position'
    #[flow]
    fn flow_handle_withdraw_from_isolated_perp_position(&mut self) {
        let mut tx = HandleWithdrawFromIsolatedPerpPositionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_withdraw_from_isolated_perp_position"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_perp_order'
    #[flow]
    fn flow_handle_place_perp_order(&mut self) {
        let mut tx = HandlePlacePerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_cancel_order'
    #[flow]
    fn flow_handle_cancel_order(&mut self) {
        let mut tx = HandleCancelOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_cancel_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_cancel_order_by_user_id'
    #[flow]
    fn flow_handle_cancel_order_by_user_id(&mut self) {
        let mut tx = HandleCancelOrderByUserIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_user_order_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_cancel_order_by_user_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_cancel_orders_by_ids'
    #[flow]
    fn flow_handle_cancel_orders_by_ids(&mut self) {
        let mut tx = HandleCancelOrdersByIdsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_ids(Default::default() /* Vec < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_cancel_orders_by_ids"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_cancel_orders'
    #[flow]
    fn flow_handle_cancel_orders(&mut self) {
        let mut tx = HandleCancelOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_type(Default::default() /* Option < MarketType > */);
        tx.set_market_index(Default::default() /* Option < u16 > */);
        tx.set_direction(Default::default() /* Option < PositionDirection > */);
        self.trident.execute_transaction(&mut tx, Some("handle_cancel_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_modify_order'
    #[flow]
    fn flow_handle_modify_order(&mut self) {
        let mut tx = HandleModifyOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        tx.set_modify_order_params(Default::default() /* ModifyOrderParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_modify_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_modify_order_by_user_order_id'
    #[flow]
    fn flow_handle_modify_order_by_user_order_id(&mut self) {
        let mut tx = HandleModifyOrderByUserOrderIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_user_order_id(rand::random::<u8>());
        tx.set_modify_order_params(Default::default() /* ModifyOrderParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_modify_order_by_user_order_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_orders'
    #[flow]
    fn flow_handle_place_orders(&mut self) {
        let mut tx = HandlePlaceOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* Vec < OrderParams > */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_scale_orders'
    #[flow]
    fn flow_handle_place_scale_orders(&mut self) {
        let mut tx = HandlePlaceScaleOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* ScaleOrderParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_scale_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_orders'
    #[flow]
    fn flow_place_orders(&mut self) {
        let mut tx = PlaceOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_input(Default::default() /* PlaceOrdersInput */);
        self.trident.execute_transaction(&mut tx, Some("place_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_and_take_perp_order'
    #[flow]
    fn flow_handle_place_and_take_perp_order(&mut self) {
        let mut tx = HandlePlaceAndTakePerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_optional_params(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_and_take_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_and_make_perp_order'
    #[flow]
    fn flow_handle_place_and_make_perp_order(&mut self) {
        let mut tx = HandlePlaceAndMakePerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_taker_order_id(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_place_and_make_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_and_make_signed_msg_perp_order'
    #[flow]
    fn flow_handle_place_and_make_signed_msg_perp_order(&mut self) {
        let mut tx = HandlePlaceAndMakeSignedMsgPerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_signed_msg_order_uuid(Default::default() /* [u8 ; 8] */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_and_make_signed_msg_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_spot_order'
    #[flow]
    fn flow_handle_place_spot_order(&mut self) {
        let mut tx = HandlePlaceSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_and_take_spot_order'
    #[flow]
    fn flow_handle_place_and_take_spot_order(&mut self) {
        let mut tx = HandlePlaceAndTakeSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_fulfillment_type(Default::default() /* SpotFulfillmentType */);
        tx.set__maker_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_and_take_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_and_make_spot_order'
    #[flow]
    fn flow_handle_place_and_make_spot_order(&mut self) {
        let mut tx = HandlePlaceAndMakeSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* OrderParams */);
        tx.set_taker_order_id(rand::random::<u32>());
        tx.set_fulfillment_type(Default::default() /* SpotFulfillmentType */);
        self.trident.execute_transaction(&mut tx, Some("handle_place_and_make_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_name'
    #[flow]
    fn flow_handle_update_user_name(&mut self) {
        let mut tx = HandleUpdateUserNameTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_name(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_name"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_custom_margin_ratio'
    #[flow]
    fn flow_handle_update_user_custom_margin_ratio(&mut self) {
        let mut tx = HandleUpdateUserCustomMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_margin_ratio(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_custom_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_perp_position_custom_margin_ratio'
    #[flow]
    fn flow_handle_update_user_perp_position_custom_margin_ratio(&mut self) {
        let mut tx = HandleUpdateUserPerpPositionCustomMarginRatioTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_margin_ratio(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_perp_position_custom_margin_ratio"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_margin_trading_enabled'
    #[flow]
    fn flow_handle_update_user_margin_trading_enabled(&mut self) {
        let mut tx = HandleUpdateUserMarginTradingEnabledTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_margin_trading_enabled(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_margin_trading_enabled"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_pool_id'
    #[flow]
    fn flow_handle_update_user_pool_id(&mut self) {
        let mut tx = HandleUpdateUserPoolIdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_pool_id(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_pool_id"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_delegate'
    #[flow]
    fn flow_handle_update_user_delegate(&mut self) {
        let mut tx = HandleUpdateUserDelegateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_delegate(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_delegate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_reduce_only'
    #[flow]
    fn flow_handle_update_user_reduce_only(&mut self) {
        let mut tx = HandleUpdateUserReduceOnlyTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_reduce_only(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_reduce_only"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_advanced_lp'
    #[flow]
    fn flow_handle_update_user_advanced_lp(&mut self) {
        let mut tx = HandleUpdateUserAdvancedLpTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_advanced_lp(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_advanced_lp"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_protected_maker_orders'
    #[flow]
    fn flow_handle_update_user_protected_maker_orders(&mut self) {
        let mut tx = HandleUpdateUserProtectedMakerOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        tx.set_protected_maker_orders(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_protected_maker_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_delete_user'
    #[flow]
    fn flow_handle_delete_user(&mut self) {
        let mut tx = HandleDeleteUserTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_delete_user"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_delete_signed_msg_user_orders'
    #[flow]
    fn flow_handle_delete_signed_msg_user_orders(&mut self) {
        let mut tx = HandleDeleteSignedMsgUserOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_delete_signed_msg_user_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_reclaim_rent'
    #[flow]
    fn flow_handle_reclaim_rent(&mut self) {
        let mut tx = HandleReclaimRentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_reclaim_rent"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit_into_spot_market_revenue_pool'
    #[flow]
    fn flow_handle_deposit_into_spot_market_revenue_pool(&mut self) {
        let mut tx = HandleDepositIntoSpotMarketRevenuePoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit_into_spot_market_revenue_pool"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_enable_user_high_leverage_mode'
    #[flow]
    fn flow_handle_enable_user_high_leverage_mode(&mut self) {
        let mut tx = HandleEnableUserHighLeverageModeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__sub_account_id(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_enable_user_high_leverage_mode"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_begin_swap'
    #[flow]
    fn flow_handle_begin_swap(&mut self) {
        let mut tx = HandleBeginSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_amount_in(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_begin_swap"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_end_swap'
    #[flow]
    fn flow_handle_end_swap(&mut self) {
        let mut tx = HandleEndSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        tx.set_reduce_only(Default::default() /* Option < SwapReduceOnly > */);
        self.trident.execute_transaction(&mut tx, Some("handle_end_swap"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_constituent_status'
    #[flow]
    fn flow_handle_update_constituent_status(&mut self) {
        let mut tx = HandleUpdateConstituentStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_constituent_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_constituent_paused_operations'
    #[flow]
    fn flow_handle_update_constituent_paused_operations(&mut self) {
        let mut tx = HandleUpdateConstituentPausedOperationsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused_operations(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_constituent_paused_operations"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_lp_pool_fee_transfer_scalar'
    #[flow]
    fn flow_handle_update_perp_market_lp_pool_fee_transfer_scalar(&mut self) {
        let mut tx = HandleUpdatePerpMarketLpPoolFeeTransferScalarTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_optional_lp_fee_transfer_scalar(Default::default() /* Option < u8 > */);
        tx.set_optional_lp_net_pnl_transfer_scalar(Default::default() /* Option < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_lp_pool_fee_transfer_scalar"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_constituent_params'
    #[flow]
    fn flow_handle_update_constituent_params(&mut self) {
        let mut tx = HandleUpdateConstituentParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_constituent_params(Default::default() /* ConstituentParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_constituent_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_lp_pool_params'
    #[flow]
    fn flow_handle_update_lp_pool_params(&mut self) {
        let mut tx = HandleUpdateLpPoolParamsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_pool_params(Default::default() /* LpPoolParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_lp_pool_params"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_amm_constituent_mapping_data'
    #[flow]
    fn flow_handle_update_amm_constituent_mapping_data(&mut self) {
        let mut tx = HandleUpdateAmmConstituentMappingDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amm_constituent_mapping_data(Default::default() /* Vec < AddAmmConstituentMappingDatum > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_amm_constituent_mapping_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_remove_amm_constituent_mapping_data'
    #[flow]
    fn flow_handle_remove_amm_constituent_mapping_data(&mut self) {
        let mut tx = HandleRemoveAmmConstituentMappingDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_constituent_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_remove_amm_constituent_mapping_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_add_amm_constituent_data'
    #[flow]
    fn flow_handle_add_amm_constituent_data(&mut self) {
        let mut tx = HandleAddAmmConstituentDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_init_amm_constituent_mapping_data(Default::default() /* Vec < AddAmmConstituentMappingDatum > */);
        self.trident.execute_transaction(&mut tx, Some("handle_add_amm_constituent_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_constituent_correlation_data'
    #[flow]
    fn flow_handle_update_constituent_correlation_data(&mut self) {
        let mut tx = HandleUpdateConstituentCorrelationDataTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_index1(rand::random::<u16>());
        tx.set_index2(rand::random::<u16>());
        tx.set_corr(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_constituent_correlation_data"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_begin_lp_swap'
    #[flow]
    fn flow_handle_begin_lp_swap(&mut self) {
        let mut tx = HandleBeginLpSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_in_market_index(rand::random::<u16>());
        tx.set_out_market_index(rand::random::<u16>());
        tx.set_amount_in(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_begin_lp_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_end_lp_swap'
    #[flow]
    fn flow_handle_end_lp_swap(&mut self) {
        let mut tx = HandleEndLpSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_end_lp_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_market_lp_pool_status'
    #[flow]
    fn flow_handle_update_perp_market_lp_pool_status(&mut self) {
        let mut tx = HandleUpdatePerpMarketLpPoolStatusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lp_status(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_market_lp_pool_status"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_override_amm_cache_info'
    #[flow]
    fn flow_handle_override_amm_cache_info(&mut self) {
        let mut tx = HandleOverrideAmmCacheInfoTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_override_params(Default::default() /* OverrideAmmCacheParams */);
        self.trident.execute_transaction(&mut tx, Some("handle_override_amm_cache_info"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_fill_perp_order'
    #[flow]
    fn flow_handle_fill_perp_order(&mut self) {
        let mut tx = HandleFillPerpOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_fill_perp_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'fill_order'
    #[flow]
    fn flow_fill_order(&mut self) {
        let mut tx = FillOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(rand::random::<u32>());
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("fill_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_revert_fill'
    #[flow]
    fn flow_handle_revert_fill(&mut self) {
        let mut tx = HandleRevertFillTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_revert_fill"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_fill_spot_order'
    #[flow]
    fn flow_handle_fill_spot_order(&mut self) {
        let mut tx = HandleFillSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(Default::default() /* Option < u32 > */);
        tx.set_fulfillment_type(Default::default() /* Option < SpotFulfillmentType > */);
        tx.set__maker_order_id(Default::default() /* Option < u32 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_fill_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'fill_spot_order'
    #[flow]
    fn flow_fill_spot_order(&mut self) {
        let mut tx = FillSpotOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(rand::random::<u32>());
        tx.set_market_index(rand::random::<u16>());
        tx.set_fulfillment_type(Default::default() /* SpotFulfillmentType */);
        self.trident.execute_transaction(&mut tx, Some("fill_spot_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_trigger_order'
    #[flow]
    fn flow_handle_trigger_order(&mut self) {
        let mut tx = HandleTriggerOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_order_id(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("handle_trigger_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_force_cancel_orders'
    #[flow]
    fn flow_handle_force_cancel_orders(&mut self) {
        let mut tx = HandleForceCancelOrdersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_force_cancel_orders"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_idle'
    #[flow]
    fn flow_handle_update_user_idle(&mut self) {
        let mut tx = HandleUpdateUserIdleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_idle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_log_user_balances'
    #[flow]
    fn flow_handle_log_user_balances(&mut self) {
        let mut tx = HandleLogUserBalancesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_log_user_balances"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_fuel_bonus'
    #[flow]
    fn flow_handle_update_user_fuel_bonus(&mut self) {
        let mut tx = HandleUpdateUserFuelBonusTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_fuel_bonus"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_stats_referrer_info'
    #[flow]
    fn flow_handle_update_user_stats_referrer_info(&mut self) {
        let mut tx = HandleUpdateUserStatsReferrerInfoTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_stats_referrer_info"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_open_orders_count'
    #[flow]
    fn flow_handle_update_user_open_orders_count(&mut self) {
        let mut tx = HandleUpdateUserOpenOrdersCountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_open_orders_count"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_place_signed_msg_taker_order'
    #[flow]
    fn flow_handle_place_signed_msg_taker_order(&mut self) {
        let mut tx = HandlePlaceSignedMsgTakerOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_signed_msg_order_params_message_bytes(Default::default() /* Vec < u8 > */);
        tx.set_is_delegate_signer(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_place_signed_msg_taker_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_settle_pnl'
    #[flow]
    fn flow_handle_settle_pnl(&mut self) {
        let mut tx = HandleSettlePnlTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_settle_pnl"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_settle_multiple_pnls'
    #[flow]
    fn flow_handle_settle_multiple_pnls(&mut self) {
        let mut tx = HandleSettleMultiplePnlsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_indexes(Default::default() /* Vec < u16 > */);
        tx.set_mode(Default::default() /* SettlePnlMode */);
        self.trident.execute_transaction(&mut tx, Some("handle_settle_multiple_pnls"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_settle_funding_payment'
    #[flow]
    fn flow_handle_settle_funding_payment(&mut self) {
        let mut tx = HandleSettleFundingPaymentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_settle_funding_payment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_liquidate_perp'
    #[flow]
    fn flow_handle_liquidate_perp(&mut self) {
        let mut tx = HandleLiquidatePerpTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        tx.set_liquidator_max_base_asset_amount(rand::random::<u64>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_liquidate_perp"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_liquidate_perp_with_fill'
    #[flow]
    fn flow_handle_liquidate_perp_with_fill(&mut self) {
        let mut tx = HandleLiquidatePerpWithFillTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_liquidate_perp_with_fill"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_liquidate_spot'
    #[flow]
    fn flow_handle_liquidate_spot(&mut self) {
        let mut tx = HandleLiquidateSpotTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_market_index(rand::random::<u16>());
        tx.set_liability_market_index(rand::random::<u16>());
        tx.set_liquidator_max_liability_transfer(rand::random::<u128>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_liquidate_spot"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_liquidate_spot_with_swap_begin'
    #[flow]
    fn flow_handle_liquidate_spot_with_swap_begin(&mut self) {
        let mut tx = HandleLiquidateSpotWithSwapBeginTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_market_index(rand::random::<u16>());
        tx.set_liability_market_index(rand::random::<u16>());
        tx.set_swap_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_liquidate_spot_with_swap_begin"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_liquidate_spot_with_swap_end'
    #[flow]
    fn flow_handle_liquidate_spot_with_swap_end(&mut self) {
        let mut tx = HandleLiquidateSpotWithSwapEndTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_asset_market_index(rand::random::<u16>());
        tx.set_liability_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_liquidate_spot_with_swap_end"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_liquidate_borrow_for_perp_pnl'
    #[flow]
    fn flow_handle_liquidate_borrow_for_perp_pnl(&mut self) {
        let mut tx = HandleLiquidateBorrowForPerpPnlTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_liquidator_max_liability_transfer(rand::random::<u128>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_liquidate_borrow_for_perp_pnl"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_liquidate_perp_pnl_for_deposit'
    #[flow]
    fn flow_handle_liquidate_perp_pnl_for_deposit(&mut self) {
        let mut tx = HandleLiquidatePerpPnlForDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_liquidator_max_pnl_transfer(rand::random::<u128>());
        tx.set_limit_price(Default::default() /* Option < u64 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_liquidate_perp_pnl_for_deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_set_user_status_to_being_liquidated'
    #[flow]
    fn flow_handle_set_user_status_to_being_liquidated(&mut self) {
        let mut tx = HandleSetUserStatusToBeingLiquidatedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_set_user_status_to_being_liquidated"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_resolve_perp_pnl_deficit'
    #[flow]
    fn flow_handle_resolve_perp_pnl_deficit(&mut self) {
        let mut tx = HandleResolvePerpPnlDeficitTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        tx.set_perp_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_resolve_perp_pnl_deficit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_resolve_perp_bankruptcy'
    #[flow]
    fn flow_handle_resolve_perp_bankruptcy(&mut self) {
        let mut tx = HandleResolvePerpBankruptcyTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_quote_spot_market_index(rand::random::<u16>());
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_resolve_perp_bankruptcy"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_resolve_spot_bankruptcy'
    #[flow]
    fn flow_handle_resolve_spot_bankruptcy(&mut self) {
        let mut tx = HandleResolveSpotBankruptcyTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_resolve_spot_bankruptcy"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_funding_rate'
    #[flow]
    fn flow_handle_update_funding_rate(&mut self) {
        let mut tx = HandleUpdateFundingRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_perp_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_update_funding_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_prelaunch_oracle'
    #[flow]
    fn flow_handle_update_prelaunch_oracle(&mut self) {
        let mut tx = HandleUpdatePrelaunchOracleTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_prelaunch_oracle"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_perp_bid_ask_twap'
    #[flow]
    fn flow_handle_update_perp_bid_ask_twap(&mut self) {
        let mut tx = HandleUpdatePerpBidAskTwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_perp_bid_ask_twap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_settle_revenue_to_insurance_fund'
    #[flow]
    fn flow_handle_settle_revenue_to_insurance_fund(&mut self) {
        let mut tx = HandleSettleRevenueToInsuranceFundTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_spot_market_index(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("handle_settle_revenue_to_insurance_fund"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_spot_market_cumulative_interest'
    #[flow]
    fn flow_handle_update_spot_market_cumulative_interest(&mut self) {
        let mut tx = HandleUpdateSpotMarketCumulativeInterestTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_spot_market_cumulative_interest"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_amms'
    #[flow]
    fn flow_handle_update_amms(&mut self) {
        let mut tx = HandleUpdateAmmsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_indexes(Default::default() /* Vec < u16 > */);
        self.trident.execute_transaction(&mut tx, Some("handle_update_amms"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'view_amm_liquidity'
    #[flow]
    fn flow_view_amm_liquidity(&mut self) {
        let mut tx = ViewAmmLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_market_indexes(Default::default() /* Vec < u16 > */);
        self.trident.execute_transaction(&mut tx, Some("view_amm_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_quote_asset_insurance_stake'
    #[flow]
    fn flow_handle_update_user_quote_asset_insurance_stake(&mut self) {
        let mut tx = HandleUpdateUserQuoteAssetInsuranceStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_quote_asset_insurance_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_user_gov_token_insurance_stake'
    #[flow]
    fn flow_handle_update_user_gov_token_insurance_stake(&mut self) {
        let mut tx = HandleUpdateUserGovTokenInsuranceStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_user_gov_token_insurance_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_disable_user_high_leverage_mode'
    #[flow]
    fn flow_handle_disable_user_high_leverage_mode(&mut self) {
        let mut tx = HandleDisableUserHighLeverageModeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_disable_maintenance(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("handle_disable_user_high_leverage_mode"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_force_delete_user'
    #[flow]
    fn flow_handle_force_delete_user(&mut self) {
        let mut tx = HandleForceDeleteUserTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_force_delete_user"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_pause_spot_market_deposit_withdraw'
    #[flow]
    fn flow_handle_pause_spot_market_deposit_withdraw(&mut self) {
        let mut tx = HandlePauseSpotMarketDepositWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_pause_spot_market_deposit_withdraw"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_settle_perp_to_lp_pool'
    #[flow]
    fn flow_handle_settle_perp_to_lp_pool(&mut self) {
        let mut tx = HandleSettlePerpToLpPoolTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_settle_perp_to_lp_pool"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_update_amm_cache'
    #[flow]
    fn flow_handle_update_amm_cache(&mut self) {
        let mut tx = HandleUpdateAmmCacheTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_update_amm_cache"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_take_order'
    #[flow]
    fn flow_place_take_order(&mut self) {
        let mut tx = PlaceTakeOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_side(Default::default() /* Side */);
        tx.set_price_lots(rand::random::<i64>());
        tx.set_max_base_lots(rand::random::<i64>());
        tx.set_max_quote_lots_including_fees(rand::random::<i64>());
        tx.set_order_type(Default::default() /* PlaceOrderType */);
        tx.set_limit(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("place_take_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'place_order'
    #[flow]
    fn flow_place_order(&mut self) {
        let mut tx = PlaceOrderTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_side(Default::default() /* Side */);
        tx.set_price_lots(rand::random::<i64>());
        tx.set_max_base_lots(rand::random::<i64>());
        tx.set_max_quote_lots_including_fees(rand::random::<i64>());
        tx.set_client_order_id(rand::random::<u64>());
        tx.set_order_type(Default::default() /* PlaceOrderType */);
        tx.set_expiry_timestamp(rand::random::<u64>());
        tx.set_limit(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("place_order"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_events'
    #[flow]
    fn flow_consume_events(&mut self) {
        let mut tx = ConsumeEventsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_limit(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("consume_events"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'check_context'
    #[flow]
    fn flow_check_context(&mut self) {
        let mut tx = CheckContextTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("check_context"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'change_authority'
    #[flow]
    fn flow_change_authority(&mut self) {
        let mut tx = ChangeAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_data(Default::default() /* ChangeAuthorityData */);
        self.trident.execute_transaction(&mut tx, Some("change_authority"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_validator'
    #[flow]
    fn flow_add_validator(&mut self) {
        let mut tx = AddValidatorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_score(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("add_validator"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'remove_validator'
    #[flow]
    fn flow_remove_validator(&mut self) {
        let mut tx = RemoveValidatorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_index(rand::random::<u32>());
        tx.set_validator_vote(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("remove_validator"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_validator_score'
    #[flow]
    fn flow_set_validator_score(&mut self) {
        let mut tx = SetValidatorScoreTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_index(rand::random::<u32>());
        tx.set_validator_vote(Pubkey::new_unique());
        tx.set_score(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("set_validator_score"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'config_validator_system'
    #[flow]
    fn flow_config_validator_system(&mut self) {
        let mut tx = ConfigValidatorSystemTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_extra_runs(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("config_validator_system"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit'
    #[flow]
    fn flow_deposit(&mut self) {
        let mut tx = DepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lamports(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit_stake_account'
    #[flow]
    fn flow_deposit_stake_account(&mut self) {
        let mut tx = DepositStakeAccountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_validator_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("deposit_stake_account"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'liquid_unstake'
    #[flow]
    fn flow_liquid_unstake(&mut self) {
        let mut tx = LiquidUnstakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_msol_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("liquid_unstake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_liquidity'
    #[flow]
    fn flow_add_liquidity(&mut self) {
        let mut tx = AddLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_lamports(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("add_liquidity"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'remove_liquidity'
    #[flow]
    fn flow_remove_liquidity(&mut self) {
        let mut tx = RemoveLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_tokens(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("remove_liquidity"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'config_lp'
    #[flow]
    fn flow_config_lp(&mut self) {
        let mut tx = ConfigLpTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* ConfigLpParams */);
        self.trident.execute_transaction(&mut tx, Some("config_lp"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'config_marinade'
    #[flow]
    fn flow_config_marinade(&mut self) {
        let mut tx = ConfigMarinadeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_params(Default::default() /* ConfigMarinadeParams */);
        self.trident.execute_transaction(&mut tx, Some("config_marinade"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'order_unstake'
    #[flow]
    fn flow_order_unstake(&mut self) {
        let mut tx = OrderUnstakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_msol_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("order_unstake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'claim'
    #[flow]
    fn flow_claim(&mut self) {
        let mut tx = ClaimTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("claim"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'stake_reserve'
    #[flow]
    fn flow_stake_reserve(&mut self) {
        let mut tx = StakeReserveTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_validator_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("stake_reserve"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_active'
    #[flow]
    fn flow_update_active(&mut self) {
        let mut tx = UpdateActiveTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_stake_index(rand::random::<u32>());
        tx.set_validator_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_active"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_deactivated'
    #[flow]
    fn flow_update_deactivated(&mut self) {
        let mut tx = UpdateDeactivatedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_stake_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_deactivated"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deactivate_stake'
    #[flow]
    fn flow_deactivate_stake(&mut self) {
        let mut tx = DeactivateStakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_stake_index(rand::random::<u32>());
        tx.set_validator_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("deactivate_stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'emergency_unstake'
    #[flow]
    fn flow_emergency_unstake(&mut self) {
        let mut tx = EmergencyUnstakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_stake_index(rand::random::<u32>());
        tx.set_validator_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("emergency_unstake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'partial_unstake'
    #[flow]
    fn flow_partial_unstake(&mut self) {
        let mut tx = PartialUnstakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_stake_index(rand::random::<u32>());
        tx.set_validator_index(rand::random::<u32>());
        tx.set_desired_unstake_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("partial_unstake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'merge_stakes'
    #[flow]
    fn flow_merge_stakes(&mut self) {
        let mut tx = MergeStakesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_destination_stake_index(rand::random::<u32>());
        tx.set_source_stake_index(rand::random::<u32>());
        tx.set_validator_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("merge_stakes"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'redelegate'
    #[flow]
    fn flow_redelegate(&mut self) {
        let mut tx = RedelegateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_stake_index(rand::random::<u32>());
        tx.set_source_validator_index(rand::random::<u32>());
        tx.set_dest_validator_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("redelegate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'pause'
    #[flow]
    fn flow_pause(&mut self) {
        let mut tx = PauseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("pause"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'resume'
    #[flow]
    fn flow_resume(&mut self) {
        let mut tx = ResumeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("resume"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw_stake_account'
    #[flow]
    fn flow_withdraw_stake_account(&mut self) {
        let mut tx = WithdrawStakeAccountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_stake_index(rand::random::<u32>());
        tx.set_validator_index(rand::random::<u32>());
        tx.set_msol_amount(rand::random::<u64>());
        tx.set_beneficiary(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("withdraw_stake_account"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'realloc_validator_list'
    #[flow]
    fn flow_realloc_validator_list(&mut self) {
        let mut tx = ReallocValidatorListTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_capacity(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("realloc_validator_list"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'realloc_stake_list'
    #[flow]
    fn flow_realloc_stake_list(&mut self) {
        let mut tx = ReallocStakeListTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_capacity(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("realloc_stake_list"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'validate_findings'
    #[flow]
    fn flow_validate_findings(&mut self) {
        let mut tx = ValidateFindingsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_findings(Default::default() /* Vec < VulnerabilityFinding > */);
        self.trident.execute_transaction(&mut tx, Some("validate_findings"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'validate_findings_with_threshold'
    #[flow]
    fn flow_validate_findings_with_threshold(&mut self) {
        let mut tx = ValidateFindingsWithThresholdTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_findings(Default::default() /* Vec < VulnerabilityFinding > */);
        tx.set_min_confidence(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("validate_findings_with_threshold"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'eliminate_proven_safe'
    #[flow]
    fn flow_eliminate_proven_safe(&mut self) {
        let mut tx = EliminateProvenSafeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_findings(Default::default() /* & [VulnerabilityFinding] */);
        self.trident.execute_transaction(&mut tx, Some("eliminate_proven_safe"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'is_proven_safe'
    #[flow]
    fn flow_is_proven_safe(&mut self) {
        let mut tx = IsProvenSafeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_finding(Default::default() /* & VulnerabilityFinding */);
        self.trident.execute_transaction(&mut tx, Some("is_proven_safe"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'assign_confidence'
    #[flow]
    fn flow_assign_confidence(&mut self) {
        let mut tx = AssignConfidenceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_findings(Default::default() /* & mut [VulnerabilityFinding] */);
        self.trident.execute_transaction(&mut tx, Some("assign_confidence"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_single_property'
    #[flow]
    fn flow_verify_single_property(&mut self) {
        let mut tx = VerifySinglePropertyTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_prop(Default::default() /* & VerifiableProperty */);
        self.trident.execute_transaction(&mut tx, Some("verify_single_property"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'check_solver_result'
    #[flow]
    fn flow_check_solver_result(&mut self) {
        let mut tx = CheckSolverResultTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_solver(Default::default() /* & z3 :: Solver */);
        tx.set_prop(Default::default() /* & VerifiableProperty */);
        tx.set_vars(Default::default() /* & HashMap < String , z3 :: ast :: BV > */);
        self.trident.execute_transaction(&mut tx, Some("check_solver_result"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'print_cash'
    #[flow]
    fn flow_print_cash(&mut self) {
        let mut tx = PrintCashTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_deposit_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("print_cash"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'burn_cash'
    #[flow]
    fn flow_burn_cash(&mut self) {
        let mut tx = BurnCashTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_burn_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("burn_cash"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'print_cash'
    #[flow]
    fn flow_print_cash(&mut self) {
        let mut tx = PrintCashTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_deposit_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("print_cash"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'burn_cash'
    #[flow]
    fn flow_burn_cash(&mut self) {
        let mut tx = BurnCashTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_burn_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("burn_cash"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'new_bank'
    #[flow]
    fn flow_new_bank(&mut self) {
        let mut tx = NewBankTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__bank_bump(rand::random::<u8>());
        tx.set_crate_bump(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("new_bank"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'authorize_collateral'
    #[flow]
    fn flow_authorize_collateral(&mut self) {
        let mut tx = AuthorizeCollateralTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__bump(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("authorize_collateral"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_collateral_hard_cap'
    #[flow]
    fn flow_set_collateral_hard_cap(&mut self) {
        let mut tx = SetCollateralHardCapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_hard_cap(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("set_collateral_hard_cap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_curator'
    #[flow]
    fn flow_set_curator(&mut self) {
        let mut tx = SetCuratorTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_curator"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_bankman'
    #[flow]
    fn flow_set_bankman(&mut self) {
        let mut tx = SetBankmanTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_bankman"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw_author_fee'
    #[flow]
    fn flow_withdraw_author_fee(&mut self) {
        let mut tx = WithdrawAuthorFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("withdraw_author_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handler'
    #[flow]
    fn flow_handler(&mut self) {
        let mut tx = HandlerTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handler"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_signatures'
    #[flow]
    fn flow_close_signatures(&mut self) {
        let mut tx = CloseSignaturesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("close_signatures"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_signatures'
    #[flow]
    fn flow_post_signatures(&mut self) {
        let mut tx = PostSignaturesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian_set_index(rand::random::<u32>());
        tx.set_total_signatures(rand::random::<u8>());
        tx.set_guardian_signatures(Default::default() /* Vec < [u8 ; 66] > */);
        self.trident.execute_transaction(&mut tx, Some("post_signatures"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_hash'
    #[flow]
    fn flow_verify_hash(&mut self) {
        let mut tx = VerifyHashTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian_set_bump(rand::random::<u8>());
        tx.set_digest(Default::default() /* [u8 ; HASH_BYTES] */);
        self.trident.execute_transaction(&mut tx, Some("verify_hash"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_message'
    #[flow]
    fn flow_post_message(&mut self) {
        let mut tx = PostMessageTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_nonce(rand::random::<u32>());
        tx.set_consistency_level(Default::default() /* Finality */);
        tx.set_payload(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("post_message"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_vaa'
    #[flow]
    fn flow_consume_vaa(&mut self) {
        let mut tx = ConsumeVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian_set_bump(rand::random::<u8>());
        tx.set_vaa_body(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("consume_vaa"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_message'
    #[flow]
    fn flow_post_message(&mut self) {
        let mut tx = PostMessageTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("post_message"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_vaa'
    #[flow]
    fn flow_consume_vaa(&mut self) {
        let mut tx = ConsumeVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian_set_bump(rand::random::<u8>());
        tx.set_vaa_body(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("consume_vaa"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_message'
    #[flow]
    fn flow_post_message(&mut self) {
        let mut tx = PostMessageTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("post_message"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_core_posted_vaa'
    #[flow]
    fn flow_consume_core_posted_vaa(&mut self) {
        let mut tx = ConsumeCorePostedVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_vaa_hash(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("consume_core_posted_vaa"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_signatures'
    #[flow]
    fn flow_post_signatures(&mut self) {
        let mut tx = PostSignaturesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian_signatures(Default::default() /* Vec < [u8 ; 66] > */);
        tx.set_total_signatures(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("post_signatures"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_vaa'
    #[flow]
    fn flow_consume_vaa(&mut self) {
        let mut tx = ConsumeVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_vaa_body(Default::default() /* Vec < u8 > */);
        tx.set_guardian_set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("consume_vaa"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_vaa_via_shim'
    #[flow]
    fn flow_consume_vaa_via_shim(&mut self) {
        let mut tx = ConsumeVaaViaShimTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian_set_bump(rand::random::<u8>());
        tx.set_vaa_body(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("consume_vaa_via_shim"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_core_posted_vaa'
    #[flow]
    fn flow_consume_core_posted_vaa(&mut self) {
        let mut tx = ConsumeCorePostedVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__vaa_hash(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("consume_core_posted_vaa"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_vaa_via_shim'
    #[flow]
    fn flow_consume_vaa_via_shim(&mut self) {
        let mut tx = ConsumeVaaViaShimTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian_set_bump(rand::random::<u8>());
        tx.set_vaa_body(Default::default() /* Vec < u8 > */);
        self.trident.execute_transaction(&mut tx, Some("consume_vaa_via_shim"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume_vaa'
    #[flow]
    fn flow_consume_vaa(&mut self) {
        let mut tx = ConsumeVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_vaa_body(Default::default() /* Vec < u8 > */);
        tx.set__guardian_set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("consume_vaa"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_signatures'
    #[flow]
    fn flow_post_signatures(&mut self) {
        let mut tx = PostSignaturesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_mut guardian_signatures(Default::default() /* Vec < [u8 ; 66] > */);
        tx.set__total_signatures(rand::random::<u8>());
        self.trident.execute_transaction(&mut tx, Some("post_signatures"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'invoke_seeded'
    #[flow]
    fn flow_invoke_seeded(&mut self) {
        let mut tx = InvokeSeededTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_instruction(Default::default() /* & Instruction */);
        tx.set_seeded_acc(Default::default() /* & T */);
        tx.set_accs(Default::default() /* I */);
        self.trident.execute_transaction(&mut tx, Some("invoke_seeded"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'consume'
    #[flow]
    fn flow_consume(&mut self) {
        let mut tx = ConsumeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_payer(Pubkey::new_unique());
        tx.set_claim(Default::default() /* & mut Claim */);
        tx.set_message(Default::default() /* & PayloadMessage < T > */);
        self.trident.execute_transaction(&mut tx, Some("consume"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_signatures'
    #[flow]
    fn flow_verify_signatures(&mut self) {
        let mut tx = VerifySignaturesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut VerifySignatures */);
        tx.set_data(Default::default() /* VerifySignaturesData */);
        self.trident.execute_transaction(&mut tx, Some("verify_signatures"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'upgrade_contract'
    #[flow]
    fn flow_upgrade_contract(&mut self) {
        let mut tx = UpgradeContractTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut UpgradeContract */);
        tx.set__data(Default::default() /* UpgradeContractData */);
        self.trident.execute_transaction(&mut tx, Some("upgrade_contract"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'upgrade_guardian_set'
    #[flow]
    fn flow_upgrade_guardian_set(&mut self) {
        let mut tx = UpgradeGuardianSetTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut UpgradeGuardianSet */);
        tx.set__data(Default::default() /* UpgradeGuardianSetData */);
        self.trident.execute_transaction(&mut tx, Some("upgrade_guardian_set"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_fees'
    #[flow]
    fn flow_set_fees(&mut self) {
        let mut tx = SetFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut SetFees */);
        tx.set__data(Default::default() /* SetFeesData */);
        self.trident.execute_transaction(&mut tx, Some("set_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_fees'
    #[flow]
    fn flow_transfer_fees(&mut self) {
        let mut tx = TransferFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut TransferFees */);
        tx.set__data(Default::default() /* TransferFeesData */);
        self.trident.execute_transaction(&mut tx, Some("transfer_fees"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_vaa'
    #[flow]
    fn flow_post_vaa(&mut self) {
        let mut tx = PostVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut PostVAA */);
        tx.set_vaa(Default::default() /* PostVAAData */);
        self.trident.execute_transaction(&mut tx, Some("post_vaa"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_message'
    #[flow]
    fn flow_post_message(&mut self) {
        let mut tx = PostMessageTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut PostMessage */);
        tx.set_data(Default::default() /* PostMessageData */);
        self.trident.execute_transaction(&mut tx, Some("post_message"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_message_unreliable'
    #[flow]
    fn flow_post_message_unreliable(&mut self) {
        let mut tx = PostMessageUnreliableTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut PostMessageUnreliable */);
        tx.set_data(Default::default() /* PostMessageData */);
        self.trident.execute_transaction(&mut tx, Some("post_message_unreliable"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_message_internal'
    #[flow]
    fn flow_post_message_internal(&mut self) {
        let mut tx = PostMessageInternalTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_bridge(Default::default() /* & mut Mut < Bridge < 'b , { AccountState :: Initialized } > > */);
        tx.set_# [cfg_attr (not (feature = "trace") , allow (unused_variables))] message_key(Pubkey::new_unique());
        tx.set_message(Default::default() /* & mut MessageData */);
        tx.set_emitter(Default::default() /* & mut Signer < MaybeMut < Info < 'b > > > */);
        tx.set_sequence(Default::default() /* & mut Mut < Sequence < 'b > > */);
        tx.set_payer(Default::default() /* & mut Mut < Signer < Info < 'b > > > */);
        tx.set_fee_collector(Default::default() /* & mut Mut < FeeCollector < 'b > > */);
        tx.set_clock(Default::default() /* & mut Sysvar < 'b , Clock > */);
        tx.set_data(Default::default() /* PostMessageData */);
        self.trident.execute_transaction(&mut tx, Some("post_message_internal"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_vaa'
    #[flow]
    fn flow_post_vaa(&mut self) {
        let mut tx = PostVaaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut PostVAA */);
        tx.set_vaa(Default::default() /* PostVAAData */);
        self.trident.execute_transaction(&mut tx, Some("post_vaa"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'post_message'
    #[flow]
    fn flow_post_message(&mut self) {
        let mut tx = PostMessageTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut PostMessage */);
        tx.set_data(Default::default() /* PostMessageData */);
        self.trident.execute_transaction(&mut tx, Some("post_message"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'attest_token'
    #[flow]
    fn flow_attest_token(&mut self) {
        let mut tx = AttestTokenTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut AttestToken */);
        tx.set_data(Default::default() /* AttestTokenData */);
        self.trident.execute_transaction(&mut tx, Some("attest_token"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'complete_native_with_payload'
    #[flow]
    fn flow_complete_native_with_payload(&mut self) {
        let mut tx = CompleteNativeWithPayloadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CompleteNativeWithPayload */);
        tx.set__data(Default::default() /* CompleteNativeWithPayloadData */);
        self.trident.execute_transaction(&mut tx, Some("complete_native_with_payload"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'complete_wrapped_with_payload'
    #[flow]
    fn flow_complete_wrapped_with_payload(&mut self) {
        let mut tx = CompleteWrappedWithPayloadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CompleteWrappedWithPayload */);
        tx.set__data(Default::default() /* CompleteWrappedWithPayloadData */);
        self.trident.execute_transaction(&mut tx, Some("complete_wrapped_with_payload"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_accounts'
    #[flow]
    fn flow_update_accounts(&mut self) {
        let mut tx = UpdateAccountsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CreateWrapped */);
        tx.set__data(Default::default() /* CreateWrappedData */);
        self.trident.execute_transaction(&mut tx, Some("update_accounts"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'complete_native'
    #[flow]
    fn flow_complete_native(&mut self) {
        let mut tx = CompleteNativeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CompleteNative */);
        tx.set__data(Default::default() /* CompleteNativeData */);
        self.trident.execute_transaction(&mut tx, Some("complete_native"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'complete_wrapped'
    #[flow]
    fn flow_complete_wrapped(&mut self) {
        let mut tx = CompleteWrappedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CompleteWrapped */);
        tx.set__data(Default::default() /* CompleteWrappedData */);
        self.trident.execute_transaction(&mut tx, Some("complete_wrapped"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_native'
    #[flow]
    fn flow_transfer_native(&mut self) {
        let mut tx = TransferNativeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut TransferNative */);
        tx.set_data(Default::default() /* TransferNativeData */);
        self.trident.execute_transaction(&mut tx, Some("transfer_native"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_and_execute_native_transfers'
    #[flow]
    fn flow_verify_and_execute_native_transfers(&mut self) {
        let mut tx = VerifyAndExecuteNativeTransfersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_derivation_data(Default::default() /* & CustodyAccountDerivationData */);
        tx.set_payer(Default::default() /* & Mut < Signer < AccountInfo > > */);
        tx.set_from(Default::default() /* & Mut < Data < SplAccount , { AccountState :: Initialized } > > */);
        tx.set_mint(Default::default() /* & Mut < Data < SplMint , { AccountState :: Initialized } > > */);
        tx.set_custody(Default::default() /* & Mut < CustodyAccount < { AccountState :: MaybeInitialized } > > */);
        tx.set_authority_signer(Default::default() /* & AuthoritySigner */);
        tx.set_custody_signer(Default::default() /* & CustodySigner */);
        tx.set_bridge(Default::default() /* & Mut < CoreBridge < { AccountState :: Initialized } > > */);
        tx.set_fee_collector(Default::default() /* & Mut < Info > */);
        tx.set_raw_amount(rand::random::<u64>());
        tx.set_raw_fee(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("verify_and_execute_native_transfers"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_wrapped'
    #[flow]
    fn flow_transfer_wrapped(&mut self) {
        let mut tx = TransferWrappedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut TransferWrapped */);
        tx.set_data(Default::default() /* TransferWrappedData */);
        self.trident.execute_transaction(&mut tx, Some("transfer_wrapped"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_and_execute_wrapped_transfers'
    #[flow]
    fn flow_verify_and_execute_wrapped_transfers(&mut self) {
        let mut tx = VerifyAndExecuteWrappedTransfersTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_derivation_data(Default::default() /* & WrappedMetaDerivationData */);
        tx.set_payer(Default::default() /* & Mut < Signer < AccountInfo > > */);
        tx.set_from(Default::default() /* & Mut < Data < SplAccount , { AccountState :: Initialized } > > */);
        tx.set_from_owner(Default::default() /* & MaybeMut < Signer < Info > > */);
        tx.set_mint(Default::default() /* & Mut < WrappedMint < { AccountState :: Initialized } > > */);
        tx.set_wrapped_meta(Default::default() /* & WrappedTokenMeta < { AccountState :: Initialized } > */);
        tx.set_authority_signer(Default::default() /* & AuthoritySigner */);
        tx.set_bridge(Default::default() /* & Mut < CoreBridge < { AccountState :: Initialized } > > */);
        tx.set_fee_collector(Default::default() /* & Mut < Info > */);
        tx.set_amount(rand::random::<u64>());
        tx.set_fee(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("verify_and_execute_wrapped_transfers"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'upgrade_contract'
    #[flow]
    fn flow_upgrade_contract(&mut self) {
        let mut tx = UpgradeContractTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut UpgradeContract */);
        tx.set__data(Default::default() /* UpgradeContractData */);
        self.trident.execute_transaction(&mut tx, Some("upgrade_contract"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_native_with_payload'
    #[flow]
    fn flow_transfer_native_with_payload(&mut self) {
        let mut tx = TransferNativeWithPayloadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut TransferNativeWithPayload */);
        tx.set_data(Default::default() /* TransferNativeWithPayloadData */);
        self.trident.execute_transaction(&mut tx, Some("transfer_native_with_payload"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_wrapped_with_payload'
    #[flow]
    fn flow_transfer_wrapped_with_payload(&mut self) {
        let mut tx = TransferWrappedWithPayloadTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut TransferWrappedWithPayload */);
        tx.set_data(Default::default() /* TransferWrappedWithPayloadData */);
        self.trident.execute_transaction(&mut tx, Some("transfer_wrapped_with_payload"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'complete_native'
    #[flow]
    fn flow_complete_native(&mut self) {
        let mut tx = CompleteNativeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CompleteNative */);
        tx.set__data(Default::default() /* CompleteNativeData */);
        self.trident.execute_transaction(&mut tx, Some("complete_native"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'complete_wrapped'
    #[flow]
    fn flow_complete_wrapped(&mut self) {
        let mut tx = CompleteWrappedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CompleteWrapped */);
        tx.set__data(Default::default() /* CompleteWrappedData */);
        self.trident.execute_transaction(&mut tx, Some("complete_wrapped"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'complete_wrapped_meta'
    #[flow]
    fn flow_complete_wrapped_meta(&mut self) {
        let mut tx = CompleteWrappedMetaTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut CompleteWrappedMeta */);
        tx.set__data(Default::default() /* CompleteWrappedMetaData */);
        self.trident.execute_transaction(&mut tx, Some("complete_wrapped_meta"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_native'
    #[flow]
    fn flow_transfer_native(&mut self) {
        let mut tx = TransferNativeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut TransferNative */);
        tx.set_data(Default::default() /* TransferNativeData */);
        self.trident.execute_transaction(&mut tx, Some("transfer_native"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_wrapped'
    #[flow]
    fn flow_transfer_wrapped(&mut self) {
        let mut tx = TransferWrappedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut TransferWrapped */);
        tx.set_data(Default::default() /* TransferWrappedData */);
        self.trident.execute_transaction(&mut tx, Some("transfer_wrapped"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'upgrade_contract'
    #[flow]
    fn flow_upgrade_contract(&mut self) {
        let mut tx = UpgradeContractTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut UpgradeContract */);
        tx.set__data(Default::default() /* UpgradeContractData */);
        self.trident.execute_transaction(&mut tx, Some("upgrade_contract"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'migrate_tokens'
    #[flow]
    fn flow_migrate_tokens(&mut self) {
        let mut tx = MigrateTokensTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut MigrateTokens */);
        tx.set_data(Default::default() /* MigrateTokensData */);
        self.trident.execute_transaction(&mut tx, Some("migrate_tokens"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'remove_liquidity'
    #[flow]
    fn flow_remove_liquidity(&mut self) {
        let mut tx = RemoveLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut RemoveLiquidity */);
        tx.set_data(Default::default() /* RemoveLiquidityData */);
        self.trident.execute_transaction(&mut tx, Some("remove_liquidity"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'claim_shares'
    #[flow]
    fn flow_claim_shares(&mut self) {
        let mut tx = ClaimSharesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut ClaimShares */);
        tx.set_data(Default::default() /* ClaimSharesData */);
        self.trident.execute_transaction(&mut tx, Some("claim_shares"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_liquidity'
    #[flow]
    fn flow_add_liquidity(&mut self) {
        let mut tx = AddLiquidityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_accs(Default::default() /* & mut AddLiquidity */);
        tx.set_data(Default::default() /* AddLiquidityData */);
        self.trident.execute_transaction(&mut tx, Some("add_liquidity"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'prepare_batch_merkle_tree'
    #[flow]
    fn flow_prepare_batch_merkle_tree(&mut self) {
        let mut tx = PrepareBatchMerkleTreeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_max_depth(rand::random::<u32>());
        tx.set_max_buffer_size(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("prepare_batch_merkle_tree"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'append_canopy_nodes'
    #[flow]
    fn flow_append_canopy_nodes(&mut self) {
        let mut tx = AppendCanopyNodesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_start_index(rand::random::<u32>());
        tx.set_canopy_nodes(Default::default() /* Vec < [u8 ; 32] > */);
        self.trident.execute_transaction(&mut tx, Some("append_canopy_nodes"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'replace_leaf'
    #[flow]
    fn flow_replace_leaf(&mut self) {
        let mut tx = ReplaceLeafTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_root(Default::default() /* [u8 ; 32] */);
        tx.set_previous_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_new_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("replace_leaf"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_authority'
    #[flow]
    fn flow_transfer_authority(&mut self) {
        let mut tx = TransferAuthorityTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_authority(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("transfer_authority"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'verify_leaf'
    #[flow]
    fn flow_verify_leaf(&mut self) {
        let mut tx = VerifyLeafTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_root(Default::default() /* [u8 ; 32] */);
        tx.set_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("verify_leaf"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'append'
    #[flow]
    fn flow_append(&mut self) {
        let mut tx = AppendTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_leaf(Default::default() /* [u8 ; 32] */);
        self.trident.execute_transaction(&mut tx, Some("append"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'insert_or_append'
    #[flow]
    fn flow_insert_or_append(&mut self) {
        let mut tx = InsertOrAppendTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_root(Default::default() /* [u8 ; 32] */);
        tx.set_leaf(Default::default() /* [u8 ; 32] */);
        tx.set_index(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("insert_or_append"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'close_empty_tree'
    #[flow]
    fn flow_close_empty_tree(&mut self) {
        let mut tx = CloseEmptyTreeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("close_empty_tree"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'sign_send_instruction'
    #[flow]
    fn flow_sign_send_instruction(&mut self) {
        let mut tx = SignSendInstructionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_instruction(Default::default() /* Instruction */);
        tx.set_signers(Default::default() /* Vec < & Keypair > */);
        self.trident.execute_transaction(&mut tx, Some("sign_send_instruction"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'setup_group'
    #[flow]
    fn flow_setup_group(&mut self) {
        let mut tx = SetupGroupTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_program_id(Pubkey::new_unique());
        tx.set_group(Default::default() /* & Keypair */);
        tx.set_mint(Default::default() /* & Keypair */);
        tx.set_mint_authority(Default::default() /* & Keypair */);
        tx.set_update_authority(Pubkey::new_unique());
        tx.set_max_size(rand::random::<u64>());
        tx.set_rent_lamports(rand::random::<u64>());
        tx.set_space(Default::default() /* usize */);
        self.trident.execute_transaction(&mut tx, Some("setup_group"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);
    }

    /// Fuzz flow for instruction 'make_decision'
    #[flow]
    fn flow_make_decision(&mut self) {
        let mut tx = MakeDecisionTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_account(Pubkey::new_unique());
        tx.set_decider(Default::default() /* & Keypair */);
        tx.set_decision(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("make_decision"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'make_withdraw'
    #[flow]
    fn flow_make_withdraw(&mut self) {
        let mut tx = MakeWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_account(Pubkey::new_unique());
        tx.set_authority(Pubkey::new_unique());
        tx.set_user_transfer_authority(Pubkey::new_unique());
        tx.set_pool_deposit_account(Pubkey::new_unique());
        tx.set_user_pass_account(Pubkey::new_unique());
        tx.set_user_fail_account(Pubkey::new_unique());
        tx.set_token_pass_mint(Pubkey::new_unique());
        tx.set_token_fail_mint(Pubkey::new_unique());
        tx.set_user_account(Pubkey::new_unique());
        tx.set_withdraw_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("make_withdraw"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_token_withdraw'
    #[flow]
    fn flow_handle_token_withdraw(&mut self) {
        let mut tx = HandleTokenWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_(amount , admin_fee)(Default::default() /* (u64 , u64) */);
        tx.set_reserves_info(Default::default() /* & 'a AccountInfo < 'b > */);
        tx.set_dest_token_info(Default::default() /* & 'a AccountInfo < 'b > */);
        tx.set_admin_fee_dest_info(Default::default() /* & 'a AccountInfo < 'b > */);
        self.trident.execute_transaction(&mut tx, Some("handle_token_withdraw"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deposit'
    #[flow]
    fn flow_deposit(&mut self) {
        let mut tx = DepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_token_a_amount(rand::random::<u64>());
        tx.set_token_b_amount(rand::random::<u64>());
        tx.set_min_mint_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("deposit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'swap'
    #[flow]
    fn flow_swap(&mut self) {
        let mut tx = SwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount_in(rand::random::<u64>());
        tx.set_minimum_amount_out(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw_one'
    #[flow]
    fn flow_withdraw_one(&mut self) {
        let mut tx = WithdrawOneTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_token_amount(rand::random::<u64>());
        tx.set_minimum_token_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("withdraw_one"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'withdraw'
    #[flow]
    fn flow_withdraw(&mut self) {
        let mut tx = WithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_pool_token_amount(rand::random::<u64>());
        tx.set_minimum_token_a_amount(rand::random::<u64>());
        tx.set_minimum_token_b_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("withdraw"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'ramp_a'
    #[flow]
    fn flow_ramp_a(&mut self) {
        let mut tx = RampATransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_target_amp(rand::random::<u64>());
        tx.set_stop_ramp_ts(rand::random::<i64>());
        self.trident.execute_transaction(&mut tx, Some("ramp_a"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'stop_ramp_a'
    #[flow]
    fn flow_stop_ramp_a(&mut self) {
        let mut tx = StopRampATransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("stop_ramp_a"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'pause'
    #[flow]
    fn flow_pause(&mut self) {
        let mut tx = PauseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("pause"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'unpause'
    #[flow]
    fn flow_unpause(&mut self) {
        let mut tx = UnpauseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("unpause"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'apply_new_admin'
    #[flow]
    fn flow_apply_new_admin(&mut self) {
        let mut tx = ApplyNewAdminTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("apply_new_admin"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'commit_new_admin'
    #[flow]
    fn flow_commit_new_admin(&mut self) {
        let mut tx = CommitNewAdminTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("commit_new_admin"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_fee_account'
    #[flow]
    fn flow_set_fee_account(&mut self) {
        let mut tx = SetFeeAccountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("set_fee_account"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_new_fees'
    #[flow]
    fn flow_set_new_fees(&mut self) {
        let mut tx = SetNewFeesTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_fees(Default::default() /* stable_swap_client :: fees :: Fees */);
        self.trident.execute_transaction(&mut tx, Some("set_new_fees"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'mint_tokens'
    #[flow]
    fn flow_mint_tokens(&mut self) {
        let mut tx = MintTokensTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("mint_tokens"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'open_mint'
    #[flow]
    fn flow_open_mint(&mut self) {
        let mut tx = OpenMintTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("open_mint"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'transfer_tokens'
    #[flow]
    fn flow_transfer_tokens(&mut self) {
        let mut tx = TransferTokensTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("transfer_tokens"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'delegate_transfer'
    #[flow]
    fn flow_delegate_transfer(&mut self) {
        let mut tx = DelegateTransferTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("delegate_transfer"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'freeze_account'
    #[flow]
    fn flow_freeze_account(&mut self) {
        let mut tx = FreezeAccountTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("freeze_account"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'collect_fee'
    #[flow]
    fn flow_collect_fee(&mut self) {
        let mut tx = CollectFeeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        tx.set_fee_bps(rand::random::<u16>());
        self.trident.execute_transaction(&mut tx, Some("collect_fee"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'submit_assessment'
    #[flow]
    fn flow_submit_assessment(&mut self) {
        let mut tx = SubmitAssessmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_target_program(Pubkey::new_unique());
        tx.set_flags(Default::default() /* Vec < FlagInput > */);
        tx.set_report_ipfs_cid(Default::default() /* Vec < u8 > */);
        tx.set_target_program_version(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("submit_assessment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_assessment'
    #[flow]
    fn flow_update_assessment(&mut self) {
        let mut tx = UpdateAssessmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_target_program(Pubkey::new_unique());
        tx.set_flags(Default::default() /* Vec < FlagInput > */);
        tx.set_report_ipfs_cid(Default::default() /* Vec < u8 > */);
        tx.set_target_program_version(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_assessment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'confirm_assessment'
    #[flow]
    fn flow_confirm_assessment(&mut self) {
        let mut tx = ConfirmAssessmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_target_program(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("confirm_assessment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'query_risk'
    #[flow]
    fn flow_query_risk(&mut self) {
        let mut tx = QueryRiskTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_target_program(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("query_risk"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_guardian'
    #[flow]
    fn flow_add_guardian(&mut self) {
        let mut tx = AddGuardianTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_guardian(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("add_guardian"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'remove_guardian'
    #[flow]
    fn flow_remove_guardian(&mut self) {
        let mut tx = RemoveGuardianTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("remove_guardian"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_paused'
    #[flow]
    fn flow_set_paused(&mut self) {
        let mut tx = SetPausedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("set_paused"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'propose_authority_transfer'
    #[flow]
    fn flow_propose_authority_transfer(&mut self) {
        let mut tx = ProposeAuthorityTransferTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_authority(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("propose_authority_transfer"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'accept_authority_transfer'
    #[flow]
    fn flow_accept_authority_transfer(&mut self) {
        let mut tx = AcceptAuthorityTransferTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("accept_authority_transfer"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'cancel_authority_transfer'
    #[flow]
    fn flow_cancel_authority_transfer(&mut self) {
        let mut tx = CancelAuthorityTransferTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("cancel_authority_transfer"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deactivate_analyst'
    #[flow]
    fn flow_deactivate_analyst(&mut self) {
        let mut tx = DeactivateAnalystTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_analyst_wallet(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("deactivate_analyst"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'add_guardian'
    #[flow]
    fn flow_add_guardian(&mut self) {
        let mut tx = AddGuardianTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_guardian(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("add_guardian"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'remove_guardian'
    #[flow]
    fn flow_remove_guardian(&mut self) {
        let mut tx = RemoveGuardianTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_guardian(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("remove_guardian"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'set_paused'
    #[flow]
    fn flow_set_paused(&mut self) {
        let mut tx = SetPausedTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_paused(rand::random::<bool>());
        self.trident.execute_transaction(&mut tx, Some("set_paused"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'propose_authority_transfer'
    #[flow]
    fn flow_propose_authority_transfer(&mut self) {
        let mut tx = ProposeAuthorityTransferTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_authority(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("propose_authority_transfer"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'accept_authority_transfer'
    #[flow]
    fn flow_accept_authority_transfer(&mut self) {
        let mut tx = AcceptAuthorityTransferTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("accept_authority_transfer"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'cancel_authority_transfer'
    #[flow]
    fn flow_cancel_authority_transfer(&mut self) {
        let mut tx = CancelAuthorityTransferTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("cancel_authority_transfer"));

        // Property: balance conservation check
        // assert!(pre_balance >= post_balance || authorized_mint);

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'deactivate_analyst'
    #[flow]
    fn flow_deactivate_analyst(&mut self) {
        let mut tx = DeactivateAnalystTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_analyst_wallet(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("deactivate_analyst"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'confirm_assessment'
    #[flow]
    fn flow_confirm_assessment(&mut self) {
        let mut tx = ConfirmAssessmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_target_program(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("confirm_assessment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_assessment'
    #[flow]
    fn flow_update_assessment(&mut self) {
        let mut tx = UpdateAssessmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__target_program(Pubkey::new_unique());
        tx.set_flags(Default::default() /* Vec < FlagInput > */);
        tx.set_report_ipfs_cid(Default::default() /* Vec < u8 > */);
        tx.set_target_program_version(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("update_assessment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'submit_assessment'
    #[flow]
    fn flow_submit_assessment(&mut self) {
        let mut tx = SubmitAssessmentTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_target_program(Pubkey::new_unique());
        tx.set_flags(Default::default() /* Vec < FlagInput > */);
        tx.set_report_ipfs_cid(Default::default() /* Vec < u8 > */);
        tx.set_target_program_version(rand::random::<u32>());
        self.trident.execute_transaction(&mut tx, Some("submit_assessment"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'query_risk'
    #[flow]
    fn flow_query_risk(&mut self) {
        let mut tx = QueryRiskTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set__target_program(Pubkey::new_unique());
        self.trident.execute_transaction(&mut tx, Some("query_risk"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'stake'
    #[flow]
    fn flow_stake(&mut self) {
        let mut tx = StakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("stake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'unstake'
    #[flow]
    fn flow_unstake(&mut self) {
        let mut tx = UnstakeTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("unstake"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'claim_rewards'
    #[flow]
    fn flow_claim_rewards(&mut self) {
        let mut tx = ClaimRewardsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("claim_rewards"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'update_reward_rate'
    #[flow]
    fn flow_update_reward_rate(&mut self) {
        let mut tx = UpdateRewardRateTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_new_rate(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("update_reward_rate"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'emergency_withdraw'
    #[flow]
    fn flow_emergency_withdraw(&mut self) {
        let mut tx = EmergencyWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("emergency_withdraw"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'compound_rewards'
    #[flow]
    fn flow_compound_rewards(&mut self) {
        let mut tx = CompoundRewardsTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("compound_rewards"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_deposit'
    #[flow]
    fn flow_handle_deposit(&mut self) {
        let mut tx = HandleDepositTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_deposit"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_withdraw'
    #[flow]
    fn flow_handle_withdraw(&mut self) {
        let mut tx = HandleWithdrawTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_withdraw"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_get_secure_price'
    #[flow]
    fn flow_handle_get_secure_price(&mut self) {
        let mut tx = HandleGetSecurePriceTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_get_secure_price"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_emergency_pause'
    #[flow]
    fn flow_handle_emergency_pause(&mut self) {
        let mut tx = HandleEmergencyPauseTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        self.trident.execute_transaction(&mut tx, Some("handle_emergency_pause"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_vote_on_proposal'
    #[flow]
    fn flow_handle_vote_on_proposal(&mut self) {
        let mut tx = HandleVoteOnProposalTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_vote_weight(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_vote_on_proposal"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

    /// Fuzz flow for instruction 'handle_swap'
    #[flow]
    fn flow_handle_swap(&mut self) {
        let mut tx = HandleSwapTransaction::build(
            &mut self.trident,
            &mut self.fuzz_accounts,
        );
        tx.set_amount(rand::random::<u64>());
        self.trident.execute_transaction(&mut tx, Some("handle_swap"));

        // Property: no arithmetic overflow
        // Trident monitors for panics from unchecked math
    }

}
