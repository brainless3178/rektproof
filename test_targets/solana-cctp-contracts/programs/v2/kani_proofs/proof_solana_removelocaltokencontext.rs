//! Kani proof harness for Solana account: RemoveLocalTokenContext
//! Auto-generated by kani-verifier — DO NOT EDIT
//!
//! Source: remove_local_token.rs
//! Constraints checked: 10

/// Symbolic representation of the Solana account for verification
struct SymbolicRemoveLocalTokenContext {
    payee: u64,
    token_controller: u64,
    token_minter: u64,
    local_token: u64,
    custody_token_account: u64,
    custody_token_mint: u64,
    token_program: u64,
}

impl kani::Arbitrary for SymbolicRemoveLocalTokenContext {
    fn any() -> Self {
        Self {
            payee: kani::any(),
            token_controller: kani::any(),
            token_minter: kani::any(),
            local_token: kani::any(),
            custody_token_account: kani::any(),
            custody_token_mint: kani::any(),
            token_program: kani::any(),
        }
    }
}

/// Verify: payee.is_signer == true  /* Anchor Signer type enforces signer check */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_0() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: payee.is_signer == true  /* Anchor Signer type enforces signer check */
    kani::assert({ payee.is_signer == true  /* Anchor Signer type enforces signer check */ }, "Invariant violation: payee.is_signer == true  /* Anchor Signer type enforces signer check */");
}

/// Verify: payee is writable  /* Account marked as mutable */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_1() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: payee is writable  /* Account marked as mutable */
    kani::assert({ let _ = &account; true /* TODO: encode 'payee is writable  /* Account marked as mutable */' */ }, "Invariant violation: payee is writable  /* Account marked as mutable */");
}

/// Verify: token_controller.is_signer == true  /* Anchor Signer type enforces signer check */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_2() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: token_controller.is_signer == true  /* Anchor Signer type enforces signer check */
    kani::assert({ token_controller.is_signer == true  /* Anchor Signer type enforces signer check */ }, "Invariant violation: token_controller.is_signer == true  /* Anchor Signer type enforces signer check */");
}

/// Verify: token_minter has_one constraint enforced  /* Ownership/relationship validated */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_3() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: token_minter has_one constraint enforced  /* Ownership/relationship validated */
    kani::assert({ let _ = &account; true /* TODO: encode 'token_minter has_one constraint enforced  /* Ownership/relationship validated */' */ }, "Invariant violation: token_minter has_one constraint enforced  /* Ownership/relationship validated */");
}

/// Verify: local_token is writable  /* Account marked as mutable */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_4() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: local_token is writable  /* Account marked as mutable */
    kani::assert({ let _ = &account; true /* TODO: encode 'local_token is writable  /* Account marked as mutable */' */ }, "Invariant violation: local_token is writable  /* Account marked as mutable */");
}

/// Verify: local_token PDA validated via seeds  /* PDA derivation checked */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_5() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: local_token PDA validated via seeds  /* PDA derivation checked */
    kani::assert({ let _ = &account; true /* TODO: encode 'local_token PDA validated via seeds  /* PDA derivation checked */' */ }, "Invariant violation: local_token PDA validated via seeds  /* PDA derivation checked */");
}

/// Verify: custody_token_account is writable  /* Account marked as mutable */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_6() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: custody_token_account is writable  /* Account marked as mutable */
    kani::assert({ let _ = &account; true /* TODO: encode 'custody_token_account is writable  /* Account marked as mutable */' */ }, "Invariant violation: custody_token_account is writable  /* Account marked as mutable */");
}

/// Verify: custody_token_account PDA validated via seeds  /* PDA derivation checked */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_7() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: custody_token_account PDA validated via seeds  /* PDA derivation checked */
    kani::assert({ let _ = &account; true /* TODO: encode 'custody_token_account PDA validated via seeds  /* PDA derivation checked */' */ }, "Invariant violation: custody_token_account PDA validated via seeds  /* PDA derivation checked */");
}

/// Verify: custody_token_mint is writable  /* Account marked as mutable */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_8() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: custody_token_mint is writable  /* Account marked as mutable */
    kani::assert({ let _ = &account; true /* TODO: encode 'custody_token_mint is writable  /* Account marked as mutable */' */ }, "Invariant violation: custody_token_mint is writable  /* Account marked as mutable */");
}

/// Verify: custody_token_mint custom constraint active  /* User-defined constraint applied */
#[cfg(kani)]
#[kani::proof]
#[kani::unwind(10)]
fn proof_removelocaltokencontext_constraint_9() {
    let account: SymbolicRemoveLocalTokenContext = kani::any();

    // Preconditions: constrain symbolic values to realistic ranges

    // Postcondition: verify constraint
    // Invariant: custody_token_mint custom constraint active  /* User-defined constraint applied */
    kani::assert({ let _ = &account; true /* TODO: encode 'custody_token_mint custom constraint active  /* User-defined constraint applied */' */ }, "Invariant violation: custody_token_mint custom constraint active  /* User-defined constraint applied */");
}

